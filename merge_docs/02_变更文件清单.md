# 02 · 变更文件清单（逐文件 before / after）

> 基准：`SurveyOfFTSG/SurveyOfFTSG-main_clean/` → 目标：`SurveyOfFTSG-main0913/`
> 规模：**11 个文件、+44 / −42 行、37 个独立改动点**
> 机器可读原始 diff：`_data/diff_all_sections.txt`（317 行）

---

## 1. 汇总表

| # | 文件 | +行 | −行 | 改动点 | 主题 |
|---|---|---:|---:|---:|---|
| 1 | `01_introduction.tex` | 1 | 1 | 1 | 六族表述 |
| 2 | `02_methodology.tex` | 1 | 1 | 1 | 语料数字 47→48 |
| 3 | `03_problem_taxonomy.tex` | 2 | 2 | 2 | key 改写 + 去具名 |
| 4 | `04_extrapolation.tex` | 5 | 5 | 5 | key 改写 + 去具名 + 语法 |
| 5 | `05_imputation.tex` | 11 | 11 | 11 | 去具名（主战场） |
| 6 | `06_synthesis.tex` | 5 | 5 | 5 | 去具名 + key 改写 |
| 7 | `07_applications.tex` | 9 | 7 | 3 | 去具名 |
| 8 | `09_data_resources.tex` | 1 | 1 | 1 | key 大小写 |
| 9 | `10_challenges.tex` | 1 | 1 | 1 | key 改写 |
| 10 | `13_figures.tex` | 2 | 2 | 2 | 图注 |
| 11 | `14_tables.tex` | 6 | 6 | 5 | 表格 |

**未改动**：`00_abstract.tex`、`08_evaluation.tex`、`12_declarations.tex`

---

## 2. 改动主题归因

今日改动可归为 **5 类**，与审稿意见一一对应：

| 主题 | 对应审稿意见 | 涉及文件 |
|---|---|---|
| **A. 别名 key → author-year key** | 重复参考文献 | 03, 04, 06, 09, 10, 14 |
| **B. 去具名（删虚构作者名）** | 虚构引用 | 03, 04, 05, 06, 07 |
| **C. 数字自洽** | 数字自洽性 | 02, 14 |
| **D. 图注纠错** | 图文不一致 / 孤儿名词 | 01, 13 |
| **E. 语法与文风** | 语法与文风 | 04, 06, 07 |

---

## 3. A 类：别名 key → author-year key

仓库的 bib 用**厂商/模型名**做 key，WS 统一改为 **author-year**。正文与 bib 同步改写，
故必须成对替换。

### `03_problem_taxonomy.tex:30`
```diff
- Statistical and probabilistic methods include classical approaches such as ARIMA~\citep{box1970}
+ Statistical and probabilistic methods include classical approaches such as ARIMA~\citep{box2015time}
```

### `09_data_resources.tex:26`
```diff
- GIFT-Eval~\citep{aksu2024gifteval}, ..., TFB~\citep{qiu2024tfb},
+ GIFT-Eval~\citep{GIFT-Eval},        ..., TFB~\citep{TFB},
```

### `10_challenges.tex:12`
```diff
- \cite{gholamrezaei2023conditioning, T2S, lozano2023dual},
+ \cite{T2S, wang2024mtsisurvey},
- \cite{Time-LLM,
+ \cite{jin2024timellm,
- \cite{lozano2023dual},
+ \cite{T2S},
```

### `14_tables.tex`
```diff
- TimeDiT \citep{TimeDiT}
+ TimeDiT \citep{cao2024timedit}
- \textbf{GIFT-EVAL} \citep{GIFT-EVAL}
+ \textbf{GIFT-Eval} \citep{GIFT-Eval}
```

### `04_extrapolation.tex:20`
```diff
- \citep{box1970},
+ \citep{box2015time},
```

> ℹ️ **注意反向情况**：`GIFT-Eval` 在 WS 里是**保留**的 key（`09_data_resources` 从
> `aksu2024gifteval` 改为 `GIFT-Eval`），而 `Time-LLM` 在 WS 里改为 `jin2024timellm`。
> 这是 Phase 1 去重时做的取舍，**不是笔误**。

---

## 4. B 类：去具名（删除虚构作者名）

审稿意见指出部分具名归属的文献无法核实。策略：**保留技术论断，去掉具名归属**，
并补上可核实的真实文献。

### `03_problem_taxonomy.tex:40`
```diff
- Controllable and multimodal generative models, including TimeWeaver~\citep{narasimhan2024timeweaver},
- T2S~\citep{T2S}, and dual-sourced text--time diffusion models~\citep{lozano2023dual},
- are designed to generate sequences that respond to external drivers and market regimes.
+ Controllable and multimodal generative models, including TimeWeaver~\citep{narasimhan2024timeweaver}
+ and T2S~\citep{T2S}, are designed to generate sequences that respond to external drivers
+ and market regimes~\citep{wang2024mtsisurvey}.
```

### `04_extrapolation.tex:30`
```diff
- ... Qin et al.~\citep{qin2025}, ...
+ ... (整段删除，并补 WGAN-GP 稳定化表述)
```

### `04_extrapolation.tex:61`
```diff
- WaveGAN \citep{dai2024wavegan} incorporates a multi-resolution DWT denoising mechanism combined with a
+ A wavelet-based GAN~\citep{dai2024wavegan} instead transforms price trajectories into the wavelet
+ domain before adversarial learning, generating synthetic stock market trends that outperform
+ deep learning baselines.
```
> 技术论断保留（小波域对抗学习），具名 "WaveGAN" + 具体机制描述（multi-resolution DWT
> denoising）改为概括表述。

### `05_imputation.tex:10`（Khuwaja 去具名）
```diff
- The heterogeneous-data ... Khuwaja et al. \citep{NDDP} is included here as a related
- data-driven baseline. Operating in a continuous functional space, Kernel ridge regression~\citep{saunders1998dual,aronszajn1950theory}, in a f...
+ An ... using heterogeneous data sources ... applications~\citep{NDDP} is included here as a
+ related data-driven baseline. Operating in a continuous functional space, kernel ridge
+ regression~\citep{saunders1998dual,aronszajn1950theory} has been ... every estimator they compared.
```
> ✅ `NDDP` 这个 key 本身是**真实文献**，只是"Khuwaja et al."的具名归属不准确 → 只去掉人名。

### `05_imputation.tex:16`（Wang 去具名）
```diff
- the BiGRU-BiLSTM framework proposed by Wang et al.~\citep{wang2021bigru},
+ bidirectional GRU--LSTM frameworks,
```

### `05_imputation.tex:36`
```diff
- ... horizons \citep{zhang2024spatial}, ... boundaries \citep{zhou2024cross}.
+ ... horizons, ... across boundaries.
```

### `05_imputation.tex:44`（两篇虚构综述 → 两篇真实综述）
```diff
- surveys by Shan et al.~\citep{shan2021} and Du et al.~\citep{du2024},
+ surveys~\citep{wang2024mtsisurvey,richter2024survey},
```

### `05_imputation.tex:46`
```diff
- Miao et al.~\citep{miao2025} propose ... Zhang et al.~\citep{zhang2023imputation} use ...
+ ... have been developed ... simultaneously capture local and global dependencies,
+ while temporal graph networks (TGNs)~\citep{rossi2020temporal} map dynamic cross-asset spillovers.
- ... during frag... data.
+ ... data~\citep{T2S}.
```
> 注意：`Cai et al.~\citep{cai2025}` 也被删除，`TGNs` 归属统一到真实的
> `rossi2020temporal`。

### `05_imputation.tex:48`
```diff
- Bamford et al.~\citep{bamford2023} and Shankar et al.~\citep{shankar2025} have proposed
+ ... have been proposed ... imbalances~\citep{lobench2025}.
- Nater and Khayati~\citep{nater2025} introduce ... shape.
+ ... are being developed ... shape~\citep{richter2024survey}.
```

### `05_imputation.tex:50`
```diff
- Qian et al.~\citep{qian2024} and Gao et al.~\citep{gao2024} introduce ... volatility.
+ ... have been introduced ... volatility~\citep{wang2024mtsisurvey}.
- Wi et al.~\citep{wi2023} emphasize ... Wang et al.~\citep{wang2024c} investigate ...
+ Imputation fidelity must be validated during simulated market crashes rather than strictly
+ during stationary periods, ensuring ... Such release-timing effects are empirically
+ detectable ... macro-driven markets~\citep{frino2018macroeconomic}.
```

### `05_imputation.tex:26`（AttnWGAIN / ImputeGAN 技术论断修正）
```diff
- AttnWGAIN \citep{wang2025attn} substitutes traditional ... layers ... diagonal ... explicitly
- learn temporal ... feature-wise correlations ... accessing masked future tokens.
- Similarly, ImputeGAN~\citep{qin2023imputegan} leverages an Informer-based generator~\citep{zhou2021informer}
- utilizing probabilistic sparse attention, significantly red...
+ AttnWGAIN~\citep{wang2025attn} replaces the generator ... a stack of diagonally ... capture
+ temporal and cross-feature correlations ... while the discriminator scores observed against
+ imputed elements. Introduced for IoT sensor streams, the design transfers naturally to
+ financial panels that exhibit the same contiguous-outage structure ... the generalisability
+ of the completed values ... assuming the missingness pattern in advance.
```
> 🔑 **关键修正**：原文称 ImputeGAN 使用 **Informer** 骨干，经 Crossref 核对 DOI
> `10.3390/e25010137` 的摘要，**并无 Informer** → 已删除该论断。

### `05_imputation.tex:28`（CWGAIN-GP 技术论断修正）
```diff
- CWGAIN-GP framework \citep{wang2024cwgain} employs a fortified penalty~\citep{arjovsky2017wgan,gulrajani2017wgangp}.
+ ... target case of long contiguous gaps, Wang et al.~\citep{wang2024cwgain} train ...
+ penalty~\citep{arjovsky2017wgan,gulrajani2017wgangp} explicitly for continuous
+ missing-value blocks.
```

### `06_synthesis.tex:46`（作者名纠正）
```diff
- Alex et al.~\citep{alex2024macroeconomic}
+ Rusnak and Daul~\citep{alex2024macroeconomic}
```
> 该 key 是真实文献，但原文作者名写错了 → 已改正。

### `06_synthesis.tex:48`
```diff
- (DiGA / DigMA)
+ (DiGA)
```

### `06_synthesis.tex:59/61`
```diff
- \citep{gholamrezaei2023conditioning}.
+ \citep{wang2024mtsisurvey}.
- Lozano et al.~\citep{lozano2023dual} propose advanced ... have been developed
- Tao et al.~\citep{tao2024} introduce distillation ... process,
- Oppel et al.~\citep{oppel2025} demonstrate how these advanced ... practically utilized, embedding them
+ ... have been developed ... Distillation ... process have been introduced, ... now being ... embedded
```

### `07_applications.tex`（ImputeGAN 描述修正）
```diff
- ImputeGAN~\citep{qin2023imputegan} and AttnWGAIN~\citep{wang2025attn} replace generic CNN
- backbones with Informer and diagonal-masked attention respectively, both reporting smaller
- distributional drift on the K-line dataset than linear interpolation or MICE baselines~\citep{vanbuuren1999multiple}.
+ ImputeGAN~\citep{qin2023imputegan} couples a GAN with an iterative gradient-based refinement
+ step, reporting smaller distributional drift than case deletion, statistics-based and
+ machine-learning baselines~\citep{vanbuuren1999multiple}. A complementary line of work
+ replaces the convolutional generator outright: diagonal-masked attention
```
> 同样的 Informer 误述在此处也被修正。

---

## 5. C 类：数字自洽

### `02_methodology.tex:17`
```diff
- ... extrapolation (54), imputation (29), and synthesis (47).
+ ... extrapolation (54), imputation (29), and synthesis (48).
```
✅ 校验：`54 + 29 + 48 = 131` = 语料规模。原 `47` 使总和只有 130，与"131 studies"矛盾。

### `14_tables.tex`（Table 1 与语料表）

**Table 1 · `# refs` 列**（详见 `00` §3.2，已确认 `199` 正确）
```diff
- \textbf{This survey} & ... & \textbf{131} & \textbf{2021--early 2026} \\
+ \textbf{This survey} & ... & \textbf{199} & \textbf{1950--2026} \\
```

**Table 1 caption** 追加了 window 口径说明：
```diff
- ...\textbf{Window} is the publication-year range of each survey's references.}
+ ...\textbf{Window} is the publication-year range of each survey's references;
+ for this survey it extends before the 2021--early 2026 search window because
+ foundational statistical and econometric references are retained.}
```

**语料任务分布表**（`14_tables.tex` 末尾）
```diff
- \quad Synthesis / Simulation & & 47 & \\
+ \quad Synthesis / Simulation & & 48 & \\
```

---

## 6. D 类：图注纠错（`13_figures.tex`）

### Figure 1 图注
```diff
- \caption{\textbf{Evolution of Financial Time-Series Generation techniques over the
- past decade.} Timeline of representative milestones ... from 2020 to early 2026. ...
```
修正内容：
1. **删除虚构的 2022 时间轴标记**
2. **删除 `NDDP` 这类 bib-key 孤儿名词**
3. **修正误导性标签** "Transformer-based Models"
4. **".  " 双句点** → 单句点

### Figure 2 图注
```diff
- ... 显示五个技术族 ...
+ ... 显示六个技术族 ... (与正文 "six families" 对齐)
```
修正内容：
1. **删除 8 个幽灵模型名**（图中并不存在的模型）
2. **技术族数量从 5 改为 6** —— 与正文 `six families` / `controllable multimodal` 一致
3. 与 `05_imputation.tex` 的第六族定义对齐

### `01_introduction.tex:16`
```diff
- ... and foundation-model ...
+ ... foundation-model, and controllable multimodal ...
```
> 图 2 加了第六族，正文的 `how` 括号列表同步补上第 6 项。

---

## 7. E 类：语法与文风

| 文件:行 | before | after |
|---|---|---|
| `04_extrapolation.tex:75` | `Addressing the limitations of ... in capturing` | `Because ... struggle to capture` |
| `04_extrapolation.tex:120` | `face with persistent` | `face persistent` |
| `06_synthesis.tex:50` | `The survey's` / `claim` | `Our` / `statement` |
| `07_applications.tex:125` | `perhaps` | `Perhaps` |
| `07_applications.tex:131` | `DOW-30,` | `Dow-30,` |
| `02_methodology.tex` | — | （数字见 C 类） |

> `04_extrapolation.tex:75` 的改写同时解决了一个 **14.25pt overfull box**
> （改写后最大 overfull 降到 4.82pt）。

---

## 8. `11_conclusion.tex` 的处理（待决事项 D）

| 位置 | 是否存在 |
|---|---|
| 根目录 `sections/11_conclusion.tex` | ✅ 存在（623 B） |
| `main_clean/sections/11_conclusion.tex` | ❌ 不存在 |
| WS `sections/11_conclusion.tex` | ❌ 不存在 |

**是否被引用**：
```bash
$ grep -rn '11_conclusion' *.tex
is26_cas_backup.tex:124:\input{sections/11_conclusion.tex}
```
→ **只有废弃的 `is26_cas_backup.tex` 引用它**；`is26.tex`、`is26_marked.tex` 都不引用。

**内容**（`\section{Conclusion}`，2 段）：

> This survey presents a systematic review of the financial time-series generation (FTSG)
> landscape. Our main contribution is a two-level taxonomy that unifies the field across
> both task-oriented and technique-driven dimensions, providing a structured framework for
> future studies.
>
> Furthermore, we characterize the data resources by categorizing them into raw sources,
> curated databases, and carefully designed benchmarks. By outlining promising directions
> for addressing the unique challenges in this domain, we hope this work offers critical
> insights and advances the research community.

**建议**：**保留不动**（不删除、不复制到 WS）。理由：
- 它未被任何在用 master 引用，**对编译无影响**
- 删除它会增加 diff 噪音，且它已存在于 HEAD，属于历史遗留
- WS 空缺该文件是**正确的**，因为 WS 的 `is26.tex` 不引用它

> ⚠️ 若合并脚本按"整体同步 `sections/`"执行，**务必确认不把根目录的
> `11_conclusion.tex` 删掉**。`04_合并操作命令.md` 的脚本已做保护：只覆盖同名文件，
> 不做删除。

---

## 9. 复现本清单

```bash
cd "/Volumes/FunkDisk/Study/PHD/论文发表/论文一：金融数据合成综述"
diff -r -u SurveyOfFTSG/SurveyOfFTSG-main_clean/sections \
          SurveyOfFTSG-main0913/sections | less
```
