\documentclass[11pt,a4paper]{article}
%DIF LATEXDIFF DIFFERENCE FILE
%DIF DEL /tmp/is26_old_with_current_tables.tex   Fri Sep 11 14:24:35 2026
%DIF ADD /tmp/is26_current_flat_clean.tex        Fri Sep 11 14:24:35 2026

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\setstretch{1.15}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs,multirow}
\usepackage{xcolor,colortbl}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,arrows.meta}
\usepackage{caption}
\usepackage[edges]{forest}
\usepackage{float}
\usepackage{microtype}

\usepackage[numbers,sort&compress]{natbib}
% Nature format: superscript citations + numbered reference list without brackets
\setcitestyle{numbers,sort&compress,super}
\renewcommand{\bibnumfmt}[1]{#1.}
\usepackage[colorlinks=true,linkcolor=blue!70!black,citecolor=blue!70!black,urlcolor=blue!70!black]{hyperref}

% npj Artificial Intelligence requirement: unnumbered section headings
\setcounter{secnumdepth}{0}

% Clean version: revision markers render as plain text (marked version redefines them in red)
\providecommand{\revadd}[1]{#1}
\providecommand{\revdel}[1]{}

% Title, Authors, and Affiliations
\title{\Large\textbf{Financial Time-Series Generation: A Systematic Survey of Taxonomy, Evaluation, Datasets, and Open Challenges}}

\author{
  Yingxiao Zhang$^{1,2}$, 
  Jiaxin Duan$^{3}$, 
  Yue Zou$^{1,2}$, 
  Ke Feng$^{1,*}$, 
  Jing Xiao$^{4,*}$
}

\date{}
%DIF PREAMBLE EXTENSION ADDED BY LATEXDIFF
%DIF UNDERLINE PREAMBLE %DIF PREAMBLE
\RequirePackage[normalem]{ulem} %DIF PREAMBLE
\RequirePackage{color}\definecolor{RED}{rgb}{1,0,0}\definecolor{BLUE}{rgb}{0,0,1} %DIF PREAMBLE
\providecommand{\DIFaddtex}[1]{{\protect\color{blue}\uwave{#1}}} %DIF PREAMBLE
\providecommand{\DIFdeltex}[1]{{\protect\color{red}\sout{#1}}} %DIF PREAMBLE
%DIF SAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddbegin}{} %DIF PREAMBLE
\providecommand{\DIFaddend}{} %DIF PREAMBLE
\providecommand{\DIFdelbegin}{} %DIF PREAMBLE
\providecommand{\DIFdelend}{} %DIF PREAMBLE
\providecommand{\DIFmodbegin}{} %DIF PREAMBLE
\providecommand{\DIFmodend}{} %DIF PREAMBLE
%DIF FLOATSAFE PREAMBLE %DIF PREAMBLE
\providecommand{\DIFaddFL}[1]{\DIFadd{#1}} %DIF PREAMBLE
\providecommand{\DIFdelFL}[1]{\DIFdel{#1}} %DIF PREAMBLE
\providecommand{\DIFaddbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFaddendFL}{} %DIF PREAMBLE
\providecommand{\DIFdelbeginFL}{} %DIF PREAMBLE
\providecommand{\DIFdelendFL}{} %DIF PREAMBLE
%DIF HYPERREF PREAMBLE %DIF PREAMBLE
\providecommand{\DIFadd}[1]{\texorpdfstring{\DIFaddtex{#1}}{#1}} %DIF PREAMBLE
\providecommand{\DIFdel}[1]{\texorpdfstring{\DIFdeltex{#1}}{}} %DIF PREAMBLE
\providecommand{\DIFscaledelfig}{0.5}
%DIF HIGHLIGHTGRAPHICS PREAMBLE %DIF PREAMBLE
\RequirePackage{settobox} %DIF PREAMBLE
\RequirePackage{letltxmacro} %DIF PREAMBLE
\newsavebox{\DIFdelgraphicsbox} %DIF PREAMBLE
\newlength{\DIFdelgraphicswidth} %DIF PREAMBLE
\newlength{\DIFdelgraphicsheight} %DIF PREAMBLE
% store original definition of \includegraphics %DIF PREAMBLE
\LetLtxMacro{\DIFOincludegraphics}{\includegraphics} %DIF PREAMBLE
\providecommand{\DIFaddincludegraphics}[2][]{{\color{blue}\fbox{\DIFOincludegraphics[#1]{#2}}}} %DIF PREAMBLE
\providecommand{\DIFdelincludegraphics}[2][]{% %DIF PREAMBLE
\sbox{\DIFdelgraphicsbox}{\DIFOincludegraphics[#1]{#2}}% %DIF PREAMBLE
\settoboxwidth{\DIFdelgraphicswidth}{\DIFdelgraphicsbox} %DIF PREAMBLE
\settoboxtotalheight{\DIFdelgraphicsheight}{\DIFdelgraphicsbox} %DIF PREAMBLE
\scalebox{\DIFscaledelfig}{% %DIF PREAMBLE
\parbox[b]{\DIFdelgraphicswidth}{\usebox{\DIFdelgraphicsbox}\\[-\baselineskip] \rule{\DIFdelgraphicswidth}{0em}}\llap{\resizebox{\DIFdelgraphicswidth}{\DIFdelgraphicsheight}{% %DIF PREAMBLE
\setlength{\unitlength}{\DIFdelgraphicswidth}% %DIF PREAMBLE
\begin{picture}(1,1)% %DIF PREAMBLE
\thicklines\linethickness{2pt} %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\framebox(1,1){}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,0){\line( 1,1){1}}}% %DIF PREAMBLE
{\color[rgb]{1,0,0}\put(0,1){\line(1,-1){1}}}% %DIF PREAMBLE
\end{picture}% %DIF PREAMBLE
}\hspace*{3pt}}} %DIF PREAMBLE
} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbegin}{\DIFaddbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddend}{\DIFaddend} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbegin}{\DIFdelbegin} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelend}{\DIFdelend} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbegin}{\DIFOaddbegin \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbegin}{\DIFOdelbegin \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelend}{\DIFOaddend \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddbeginFL}{\DIFaddbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOaddendFL}{\DIFaddendFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelbeginFL}{\DIFdelbeginFL} %DIF PREAMBLE
\LetLtxMacro{\DIFOdelendFL}{\DIFdelendFL} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddbeginFL}{\DIFOaddbeginFL \let\includegraphics\DIFaddincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFaddendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelbeginFL}{\DIFOdelbeginFL \let\includegraphics\DIFdelincludegraphics} %DIF PREAMBLE
\DeclareRobustCommand{\DIFdelendFL}{\DIFOaddendFL \let\includegraphics\DIFOincludegraphics} %DIF PREAMBLE
%DIF AMSMATHULEM PREAMBLE %DIF PREAMBLE
\makeatletter %DIF PREAMBLE
\let\sout@orig\sout %DIF PREAMBLE
\renewcommand{\sout}[1]{\ifmmode\text{\sout@orig{\ensuremath{#1}}}\else\sout@orig{#1}\fi} %DIF PREAMBLE
\makeatother %DIF PREAMBLE
%DIF COLORLISTINGS PREAMBLE %DIF PREAMBLE
\RequirePackage{listings} %DIF PREAMBLE
\RequirePackage{color} %DIF PREAMBLE
\lstdefinelanguage{DIFcode}{ %DIF PREAMBLE
%DIF DIFCODE_UNDERLINE %DIF PREAMBLE
  moredelim=[il][\color{red}\sout]{\%DIF\ <\ }, %DIF PREAMBLE
  moredelim=[il][\color{blue}\uwave]{\%DIF\ >\ } %DIF PREAMBLE
} %DIF PREAMBLE
\lstdefinestyle{DIFverbatimstyle}{ %DIF PREAMBLE
	language=DIFcode, %DIF PREAMBLE
	basicstyle=\ttfamily, %DIF PREAMBLE
	columns=fullflexible, %DIF PREAMBLE
	keepspaces=true %DIF PREAMBLE
} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim}[1][]{\lstset{style=DIFverbatimstyle}}{} %DIF PREAMBLE
\lstnewenvironment{DIFverbatim*}[1][]{\lstset{style=DIFverbatimstyle,showspaces=true}}{} %DIF PREAMBLE
\lstset{extendedchars=\true,inputencoding=utf8}

%DIF END PREAMBLE EXTENSION ADDED BY LATEXDIFF

\begin{document}

\maketitle

\vspace{-2em}
\begin{center}
\footnotesize
$^{1}$School of Software and Microelectronics, Peking University, Beijing, China\\[0.3em]
$^{2}$Pingan Bank Co., Ltd., Shenzhen, China\\[0.3em]
$^{3}$China Electronics Cloud Technology Co., Ltd., Beijing, China\\[0.3em]
$^{4}$Pingan Group Co., Ltd., Shenzhen, China\\[0.5em]
$^{*}$Corresponding authors. E-mail: \texttt{fengke@pku.edu.cn} (K. Feng); \texttt{xiaojing661@pingan.com.cn} (J. Xiao)
\end{center}
\vspace{1.5em}

% Abstract (single paragraph, 150-200 words, no subheadings)
\begin{abstract}
The rapid advancement of generative artificial intelligence has catalyzed a paradigm shift in quantitative finance, transitioning from traditional point forecasting toward comprehensive financial time-series generation (FTSG) across extrapolation, imputation, and full-scale data synthesis. This transition is motivated by the critical need to simulate rare market regimes, reconstruct incomplete historical records, and produce privacy-preserving synthetic environments for stress testing and trading execution. However, empirical financial properties---including heavy tails, volatility clustering, non-stationarity, and regime shifts---pose severe hurdles for generic generative frameworks. In this systematic survey, we review 131 studies from 2021 to early 2026 to bridge the gap between machine learning capabilities and econometric validity. We introduce a two-level taxonomy structuring the domain across task formulations and algorithmic families, and establish a multi-dimensional evaluation protocol encompassing distributional fidelity, stylized facts preservation, and downstream utility. Furthermore, we catalog curated financial databases and benchmark suites while addressing empirical dataset construction pitfalls. Finally, we identify pivotal open challenges in causal representation learning, econometrically bounded modeling, and online adaptation under distribution shifts. This review provides a standardized roadmap for developing robust, domain-aligned financial generative models.
\end{abstract} 
% Introduction (no subheadings)
\section{Introduction}
\label{sec:1}

Time-series analysis (TSA)~\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{zhao2025timeseriesscientistgeneralpurposeaiagent,jiang2025multimodaltimeseriesanalysis} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{\mbox{%DIFAUXCMD
\citep{hamilton1994time,box2015time} }\hskip0pt%DIFAUXCMD
}\DIFaddend has long been a central pillar of quantitative finance\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{campbell1997econometrics,tsay2010analysis}}\hskip0pt%DIFAUXCMD
}\DIFaddend , providing the mathematical foundation for modeling market dynamics, assessing risk, and pricing \DIFdelbegin \DIFdel{complex assets~\mbox{%DIFAUXCMD
\citep{box2015time}}\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{assets}\DIFaddend . Traditionally, TSA in finance was \DIFdelbegin \DIFdel{synonymous with forecasting}\DIFdelend \DIFaddbegin \DIFadd{closely associated with forecasting~\mbox{%DIFAUXCMD
\citep{hyndman2021forecasting}}\hskip0pt%DIFAUXCMD
}\DIFaddend : predicting the next price movement, the future volatility level, or the impending macroeconomic shift. \DIFdelbegin \DIFdel{However, as }\DIFdelend \DIFaddbegin \DIFadd{Modern probabilistic forecasting goes beyond point estimates by quantifying predictive distributions and forecast uncertainty~\mbox{%DIFAUXCMD
\citep{gneiting2014probabilistic}}\hskip0pt%DIFAUXCMD
. As }\DIFaddend the financial industry becomes increasingly data-driven and algorithmic, the demand has evolved from \DIFdelbegin \DIFdel{mere }\DIFdelend point estimation to the comprehensive generation of plausible market scenarios. This paradigm shift has given rise to Financial Time-Series Generation (FTSG), \DIFdelbegin \DIFdel{a domain that extends beyond }\DIFdelend \DIFaddbegin \DIFadd{which extends conventional }\DIFaddend forecasting to include \DIFdelbegin \DIFdel{the synthesis of entirely new data, the imputationof incomplete price records, and the extrapolation of diverse financial products}\DIFdelend \DIFaddbegin \DIFadd{fully synthetic financial sequences~\mbox{%DIFAUXCMD
\citep{Time-GAN,wiese2020,naritomi2020data}}\hskip0pt%DIFAUXCMD
, missing-data imputation~\mbox{%DIFAUXCMD
\citep{yoon2018gain,tashiro2021csdi,wang2024cwgain}}\hskip0pt%DIFAUXCMD
, and probabilistic extrapolation of future financial trajectories~\mbox{%DIFAUXCMD
\citep{rasul2021timegrad}}\hskip0pt%DIFAUXCMD
}\DIFaddend .

The motivation for FTSG is deeply rooted in the structural limitations of real-world financial data. Unlike text or images, financial markets are characterized by a low signal-to-noise ratio, extreme non-stationarity, and abrupt regime shifts triggered by geopolitical events or macroeconomic policy changes. Furthermore, empirical financial distributions frequently exhibit "stylized facts" such as heavy tails, volatility clustering, and leverage effects\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{style-facts}}\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{cont2001empirical}}\hskip0pt%DIFAUXCMD
}\DIFaddend . While historical data contains instances of these phenomena, critical events like market crashes or sudden liquidity droughts are inherently rare. Consequently, discriminative models trained solely on historical records often suffer from overfitting and fail to generalize during crises. FTSG \DIFdelbegin \DIFdel{addresses }\DIFdelend \DIFaddbegin \DIFadd{can address }\DIFaddend this by creating high-fidelity\DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{—and, when trained with formal privacy controls, potentially }\DIFaddend privacy-preserving\DIFaddbegin \DIFadd{—}\DIFaddend synthetic data that can augment training sets, simulate counterfactual stress-testing scenarios, and train robust reinforcement learning agents in simulated environments.

Across the task taxonomy of this survey, we use \DIFdelbegin \textbf{\DIFdel{FTSG}} %DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{FTSG }\DIFaddend to denote the umbrella research domain (Financial Time-Series Generation), while \DIFdelbegin \textbf{\DIFdel{FTSE}}%DIFAUXCMD
\DIFdel{, }\textbf{\DIFdel{FTSI}}%DIFAUXCMD
\DIFdel{, and }\textbf{\DIFdel{FTSS}} %DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{FTSE, FTSI, and FTSS }\DIFaddend denote the three task-level formulations for extrapolation, imputation, and synthesis, respectively. This terminology is used consistently throughout the paper and is summarized again in the Problem Formulation and Taxonomy and Applications sections.

\DIFdelbegin \DIFdel{The rapid integration of Generative AI (GAI), as demonstrated }\DIFdelend \DIFaddbegin \DIFadd{Generative AI has rapidly entered financial modeling, as illustrated }\DIFaddend in Figure~\ref{fig:timeline}\DIFdelbegin \DIFdel{, including Generative Adversarial Networks }\DIFdelend \DIFaddbegin \DIFadd{. The main families include generative adversarial networks }\DIFaddend (GANs)\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{Time-GAN,wiese2020,naritomi2020data}}\hskip0pt%DIFAUXCMD
, Denoising Diffusion Probabilistic Models }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan,Time-GAN,wiese2020,naritomi2020data}}\hskip0pt%DIFAUXCMD
, denoising diffusion probabilistic models }\DIFaddend (DDPMs)\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{ho2020ddpm,rasul2021timegrad}}\hskip0pt%DIFAUXCMD
, and Large Language Models }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{ho2020ddpm,rasul2021timegrad}}\hskip0pt%DIFAUXCMD
, Transformers~\mbox{%DIFAUXCMD
\citep{vaswani2017attention} }\hskip0pt%DIFAUXCMD
and large language models }\DIFaddend (LLMs)\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{ansari2024chronos,Time-LLM}}\hskip0pt%DIFAUXCMD
---into financial modeling has }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{brown2020language,jin2024timellm,ansari2024chronos}}\hskip0pt%DIFAUXCMD
. These developments, together with their financial adaptations, have }\DIFaddend produced a fragmented and rapidly expanding body of literature. \DIFdelbegin \DIFdel{While several surveys have attempted to capture these advancements \mbox{%DIFAUXCMD
\citep{Survey1,Survey2,Survey3,Survey4,FinSurvey1,FinSurvey3}}\hskip0pt%DIFAUXCMD
, they suffer from notable limitations in technical and methodological aspects}\DIFdelend \DIFaddbegin \DIFadd{Zhang et al.~\mbox{%DIFAUXCMD
\citep{Survey1} }\hskip0pt%DIFAUXCMD
survey LLMs for time series, while Liang et al.~\mbox{%DIFAUXCMD
\citep{Survey2} }\hskip0pt%DIFAUXCMD
review time-series foundation models. Kim et al.~\mbox{%DIFAUXCMD
\citep{Survey3} }\hskip0pt%DIFAUXCMD
and Kong et al.~\mbox{%DIFAUXCMD
\citep{Survey4} }\hskip0pt%DIFAUXCMD
review deep time-series forecasting architectures, and Nie et al.~\mbox{%DIFAUXCMD
\citep{FinSurvey1} }\hskip0pt%DIFAUXCMD
and Arsenault et al.~\mbox{%DIFAUXCMD
\citep{FinSurvey3} }\hskip0pt%DIFAUXCMD
review financial-AI applications. These surveys help map parts of the literature, but they leave several task- and evaluation-oriented gaps}\DIFaddend .

First, the majority of existing surveys remain heavily skewed toward forecasting (extrapolation) and classification, often treating generation merely as an auxiliary data augmentation step rather than a standalone class of problems encompassing synthesis and imputation. Second, \DIFdelbegin \DIFdel{most reviews are organized strictly by model architecture (e.g., "A Survey of LLMs in Finance" or "Diffusion Models for Time Series"), which obscures the fundamental }\DIFdelend \DIFaddbegin \DIFadd{the reviews by Zhang et al.~\mbox{%DIFAUXCMD
\citep{Survey1} }\hskip0pt%DIFAUXCMD
and Liang et al.~\mbox{%DIFAUXCMD
\citep{Survey2} }\hskip0pt%DIFAUXCMD
are organized primarily around model families and representation-learning components, whereas the reviews by Kim et al.~\mbox{%DIFAUXCMD
\citep{Survey3}}\hskip0pt%DIFAUXCMD
, Kong et al.~\mbox{%DIFAUXCMD
\citep{Survey4}}\hskip0pt%DIFAUXCMD
, Nie et al.~\mbox{%DIFAUXCMD
\citep{FinSurvey1}}\hskip0pt%DIFAUXCMD
, and Arsenault et al.~\mbox{%DIFAUXCMD
\citep{FinSurvey3} }\hskip0pt%DIFAUXCMD
emphasize architectures or application domains; this organization obscures }\DIFaddend differences in task formulation, supervision signals, and conditioning contexts\DIFdelbegin \DIFdel{inherent in financial applications}\DIFdelend . Third, and most critically, existing literature lacks a systematic discussion on evaluation protocols. Assessing a synthetic financial sequence requires moving beyond generic point-wise error metrics (like MSE or MAE) to evaluate cross-sectional dependence, tail-risk preservation, and downstream utility---areas where current surveys offer little standardization or critical synthesis. Finally, there is a scarcity of resource-oriented reviews that comprehensively catalog financial benchmarks, datasets, and the practical biases (e.g., survivorship bias) that plague empirical financial research.

To substantiate these gaps concretely, Table~\ref{tab:related_surveys} positions our survey against the six prior surveys cited above along five explicit dimensions: task coverage (whether each of the three FTSG sub-tasks---extrapolation (FTSE), imputation (FTSI), and synthesis (FTSS)---is treated as a first-class topic), evaluation treatment (whether a multi-dimensional protocol is provided, beyond per-paper metric tables), dataset cataloging (whether FTSG-relevant financial benchmarks are systematically inventoried), number of references in the bibliography \DIFaddbegin \DIFadd{(coded approximately from the available versions)}\DIFaddend , and time window of coverage. \DIFdelbegin \DIFdel{The table confirms }\DIFdelend \DIFaddbegin \DIFadd{Our coding suggests }\DIFaddend that no prior survey jointly covers all three FTSG sub-tasks with a dedicated evaluation protocol and a curated financial dataset catalog; each gap maps to one of the four motivating critiques above and motivates a corresponding contribution of our survey discussed below.

To address these critical gaps, this paper provides a systematic, evaluation-oriented survey of FTSG, focusing on the intersection of generative AI and financial econometrics over the past five years (2021--early 2026). We elevate the discourse from a mere enumeration of literature to a structured problem formulation and benchmarking perspective. Our \DIFdelbegin \DIFdel{main }\DIFdelend contributions are fourfold\DIFdelbegin \DIFdel{:
}%DIFDELCMD < 

%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{A Task-Technique Two-Level Taxonomy}}%DIFAUXCMD
\DIFdel{: }\DIFdelend \DIFaddbegin \DIFadd{. First, we propose a task-technique two-level taxonomy. }\DIFaddend By comprehensively reviewing 131 studies published in 2021--early 2026, we \DIFdelbegin \DIFdel{propose a novel }\DIFdelend \DIFaddbegin \DIFadd{develop a }\DIFaddend hierarchical classification system that decouples the \textit{what} (extrapolation, imputation, \DIFaddbegin \DIFadd{and }\DIFaddend synthesis) from the \textit{how} (statistical, deep discriminative, GAN\DIFdelbegin \DIFdel{s}\DIFdelend  , diffusion, \DIFdelbegin \DIFdel{foundation models}\DIFdelend \DIFaddbegin \DIFadd{and foundation-model approaches}\DIFaddend ), enabling a clearer mapping of \DIFdelbegin \DIFdel{specific }\DIFdelend generative techniques to \DIFdelbegin \DIFdel{their optimal }\DIFdelend financial use cases. \DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{A Systematic Evaluation Framework}}%DIFAUXCMD
\DIFdel{: We introduce a comprehensive, multi-dimensional evaluation protocol for FTSG . Moving }\DIFdelend \DIFaddbegin \DIFadd{Second, we establish a systematic evaluation framework for FTSG that moves }\DIFaddend beyond generic reconstruction metrics \DIFdelbegin \DIFdel{, we establish a unified checklist encompassing }\DIFdelend \DIFaddbegin \DIFadd{to encompass }\DIFaddend distributional fidelity, \DIFdelbegin \DIFdel{stylized facts }\DIFdelend \DIFaddbegin \DIFadd{stylized-facts }\DIFaddend preservation, downstream utility, and robustness \DIFdelbegin \DIFdel{against }\DIFdelend \DIFaddbegin \DIFadd{to }\DIFaddend regime shifts. \DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{A Comprehensive Resource Synthesis}}%DIFAUXCMD
\DIFdel{: We }\DIFdelend \DIFaddbegin \DIFadd{Third, we }\DIFaddend compile and categorize \DIFdelbegin \DIFdel{a wide array of }\DIFdelend public data resources, APIs, and emerging benchmark suites\DIFdelbegin \DIFdel{(e.g., FinTSB \mbox{%DIFAUXCMD
\citep{hu2025fintsb}}\hskip0pt%DIFAUXCMD
, TFB \mbox{%DIFAUXCMD
\citep{TFB}}\hskip0pt%DIFAUXCMD
) }\DIFdelend \DIFaddbegin \DIFadd{, including FinTSB \mbox{%DIFAUXCMD
\citep{hu2025fintsb} }\hskip0pt%DIFAUXCMD
and TFB \mbox{%DIFAUXCMD
\citep{TFB}}\hskip0pt%DIFAUXCMD
, }\DIFaddend specifically relevant to financial generation, \DIFdelbegin \DIFdel{alongside a critical discussion of practical dataset construction issues. }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Identification of Open Challenges}}%DIFAUXCMD
\DIFdel{: We synthesize the literature to identify critical bottlenecks }\DIFdelend \DIFaddbegin \DIFadd{and discuss practical issues in dataset construction. Fourth, we synthesize open challenges }\DIFaddend in financial validity \DIFdelbegin \DIFdel{, proposing }\DIFdelend \DIFaddbegin \DIFadd{and identify }\DIFaddend actionable future directions\DIFdelbegin \DIFdel{such as }\DIFdelend \DIFaddbegin \DIFadd{, including }\DIFaddend financial-theory-informed generative modeling and causal representation learning.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

% 【文生图】 [A flow diagram illustrating the mapping from financial generation tasks (extrapolation, imputation, synthesis) to practical downstream applications (risk warning, data repair, stress testing, simulation). Style: Professional academic flowchart, Information Sciences journal aesthetic, clear directional arrows.]




The remainder of this paper is organized according to the IRDM structure. The Results section presents our core survey findings across six thematic areas: problem formulation and two-level taxonomy, task-specific analyses of extrapolation, imputation, and synthesis, downstream quantitative applications, evaluation protocols for financial realism, and data resources with benchmarks. The Discussion section critically examines pressing open challenges and future research directions, synthesizing the implications for financial generative modeling. Finally, the Methods section details the systematic literature retrieval strategy \DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{and }\DIFaddend PRISMA-compliant screening protocol\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{page2021prisma}}\hskip0pt%DIFAUXCMD
}\DIFaddend , inclusion criteria, and coding procedures governing this survey. 
% Results (compulsory subheadings)
\section{Results}
\subsection{Problem Formulation and \DIFdelbegin \DIFdel{A Two-Level Taxonomy}\DIFdelend \DIFaddbegin \DIFadd{Setup}\DIFaddend }
\label{sec:taxonomy}

% \definecolor{ColorExtrapolation}{RGB}{240, 245, 255}
% \definecolor{ColorImputation}{RGB}{240, 255, 240}
% \definecolor{ColorAugmentation}{RGB}{255, 245, 235}
% \definecolor{BorderGray}{RGB}{180, 180, 180}

\definecolor{ColorExtrapolation}{RGB}{240, 245, 255}
\definecolor{ColorImputation}{RGB}{240, 255, 240}
\definecolor{ColorAugmentation}{RGB}{255, 245, 235}
\definecolor{BorderGray}{RGB}{180, 180, 180}

\DIFdelbegin \subsection{\DIFdel{Problem Setup}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend To establish a rigorous conceptual foundation, Figure~\ref{fig:taxonomy} presents a comprehensive two-level hierarchical taxonomy that structures the entire financial time-series generation landscape across task formulations and algorithmic families.
Let $\mathbf{X} = \{x_1, x_2, \dots, x_T\}$ denote a multivariate financial time series, where each $x_t \in \mathbb{R}^d$ represents a vector of financial variables (e.g., asset prices, returns, trading volumes, or limit order book states) at time step $t$. Financial time series are inherently stochastic, heavily influenced by non-stationary market regimes, and exhibit complex inter-variable dependencies.
The core objective of Financial Time-Series Generation (FTSG) is to learn a probabilistic generative model $P_\theta$ capable of producing synthetic sequences $\hat{\mathbf{X}}$ that capture both the marginal distributions and the temporal dynamics of the true data-generating process $P_{data}$.

\subsection{Task-Level Taxonomy}
As illustrated in Figure~\ref{fig:tasks}, \DIFdelbegin \DIFdel{we organize FTSG into }\DIFdelend \DIFaddbegin \DIFadd{FTSG comprises }\DIFaddend three distinct problem formulations based on \DIFdelbegin \DIFdel{the generation context:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Financial Time-Series Extrapolation:}} %DIFAUXCMD
\DIFdel{A }\DIFdelend \DIFaddbegin \DIFadd{generation context.
}

\DIFadd{Financial time-series extrapolation is a }\DIFaddend conditional, forward-looking generation task. Given a historical prefix $\mathbf{x}_{1:t}$, the goal is to generate future trajectories $\mathbf{x}_{t+1:T}$. The predictive distribution is modeled as $P(\mathbf{x}_{t+1:T} \mid \mathbf{x}_{1:t}; \theta) = \prod_{i=t+1}^{T} P(x_i \mid \mathbf{x}_{1:i-1}; \theta)$. This encompasses probabilistic forecasting, where the output is a set of plausible future paths rather than a single point estimate.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Financial Time-Series Imputation:}} %DIFAUXCMD
\DIFdel{A }\DIFdelend \DIFaddbegin 

\DIFadd{Financial time-series imputation is a }\DIFaddend bidirectional, closed-format generation task. Let $\mathbf{M} \in \{0, 1\}^T$ be a binary mask indicating missing values. Given an incomplete sequence with missing elements $\mathbf{x}_{miss} = \mathbf{x} \odot \mathbf{M}$ and observed elements $\mathbf{x}_{obs} = \mathbf{x} \odot (1-\mathbf{M})$, the objective is to reconstruct the missing data while preserving local and global statistical coherence: $P(\mathbf{x}_{miss} \mid \mathbf{x}_{obs}; \theta)$.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Synthetic Sequence Generation / Data Synthesis:}} %DIFAUXCMD
\DIFdel{An unconditional or conditionally-guided }\DIFdelend \DIFaddbegin 

\DIFadd{Synthetic sequence generation, or data synthesis, is an unconditional or conditionally guided }\DIFaddend generation task aimed at creating entirely new data manifolds. The model learns to map a latent noise variable $\mathbf{z} \sim \mathcal{N}(0, \mathbf{I})$ (and optionally a condition $c$ such as market regime or volatility target) to a realistic sequence: $\hat{\mathbf{X}} = G_{\theta}(\mathbf{z}, c)$, aiming to minimize the divergence between the generated distribution $P_g$ and the real distribution $P_{data}$.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{Technique-Level Taxonomy}
To map the rapid evolution of algorithmic approaches, \DIFdelbegin \DIFdel{we classify }\DIFdelend the underlying generative techniques \DIFdelbegin \DIFdel{into the following families:
}%DIFDELCMD < \begin{enumerate}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Statistical and Probabilistic Methods:}} %DIFAUXCMD
\DIFdel{Classical }\DIFdelend \DIFaddbegin \DIFadd{can be grouped into six families.
}

\DIFadd{Statistical and probabilistic methods include classical }\DIFaddend approaches such as ARIMA\DIFdelbegin \DIFdel{and GARCH}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{box1970} }\hskip0pt%DIFAUXCMD
and GARCH~\mbox{%DIFAUXCMD
\citep{bollerslev1986garch}}\hskip0pt%DIFAUXCMD
}\DIFaddend , which model conditional mean and variance based on rigid mathematical assumptions.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Discriminative Deep Sequence Models:}} %DIFAUXCMD
\DIFdel{Recurrent Neural Networks }\DIFdelend \DIFaddbegin 

\DIFadd{Discriminative deep sequence models include recurrent neural networks }\DIFaddend (RNNs)\DIFdelbegin \DIFdel{and hybrid LSTMs }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{elman1990finding} }\hskip0pt%DIFAUXCMD
and long short-term memory (LSTM) networks~\mbox{%DIFAUXCMD
\citep{hochreiter1997lstm} }\hskip0pt%DIFAUXCMD
}\DIFaddend optimized via pointwise loss functions.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Adversarial Generative Models (GANs):}} %DIFAUXCMD
\DIFdel{Frameworks utilizing }\DIFdelend \DIFaddbegin 

\DIFadd{Adversarial generative models (GANs)~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan} }\hskip0pt%DIFAUXCMD
use }\DIFaddend generator-discriminator dynamics\DIFdelbegin \DIFdel{(e.g., }\DIFdelend \DIFaddbegin \DIFadd{, including the Wasserstein GAN with gradient penalty (}\DIFaddend WGAN-GP)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{arjovsky2017wgan,gulrajani2017wgangp}}\hskip0pt%DIFAUXCMD
, }\DIFaddend to implicitly learn the data distribution.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Diffusion / Score-Based Models:}} %DIFAUXCMD
\DIFdel{Models that }\DIFdelend \DIFaddbegin 

\DIFadd{Diffusion and score-based models~\mbox{%DIFAUXCMD
\citep{ho2020ddpm,song2019generative,song2021score} }\hskip0pt%DIFAUXCMD
}\DIFaddend learn to reverse a gradual noising process, offering stable training and high-fidelity distribution matching.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Foundation Models / LLM-Based Models:}} %DIFAUXCMD
\DIFdel{Recent }\DIFdelend \DIFaddbegin 

\DIFadd{Foundation models and LLM-based models are recent }\DIFaddend adaptations of large pre-trained \DIFdelbegin \DIFdel{transformers }\DIFdelend \DIFaddbegin \DIFadd{Transformers~\mbox{%DIFAUXCMD
\citep{vaswani2017attention,brown2020language} }\hskip0pt%DIFAUXCMD
}\DIFaddend to the time-series domain, \DIFdelbegin \DIFdel{enabling zero-shot generation and cross-modal reasoning.
}%DIFDELCMD < \item %%%
\textbf{\DIFdel{Controllable and Multimodal Generative Models:}} %DIFAUXCMD
\DIFdel{Advanced architectures }\DIFdelend \DIFaddbegin \DIFadd{represented by models such as Chronos~\mbox{%DIFAUXCMD
\citep{ansari2024chronos}}\hskip0pt%DIFAUXCMD
, OneFitsAll~\mbox{%DIFAUXCMD
\citep{zhou2023onefitsall}}\hskip0pt%DIFAUXCMD
, and Time-LLM~\mbox{%DIFAUXCMD
\citep{jin2024timellm}}\hskip0pt%DIFAUXCMD
.
}

\DIFadd{Controllable and multimodal generative models, including TimeWeaver~\mbox{%DIFAUXCMD
\citep{narasimhan2024timeweaver}}\hskip0pt%DIFAUXCMD
, T2S~\mbox{%DIFAUXCMD
\citep{T2S}}\hskip0pt%DIFAUXCMD
, and dual-sourced text--time diffusion models~\mbox{%DIFAUXCMD
\citep{lozano2023dual}}\hskip0pt%DIFAUXCMD
, are }\DIFaddend designed to generate sequences conditioned on textual instructions, macroeconomic indicators, or specific volatility regimes.
\DIFdelbegin %DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdelend 

The detailed classification of FTSG methods, organized by task and technique, is summarized in Figure ~\ref{fig:taxonomy} for a clear overview.

\subsection{Mapping Tasks to Techniques}
Table \ref{tab:mapping} provides a unified mapping of these tasks to the corresponding techniques, highlighting the evolution from deterministic prediction to flexible, scenario-based generative paradigms. Specifically, for the extrapolation task, the landscape spans from deep sequence architectures \DIFdelbegin \DIFdel{(e.g., }\DIFdelend \DIFaddbegin \DIFadd{such as }\DIFaddend SGP-LSTM\DIFdelbegin \DIFdel{) }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{SGP-LSTM1} }\hskip0pt%DIFAUXCMD
}\DIFaddend that forecast prices and returns using deterministic metrics like MSE, to advanced diffusion and foundation models \DIFdelbegin \DIFdel{(e.g., TimeGrad}\DIFdelend \DIFaddbegin \DIFadd{such as TimeGrad~\mbox{%DIFAUXCMD
\citep{rasul2021timegrad}}\hskip0pt%DIFAUXCMD
, Chronos~\mbox{%DIFAUXCMD
\citep{ansari2024chronos}}\hskip0pt%DIFAUXCMD
}\DIFaddend , \DIFdelbegin \DIFdel{Chronos, }\DIFdelend and FinCast\DIFdelbegin \DIFdel{)}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{zhu2025fincast}}\hskip0pt%DIFAUXCMD
}\DIFaddend . These newer paradigms handle multivariate and multi-asset series, shifting the evaluation focus toward \DIFdelbegin \DIFdel{probabilistic measures }\DIFdelend \DIFaddbegin \DIFadd{proper probabilistic scoring rules such as the continuous ranked probability score }\DIFaddend (CRPS)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{gneiting2007strictly} }\hskip0pt%DIFAUXCMD
}\DIFaddend and zero-shot capabilities. In the realm of imputation, missing fundamentals and prices are primarily addressed using tensor methods \DIFdelbegin \DIFdel{(RegTensor) }\DIFdelend \DIFaddbegin \DIFadd{such as RegTensor~\mbox{%DIFAUXCMD
\citep{zhou2023regtensor} }\hskip0pt%DIFAUXCMD
}\DIFaddend and diffusion models \DIFdelbegin \DIFdel{(CSDI, TimeDiT)}\DIFdelend \DIFaddbegin \DIFadd{such as CSDI~\mbox{%DIFAUXCMD
\citep{tashiro2021csdi} }\hskip0pt%DIFAUXCMD
and TimeDiT~\mbox{%DIFAUXCMD
\citep{cao2024timedit}}\hskip0pt%DIFAUXCMD
}\DIFaddend , where performance is gauged by reconstruction error and temporal coherence. Finally, for data synthesis, generative frameworks such as QuantGAN\DIFdelbegin \DIFdel{and CoFinDiff }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{wiese2020} }\hskip0pt%DIFAUXCMD
and CoFinDiff~\mbox{%DIFAUXCMD
\citep{tanaka2025cofindiff} }\hskip0pt%DIFAUXCMD
}\DIFaddend are employed to simulate realistic returns, market scenarios, and complex limit order flows. Consequently, the evaluation criteria for synthesis shift entirely away from point-wise accuracy, prioritizing instead the preservation of stylized facts, distributional fidelity, and the practical downstream utility of the generated data.

 \subsection{\DIFdelbegin \DIFdel{Financial Time-Series }\DIFdelend Extrapolation\DIFaddbegin \DIFadd{: Problem Characteristics}\DIFaddend }
\label{sec:extrapolation}

\DIFdelbegin \subsection{\DIFdel{Problem Characteristics}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend Financial Time-Series Extrapolation (FTSE) is formally defined as the task of generating future trajectories $\mathbf{x}_{t+1:T}$ given a historical context $\mathbf{x}_{1:t}$. Unlike traditional point forecasting, modern FTSE is approached as a probabilistic conditional generation problem: learning the conditional distribution $p(\mathbf{x}_{t+1:T} | \mathbf{x}_{1:t})$ to capture the inherent uncertainty and multi-modal nature of financial markets. This shift acknowledges that financial markets are stochastic systems with complex, time-varying dynamics.

\DIFdelbegin \DIFdel{The problem exhibits several distinct characteristics that differentiate it }\DIFdelend \DIFaddbegin \DIFadd{Four characteristics distinguish this problem }\DIFaddend from general sequence extrapolation\DIFdelbegin \DIFdel{:
}%DIFDELCMD < \begin{enumerate}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Causality Constraint}}%DIFAUXCMD
\DIFdel{: The generation must be strictly causal—}\DIFdelend \DIFaddbegin \DIFadd{.
}

\DIFadd{The causality constraint requires strict causality: }\DIFaddend future values depend solely on past observations without any look-ahead bias. This is analogous to autoregressive language modeling, but with continuous, real-valued tokens.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Non-Stationarity}}%DIFAUXCMD
\DIFdel{: Financial }\DIFdelend \DIFaddbegin 

\DIFadd{Non-stationarity means that financial }\DIFaddend time series exhibit time-varying statistical properties (e.g., changing volatility regimes, structural breaks). The conditional distribution $p(\mathbf{x}_{t+1:T} | \mathbf{x}_{1:t})$ itself evolves over time, requiring models to adapt to distributional shifts.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Low Signal-to-Noise Ratio}}%DIFAUXCMD
\DIFdel{: Market }\DIFdelend \DIFaddbegin 

\DIFadd{The low signal-to-noise ratio reflects that market }\DIFaddend data is dominated by noise, making it challenging to separate genuine predictive signals from random fluctuations. This often leads to overfitting in deterministic models.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{High-Dimensional Dependencies}}%DIFAUXCMD
\DIFdel{: In multi-variate settings, }\DIFdelend \DIFaddbegin 

\DIFadd{High-dimensional dependencies arise in multivariate settings, where }\DIFaddend assets exhibit complex cross-sectional dependencies (e.g., lead-lag relationships, sector correlations). Modeling these dependencies is crucial for coherent joint generation.
\DIFdelbegin %DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdelend 

Formally, the objective is to learn a generator $G_\theta$ that maps the context $\mathbf{x}_{1:t}$ to a distribution over future sequences, often parameterized as a set of samples $\{\mathbf{x}_{t+1:T}^{(i)}\}_{i=1}^N$. The evaluation metrics extend beyond point-wise accuracy (e.g., MSE) to probabilistic scoring rules \DIFdelbegin \DIFdel{(e.g., CRPS, Energy Score) }\DIFdelend \DIFaddbegin \DIFadd{such as CRPS~\mbox{%DIFAUXCMD
\citep{gneiting2007strictly} }\hskip0pt%DIFAUXCMD
and the energy score~\mbox{%DIFAUXCMD
\citep{gneiting2008assessing}}\hskip0pt%DIFAUXCMD
, }\DIFaddend and domain-specific financial utility measures (e.g., portfolio returns, risk metrics).

\subsection{\DIFaddbegin \DIFadd{Extrapolation: }\DIFaddend Classical and Hybrid Predictive Models}

Classical statistical frameworks have historically provided the mathematical bedrock for modeling financial time-series dynamics through rigorous probabilistic assumptions. A prominent example is the Auto-Regressive Integrated Moving Average (ARIMA) model \citep{box1970}, which explicitly formulates temporal dependencies by combining autoregressive (AR) processes, differencing (I) to achieve stationarity, and moving average (MA) components. For a given time series $y_t$, an $\text{ARIMA}(p, d, q)$ specification is mathematically expressed as:
\begin{equation}
\nabla^d y_t = c + \sum_{i=1}^p \phi_i \nabla^d y_{t-i} + \sum_{j=1}^q \theta_j \varepsilon_{t-j} + \varepsilon_t
\end{equation}
where $\nabla^d$ represents the $d$-th order difference operator, $\phi_i$ and $\theta_j$ are the AR and MA coefficients respectively, and $\varepsilon_t \sim \mathcal{N}(0, \sigma^2)$ acts as white noise. While ARIMA models are effective at isolating linear relationships, they inherently fail to encompass complex market phenomena such as volatility clustering and heavy-tailed distributions. To capture these time-varying variance dynamics, the \DIFdelbegin \DIFdel{Generalized Autoregressive Conditional Heteroskedasticity }\DIFdelend \DIFaddbegin \DIFadd{generalized autoregressive conditional heteroskedasticity }\DIFaddend (GARCH) framework\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{kim2021} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{bollerslev1986garch} }\hskip0pt%DIFAUXCMD
}\DIFaddend was introduced. The conditional variance $\sigma_t^2$ in a $\text{GARCH}(p, q)$ model is iteratively defined by past squared innovations and past variances:
\begin{equation}
\sigma_t^2 = \omega + \sum_{i=1}^p \alpha_i \varepsilon_{t-i}^2 + \sum_{j=1}^q \beta_j \sigma_{t-j}^2
\end{equation}
where $\omega > 0$, $\alpha_i \ge 0$, and $\beta_j \ge 0$. Although extensions \DIFdelbegin \DIFdel{like EGARCH}\DIFdelend \DIFaddbegin \DIFadd{such as EGARCH~\mbox{%DIFAUXCMD
\citep{nelson1991egarch} }\hskip0pt%DIFAUXCMD
}\DIFaddend further accommodate asymmetric leverage effects resulting from negative shocks, these purely parametric methods struggle to scale to high-dimensional dependencies and intricate non-linear patterns.

To address the constraints of classical statistical models, the field transitioned toward deep learning architectures, particularly \DIFdelbegin \DIFdel{Recurrent Neural Networks }\DIFdelend \DIFaddbegin \DIFadd{recurrent neural networks }\DIFaddend (RNNs)\DIFdelbegin \DIFdel{and Long Short-Term Memory }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{elman1990finding} }\hskip0pt%DIFAUXCMD
and long short-term memory }\DIFaddend (LSTM) networks\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{tsa1, qin2025, li2025ceemdan, caliskan2025, SGP-LSTM1}}\hskip0pt%DIFAUXCMD
.}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{hochreiter1997lstm}}\hskip0pt%DIFAUXCMD
; recent financial applications include the hybrid models studied by Qin et al.~\mbox{%DIFAUXCMD
\citep{qin2025}}\hskip0pt%DIFAUXCMD
, Li et al.~\mbox{%DIFAUXCMD
\citep{li2025ceemdan}}\hskip0pt%DIFAUXCMD
, Caliskan~\mbox{%DIFAUXCMD
\citep{caliskan2025}}\hskip0pt%DIFAUXCMD
, and Li et al.~\mbox{%DIFAUXCMD
\citep{SGP-LSTM1}}\hskip0pt%DIFAUXCMD
. }\DIFaddend LSTMs leverage a sophisticated gating mechanism—comprising forget ($f_t$), input ($i_t$), and output ($o_t$) gates alongside a cell state ($C_t$) and hidden state ($h_t$)—to effectively capture long-range temporal dependencies without suffering from severe vanishing gradients:
\begin{align}
f_t &= \sigma(\mathbf{W}_f \cdot[h_{t-1}, x_t] + b_f) \\
i_t &= \sigma(\mathbf{W}_i \cdot [h_{t-1}, x_t] + b_i) \\
\tilde{C}_t &= \tanh(\mathbf{W}_C \cdot[h_{t-1}, x_t] + b_C) \\
C_t &= f_t \odot C_{t-1} + i_t \odot \tilde{C}_t \\
o_t &= \sigma(\mathbf{W}_o \cdot [h_{t-1}, x_t] + b_o) \\
h_t &= o_t \odot \tanh(C_t)
\end{align}
However, when standard LSTMs are optimized using traditional pointwise loss functions such as the Mean Squared Error (MSE), defined as $\mathcal{L}_{MSE} = \frac{1}{n} \sum_{i=1}^n (y_i - \hat{y}_i)^2$, they often overfit the ubiquitous noise present in financial data.

Consequently, recent literature has pivoted towards sophisticated hybrid strategies that couple deep learning backbones with domain-specific signal processing or invariant learning techniques to enhance robustness. Denoising-based hybrids, for example, incorporate explicit signal decomposition to systematically separate underlying predictive trends from stochastic market noise. The PEC-W model \citep{caliskan2025} leverages the \DIFdelbegin \DIFdel{Discrete Wavelet Transform }\DIFdelend \DIFaddbegin \DIFadd{discrete wavelet transform }\DIFaddend (DWT)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{mallat1989theory} }\hskip0pt%DIFAUXCMD
}\DIFaddend to split raw input series into multi-resolution scale and wavelet representations:
\begin{equation}
x(t) = \sum_k c_{J,k} \phi_{J,k}(t) + \sum_{j=1}^J \sum_k d_{j,k} \psi_{j,k}(t)
\end{equation}
where $\phi$ and $\psi$ denote the scaling and wavelet functions. These denoised approximation coefficients provide a cleaner input signal for the LSTM, a concept similarly explored by CEEMDAN (Complete Ensemble Empirical Mode Decomposition with Adaptive Noise)\DIFdelbegin \DIFdel{-Informer-LSTM }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{torres2011complete}}\hskip0pt%DIFAUXCMD
-Informer~\mbox{%DIFAUXCMD
\citep{zhou2021informer}}\hskip0pt%DIFAUXCMD
-LSTM model of Li et al.~}\DIFaddend \citep{li2025ceemdan}, which decomposes the signal into intrinsic mode functions. Beyond denoising, feature engineering hybrids seek to automate the discovery of robust predictive inputs. The SGP-LSTM framework \citep{SGP-LSTM1} employs \DIFdelbegin \DIFdel{Symbolic Genetic Programming }\DIFdelend \DIFaddbegin \DIFadd{symbolic genetic programming~\mbox{%DIFAUXCMD
\citep{koza1992genetic} }\hskip0pt%DIFAUXCMD
}\DIFaddend to iteratively evolve technical indicators, steering the search via a multi-objective fitness function that balances return, consistency, and predictive correlation (IC):
\begin{equation}
\text{Fitness} = \text{TopR} + \lambda_1 \times \text{Monotonicity} + \lambda_2 \times \text{IC}
\end{equation}
Finally, invariant learning hybrids target the problem of distributional shifts across varying market regimes. \DIFdelbegin \DIFdel{The Invariant Risk Minimization }\DIFdelend \DIFaddbegin \DIFadd{Building on invariant risk minimization }\DIFaddend (IRM)\DIFdelbegin \DIFdel{approach proposed by \mbox{%DIFAUXCMD
\citep{cao2023irm} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{arjovsky2019invariant}}\hskip0pt%DIFAUXCMD
, Cao et al.~\mbox{%DIFAUXCMD
\citep{cao2023irm} }\hskip0pt%DIFAUXCMD
propose a financial adaptation that }\DIFaddend enforces the extraction of representations that hold true regardless of whether the market is in a bullish, bearish, or sideways phase, minimizing an objective that penalizes environmental variance:
\begin{equation}
\mathcal{L}_{IRM} = \underbrace{R^e(\mathbf{w} \cdot \Phi)}_{\text{Empirical risk}} + \gamma \underbrace{\left\| \nabla_{\mathbf{w}|_{\mathbf{w}=1.0}} R^e(\mathbf{w} \cdot \Phi) \right\|_2^2}_{\text{Invariant penalty}}
\end{equation}
where $\Phi$ acts as the representation function and $\mathbf{w}$ is a linear classifier. Despite these multifaceted improvements, hybrid predictive architectures ultimately generate deterministic point forecasts, fundamentally limiting their capacity to model the full predictive distribution essential for robust financial scenario generation.

\subsection{\DIFaddbegin \DIFadd{Extrapolation: }\DIFaddend Generative Models\DIFdelbegin \DIFdel{for Extrapolation}\DIFdelend }

Recognizing the intrinsic limitations of deterministic forecasting, the paradigm of generative models shifts the focus toward learning the underlying data distribution $p(\mathbf{x})$ or the conditional probability $p(\mathbf{x}_{t+1:T}|\mathbf{x}_{1:t})$. This transition allows practitioners to sample multiple plausible future trajectories, accommodating the multi-modal uncertainty inherent in financial markets. Early \DIFdelbegin \DIFdel{efforts in this domain heavily utilized Generative Adversarial Networks }\DIFdelend \DIFaddbegin \DIFadd{financial studies adapted generative adversarial networks }\DIFaddend (GANs)\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{Time-GAN, naritomi2020data}}\hskip0pt%DIFAUXCMD
, which }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan}}\hskip0pt%DIFAUXCMD
; examples include TimeGAN~\mbox{%DIFAUXCMD
\citep{Time-GAN} }\hskip0pt%DIFAUXCMD
and the conditional GAN proposed by Naritomi and Adachi~\mbox{%DIFAUXCMD
\citep{naritomi2020data}}\hskip0pt%DIFAUXCMD
. These models }\DIFaddend deploy a generator $G$ and a discriminator $D$ locked in a competitive minimax game. The generator synthesizes sequences from historical context $\mathbf{x}_{1:t}$ and a latent noise vector $\mathbf{z}$, while the discriminator attempts to distinguish real historical samples from the generated distributions:
\begin{equation}
\min_G \max_D \mathbb{E}_{\mathbf{x} \sim p_{data}}[\log D(\mathbf{x})] + \mathbb{E}_{\mathbf{z} \sim p_z}[\log(1 - D(G(\mathbf{z})))]
\end{equation}
While conceptually elegant, GANs \DIFdelbegin \DIFdel{are notoriously }\DIFdelend \DIFaddbegin \DIFadd{can be }\DIFaddend difficult to train on volatile financial data, frequently suffering from mode collapse and unstable convergence. To mitigate these issues, specialized adaptations have been devised. WaveGAN \citep{dai2024wavegan} incorporates a multi-resolution DWT denoising mechanism combined with a Wasserstein GAN equipped with \DIFdelbegin \DIFdel{Gradient Penalty }\DIFdelend \DIFaddbegin \DIFadd{gradient penalty }\DIFaddend (WGAN-GP)\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{wang2024denoiser}}\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{arjovsky2017wgan,gulrajani2017wgangp}}\hskip0pt%DIFAUXCMD
}\DIFaddend . The WGAN-GP loss enforces a Lipschitz constraint to prevent vanishing gradients:
\begin{equation}
\mathcal{L}_{\text{WGAN-GP}} = \mathbb{E}_{\tilde{\mathbf{x}} \sim p_g}[D(\tilde{\mathbf{x}})] - \mathbb{E}_{\mathbf{x} \sim p_{data}}[D(\mathbf{x})] + \lambda \mathbb{E}_{\hat{\mathbf{x}} \sim p_{\hat{\mathbf{x}}}}[(\|\nabla_{\hat{\mathbf{x}}} D(\hat{\mathbf{x}})\|_2 - 1)^2]
\end{equation}
where $\hat{\mathbf{x}}$ represents a convex interpolation of real and generated instances. Concurrently, models like N-BEATS-GAN \citep{dai2026nbeats} fuse interpretable basis expansions \citep{oreshkin2020nbeats} with an adversarial framework, modeling future values as a sum of trend and seasonality bases $\hat{\mathbf{y}} = \sum_{i=1}^N \text{Basis}_i(\boldsymbol{\theta}_i)$. While these modifications enhance stability and interpretability, GANs often yield over-smoothed samples that fail to encapsulate the sharp, multi-modal nature of empirical return distributions.

To achieve higher fidelity in distribution matching, Denoising Diffusion Probabilistic Models (DDPMs) \citep{ho2020ddpm} have emerged as a more stable and expressive alternative. Diffusion frameworks systematically destroy structured data by injecting Gaussian noise over $T$ forward steps—$q(\mathbf{x}_t|\mathbf{x}_{t-1}) = \mathcal{N}(\mathbf{x}_t; \sqrt{1 - \beta_t}\mathbf{x}_{t-1}, \beta_t \mathbf{I})$—and then learn a parameterized reverse process $p_\theta(\mathbf{x}_{t-1}|\mathbf{x}_t) = \mathcal{N}(\mathbf{x}_{t-1}; \boldsymbol{\mu}_\theta(\mathbf{x}_t, t), \sigma_t^2 \mathbf{I})$ to reconstruct the signal. The optimization simplifies to predicting the incrementally added noise $\boldsymbol{\epsilon}$:
\begin{equation}
\mathcal{L}_{simple} = \mathbb{E}_{t, \mathbf{x}_0, \boldsymbol{\epsilon}}[\|\boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t)\|^2]
\end{equation}
For context-conditioned financial generation, TimeGrad \citep{rasul2021timegrad} integrates an RNN-based encoder with a score-based diffusion decoder, generating future states via Langevin dynamics where the context encoding $\mathbf{c}$ steers the reverse sampling:
\begin{equation}
\mathbf{x}_{t-1} = \frac{1}{\sqrt{\alpha_t}} \left( \mathbf{x}_t - \frac{\beta_t}{\sqrt{1 - \bar{\alpha}_t}} \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, \mathbf{c}) \right) + \sigma_t \mathbf{z}
\end{equation}
Addressing the limitations of recurrent architectures in capturing extended dependencies, TimeDiT\DIFaddbegin \DIFadd{~}\DIFaddend \citep{cao2024timedit} replaces the RNN with a \DIFdelbegin \DIFdel{Diffusion Transformer }\DIFdelend \DIFaddbegin \DIFadd{diffusion Transformer of the type introduced by Peebles and Xie~\mbox{%DIFAUXCMD
\citep{peebles2023scalable}}\hskip0pt%DIFAUXCMD
}\DIFaddend . Utilizing patch embeddings and multi-head attention, TimeDiT operates on a $\mu$-parameterized loss function, explicitly aligning the predicted mean of the reverse process with its theoretical target:
\begin{equation}
\mathcal{L} = \sum_{t=1}^T \mathbb{E}_{q(\mathbf{x}_t^{tar}|\mathbf{x}_0^{con})} \left[ \|\boldsymbol{\mu}(\mathbf{x}_t^{tar}, \mathbf{x}_0^{con}) - \boldsymbol{\mu}_\theta(\mathbf{x}_t^{tar}, t|\mathbf{x}_0^{con})\|^2 \right]
\end{equation}
An alternative diffusion formulation leverages continuous-time score matching\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{song2019generative,song2021score}}\hskip0pt%DIFAUXCMD
}\DIFaddend , optimizing a score network to approximate the gradient of the log-density:
\begin{equation}
\mathcal{L}_{score} = \mathbb{E}_{t, \mathbf{x}_0, \boldsymbol{\epsilon}} \left[ \left\| \boldsymbol{\epsilon} - \sigma_t \nabla_{\mathbf{x}_t} \log p_\theta(\mathbf{x}_t) \right\|^2 \right]
\end{equation}
While diffusion and score-based models generate high-fidelity distributions with sharp multi-modal properties, their iterative sampling procedures introduce significant inference latency, presenting an ongoing challenge for high-frequency financial applications.

\subsection{\DIFdelbegin \DIFdel{Time Series }\DIFdelend \DIFaddbegin \DIFadd{Extrapolation: Time-Series }\DIFaddend Foundation Models}

Inspired by the \DIFdelbegin \DIFdel{transformative success of Large Language Models (LLMs) }\DIFdelend \DIFaddbegin \DIFadd{success of Transformers~\mbox{%DIFAUXCMD
\citep{vaswani2017attention} }\hskip0pt%DIFAUXCMD
and large language models~\mbox{%DIFAUXCMD
\citep{brown2020language} }\hskip0pt%DIFAUXCMD
}\DIFaddend in natural language processing\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{Survey1, Survey2, tsa2}}\hskip0pt%DIFAUXCMD
}\DIFdelend , current research is heavily invested in adapting the "pre-train, then fine-tune" paradigm to financial time-series extrapolation. These \DIFdelbegin \DIFdel{Foundation Models }\DIFdelend \DIFaddbegin \DIFadd{foundation models }\DIFaddend are trained on massive, heterogeneous datasets to derive generalized temporal representations, effectively enabling zero-shot inference and cross-domain adaptation. The primary challenge in this domain is bridging the continuous numerical space of financial data with the discrete, tokenized vocabulary natively processed by \DIFdelbegin \DIFdel{transformer-based LLMs}\DIFdelend \DIFaddbegin \DIFadd{Transformer-based language models}\DIFaddend . Tokenization-based fine-tuning strategies directly address this by casting forecasting as a next-token prediction task. For instance, Chronos \citep{ansari2024chronos} scales time-series observations and maps them into a fixed dictionary of tokens using quantile binning:
\begin{equation}
\text{token}(\mathbf{x}) = \arg\min_i |x - q_i|
\end{equation}
where $q_i$ represent pre-computed quantiles. While this approach benefits from robust zero-shot generalization, the inherent discretization process sacrifices fine-grained numerical precision. Conversely, patch-based fine-tuning techniques, as exemplified by OneFitsAll \citep{zhou2023onefitsall}, bypass discretization by segmenting the time-series into sequences of sub-series patches. Each patch is projected into the high-dimensional embedding space of the LLM via a linear transformation:
\begin{equation}
\mathbf{E}_{patch} = \mathbf{W}_p \mathbf{x}_{patch} + \mathbf{b}_p
\end{equation}
These continuous patch embeddings are then prepended as visual-like tokens, \DIFdelbegin \DIFdel{fully }\DIFdelend preserving numerical fidelity while \DIFdelbegin \DIFdel{seamlessly }\DIFdelend leveraging the LLM's cross-patch attention mechanisms.

As illustrated in Figure \ref{fig:llm_comparison}, beyond conventional fine-tuning, sophisticated model editing and reprogramming approaches have emerged to align disparate modalities using lightweight adapters, averting the computational burden of full-parameter updates. StockTime \citep{wang2024stocktime} enriches time-series representations by encoding high-frequency tick data alongside handcrafted textual summaries (e.g., "bullish engulfing pattern") via a dual-encoder. Similarly, frameworks like LLM4FTS \citep{liu2025llm4fts} and LLM-PS \citep{tang2025llmps} \DIFdelbegin \DIFdel{utilize Dynamic Time Warping }\DIFdelend \DIFaddbegin \DIFadd{use dynamic time warping }\DIFaddend (DTW)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{sakoe1978dynamic} }\hskip0pt%DIFAUXCMD
}\DIFaddend and text-translation modules to bridge the semantic divide between raw price trajectories and natural language descriptions. A more fundamental alignment is achieved through Prompt-as-Prefix reprogramming, popularized by Time-LLM \citep{jin2024timellm}. This technique steers frozen LLMs by projecting time-series patches onto a set of learned "text prototypes" $\mathbf{P} \in \mathbb{R}^{M \times d}$ using cross-attention dynamics:
\begin{align}
\alpha_{ij} &= \text{softmax} \left(\frac{(\mathbf{W}_q \mathbf{x}_i) \cdot (\mathbf{W}_k \mathbf{p}_j)^\top}{\sqrt{d}}\right) \\
\tilde{\mathbf{x}}_i &= \sum_j \alpha_{ij} \mathbf{p}_j
\end{align}
The resulting reprogrammed patches act as informative prefixes for the frozen language model. Expanding upon this, the CALF architecture \citep{liu2025calf} employs a dual-branch structure \DIFdelbegin \DIFdel{fortified by }\DIFdelend \DIFaddbegin \DIFadd{with }\DIFaddend consistency losses to \DIFdelbegin \DIFdel{strictly minimize }\DIFdelend \DIFaddbegin \DIFadd{penalize }\DIFaddend the discrepancy between textual and temporal feature distributions.

In parallel, researchers are developing purpose-built forecasting architectures that integrate domain-specific inductive biases directly into the foundation model backbone. TimeHF \citep{qi2025timehf} introduces a time-series policy optimization (TSPO) framework that aligns generative outputs with specific financial human preferences, such as maximizing risk-adjusted returns, using reinforcement learning from human feedback (RLHF)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{christiano2017deep,ouyang2022training}}\hskip0pt%DIFAUXCMD
}\DIFaddend :
\begin{equation}
\mathcal{L}_{TSPO} = -\mathbb{E}_{\tau \sim \pi_\theta}[R(\tau)] + \beta \text{KL}[\pi_\theta \| \pi_{ref}]
\end{equation}
where $\pi_\theta$ denotes the policy (generative model) parameterized by $\theta$, $\tau \sim \pi_\theta$ is a sampled generation trajectory, $R(\tau)$ is the reward function that scores trajectory quality according to human financial preferences, $\beta$ controls the strength of the KL-divergence regularization, and $\pi_{\text{ref}}$ is the frozen reference policy that prevents catastrophic drift from pre-trained behavior.
FinCast \citep{zhu2025fincast} \DIFdelbegin \DIFdel{attacks }\DIFdelend \DIFaddbegin \DIFadd{addresses }\DIFaddend the multi-scale nature of market data using a sparse \DIFdelbegin \DIFdel{Mixture-of-Experts }\DIFdelend \DIFaddbegin \DIFadd{mixture-of-experts }\DIFaddend (MoE) backbone\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{shazeer2017outrageously,fedus2022switch} }\hskip0pt%DIFAUXCMD
}\DIFaddend enhanced with learnable frequency embeddings. To ensure the model delivers both accurate point estimates and reliable probabilistic bounds, it optimizes a joint Point-Quantile loss:
\begin{align}
\mathcal{L}_{PQ} &= \lambda_1 \mathcal{L}_{point} + \lambda_2 \mathcal{L}_{quantile} \\
\mathcal{L}_{quantile} &= \sum_q \rho_q(y - \hat{y}_q), \quad \rho_q(u) = u(q - \mathbb{I}(u < 0))
\end{align}
where $\mathcal{L}_{point}$ handles deterministic accuracy and the pinball loss \DIFdelbegin \DIFdel{$\mathcal{L}_{quantile}$ }\DIFdelend \DIFaddbegin \DIFadd{derived from quantile regression~\mbox{%DIFAUXCMD
\citep{koenker1978regression} }\hskip0pt%DIFAUXCMD
}\DIFaddend governs risk coverage, \DIFdelbegin \DIFdel{yielding highly }\DIFdelend \DIFaddbegin \DIFadd{with the reported aim of improving }\DIFaddend robust zero-shot performance across \DIFdelbegin \DIFdel{diverse }\DIFdelend asset classes.

\subsection{\DIFaddbegin \DIFadd{Extrapolation: }\DIFaddend Emerging Trends}
\DIFdelbegin \DIFdel{Moving forward, the field is poised to tackle several critical bottlenecks that limit the operationalization of generative models }\DIFdelend \DIFaddbegin \DIFadd{Several bottlenecks remain before generative models can be operationalized }\DIFaddend in live financial systems. A primary objective is the development of unified hybrid architectures that can \DIFdelbegin \DIFdel{seamlessly amalgamate }\DIFdelend \DIFaddbegin \DIFadd{combine }\DIFaddend the low-latency inference speeds of discriminative models with the distributional robustness of diffusion frameworks, potentially leveraging \DIFdelbegin \DIFdel{sophisticated knowledge distillationtechniques. Concurrently, there is an urgent need to redefine evaluation protocols}\DIFdelend \DIFaddbegin \DIFadd{knowledge distillation~\mbox{%DIFAUXCMD
\citep{hinton2015distilling}}\hskip0pt%DIFAUXCMD
. Evaluation protocols also need to be expanded}\DIFaddend ; existing metrics such as the \DIFdelbegin \DIFdel{Continuous Ranked Probability Score }\DIFdelend \DIFaddbegin \DIFadd{continuous ranked probability score }\DIFaddend (CRPS) \DIFdelbegin \DIFdel{must }\DIFdelend \DIFaddbegin \DIFadd{should }\DIFaddend be augmented with practical financial utility measures, including risk-adjusted returns \DIFaddbegin \DIFadd{based on measures such as the Sharpe ratio~\mbox{%DIFAUXCMD
\citep{sharpe1994sharpe} }\hskip0pt%DIFAUXCMD
}\DIFaddend and maximum drawdowns, \DIFdelbegin \DIFdel{to ensure models align with authentic }\DIFdelend \DIFaddbegin \DIFadd{so that models better reflect practical }\DIFaddend trading objectives. Furthermore, future iterations of generative models \DIFdelbegin \DIFdel{must }\DIFdelend \DIFaddbegin \DIFadd{should }\DIFaddend incorporate causal constraints that respect \DIFdelbegin \DIFdel{intricate market microstructures, fundamentally eliminating }\DIFdelend \DIFaddbegin \DIFadd{market microstructures~\mbox{%DIFAUXCMD
\citep{ohara1995market,gould2013limit}}\hskip0pt%DIFAUXCMD
, reducing }\DIFaddend the look-ahead biases that \DIFdelbegin \DIFdel{persistently plague }\DIFdelend \DIFaddbegin \DIFadd{can affect }\DIFaddend synthetic market simulators. Finally, advancing computationally efficient fine-tuning protocols \DIFdelbegin \DIFdel{will be paramount to }\DIFdelend \DIFaddbegin \DIFadd{is important to help }\DIFaddend ensure that massive foundation models can rapidly adapt to sudden structural regime shifts without succumbing to catastrophic forgetting\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{mccloskey1989catastrophic,zenke2017continual}}\hskip0pt%DIFAUXCMD
}\DIFaddend .

\subsection{\DIFdelbegin \DIFdel{Summarization}\DIFdelend \DIFaddbegin \DIFadd{Extrapolation: Research Synthesis}\DIFaddend }
The progressive evolution of financial time-series extrapolation models demonstrates a clear overarching trajectory in machine learning: transitioning from deterministic, discriminative point predictors to expressive generative distribution learners, and ultimately culminating in large-scale foundation models. As synthesized in Table \ref{tab:extrapolation_comparison}, each methodological paradigm presents distinct trade-offs between inferential speed, distributional fidelity, and generalization capability. Classical and hybrid predictive models, such as ARIMA and LSTM-based architectures, \DIFdelbegin \DIFdel{retain their dominance }\DIFdelend \DIFaddbegin \DIFadd{remain attractive }\DIFaddend in latency-sensitive environments like high-frequency execution algorithms, where rapid point forecasting is \DIFdelbegin \DIFdel{paramount}\DIFdelend \DIFaddbegin \DIFadd{important}\DIFaddend . However, their structural inability to quantify tail-risk and multimodal uncertainty \DIFdelbegin \DIFdel{severely restricts }\DIFdelend \DIFaddbegin \DIFadd{limits }\DIFaddend their utility in comprehensive risk management. To resolve this, generative architectures—notably GANs and diffusion models—\DIFdelbegin \DIFdel{excel }\DIFdelend \DIFaddbegin \DIFadd{are often used }\DIFaddend in scenario generation and stress testing by synthesizing \DIFdelbegin \DIFdel{highly realistic, }\DIFdelend \DIFaddbegin \DIFadd{plausible }\DIFaddend multi-modal market trajectories. \DIFdelbegin \DIFdel{Recent studies often report that diffusion models yield }\DIFdelend \DIFaddbegin \DIFadd{Studies of TimeGrad, TimeDiT, and related financial diffusion models often report }\DIFaddend better calibrated distributions than \DIFdelbegin \DIFdel{GAN baselines}\DIFdelend \DIFaddbegin \DIFadd{selected GAN baselines~\mbox{%DIFAUXCMD
\citep{rasul2021timegrad,cao2024timedit,tanaka2025cofindiff}}\hskip0pt%DIFAUXCMD
}\DIFaddend , although this gain typically comes at the cost of substantially higher computational burden during iterative reverse-sampling. Time-series foundation models have recently become an important direction by exhibiting \DIFdelbegin \DIFdel{unprecedented }\DIFdelend \DIFaddbegin \DIFadd{strong }\DIFaddend zero-shot forecasting capabilities \DIFaddbegin \DIFadd{in benchmark experiments}\DIFaddend , enabling the deployment of predictive systems across previously unseen assets or shifting market regimes. Despite \DIFdelbegin \DIFdel{these breakthroughs}\DIFdelend \DIFaddbegin \DIFadd{this progress}\DIFaddend , foundation models still \DIFdelbegin \DIFdel{grapple }\DIFdelend \DIFaddbegin \DIFadd{face }\DIFaddend with persistent challenges regarding temporal alignment, cross-modal semantic gaps, and the propensity to hallucinate implausible financial patterns over extended horizons.

 \subsection{\DIFdelbegin \DIFdel{Financial Time-Series }\DIFdelend Imputation\DIFaddbegin \DIFadd{: Problem Formulation and Characteristics}\DIFaddend }
\label{sec:imputation}

\DIFdelbegin \subsection{\DIFdel{Problem Formulation and Characteristics}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend Financial time-series imputation is fundamentally formulated as a bidirectional, closed-format generation task. Given a multivariate sequence $\mathbf{X} \in \mathbb{R}^{T \times d}$ and a corresponding binary indicator mask $\mathbf{M} \in \{0, 1\}^{T \times d}$ where $m_{t,i} = 1$ denotes a missing value, the objective is to learn a conditional distribution $p(\mathbf{X}_{miss} | \mathbf{X}_{obs}; \theta)$ to accurately reconstruct the missing elements $\mathbf{X}_{miss} = \mathbf{X} \odot \mathbf{M}$ conditioned strictly on the observed elements $\mathbf{X}_{obs} = \mathbf{X} \odot (\mathbf{1} - \mathbf{M})$.

In financial contexts, the missingness mechanisms—\DIFdelbegin \DIFdel{Missing Completely At Random }\DIFdelend \DIFaddbegin \DIFadd{missing completely at random }\DIFaddend (MCAR), \DIFdelbegin \DIFdel{Missing At Random }\DIFdelend \DIFaddbegin \DIFadd{missing at random }\DIFaddend (MAR), and \DIFdelbegin \DIFdel{Missing Not At Random }\DIFdelend \DIFaddbegin \DIFadd{missing not at random }\DIFaddend (MNAR)\DIFaddbegin \DIFadd{, formalized by Rubin~\mbox{%DIFAUXCMD
\citep{rubin1976inference}}\hskip0pt%DIFAUXCMD
}\DIFaddend —present unique challenges. MNAR is particularly prevalent due to structural market events such as sudden trading halts during market crashes or asynchronous trading hours across global exchanges\DIFaddbegin \DIFadd{, a setting related to non-synchronous observation of multivariate financial processes~\mbox{%DIFAUXCMD
\citep{hayashi2005covariance}}\hskip0pt%DIFAUXCMD
}\DIFaddend . Consequently, imputation models must conquer two primary domain-specific hurdles\DIFdelbegin \DIFdel{: (1) managing }\DIFdelend \DIFaddbegin \DIFadd{. First, models must manage }\DIFaddend \textit{contiguous missingness}, where extended data outages corrupt long temporal windows\DIFdelbegin \DIFdel{, and (2) maintaining }\DIFdelend \DIFaddbegin \DIFadd{. Second, models must maintain }\DIFaddend strict \textit{cross-variable consistency} without introducing look-ahead bias, which is critical to ensure that historical backtests of trading strategies remain structurally valid.

\subsection{\DIFaddbegin \DIFadd{Imputation: }\DIFaddend Statistical, Interpolation, and \DIFdelbegin \DIFdel{Machine Learning }\DIFdelend \DIFaddbegin \DIFadd{Machine-Learning }\DIFaddend Baselines}

Early methodologies treated financial imputation primarily as a \DIFdelbegin \DIFdel{matrix completion }\DIFdelend \DIFaddbegin \DIFadd{matrix-completion~\mbox{%DIFAUXCMD
\citep{candes2009exact} }\hskip0pt%DIFAUXCMD
}\DIFaddend or interpolation problem rooted in \DIFdelbegin \DIFdel{strict econometric }\DIFdelend \DIFaddbegin \DIFadd{explicit statistical }\DIFaddend assumptions. The classic Chow-Lin method \citep{chow1971} remains a foundational statistical approach for temporal disaggregation, frequently integrated with LASSO regression\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{tibshirani1996regression} }\hskip0pt%DIFAUXCMD
}\DIFaddend to estimate high-frequency (e.g., monthly) values from low-frequency (e.g., quarterly) observations using auxiliary market indicators. Later, \DIFdelbegin \DIFdel{machine learning }\DIFdelend \DIFaddbegin \DIFadd{machine-learning }\DIFaddend techniques such as \DIFdelbegin \DIFdel{KNN, MICE}\DIFdelend \DIFaddbegin \DIFadd{$k$-nearest-neighbour imputation~\mbox{%DIFAUXCMD
\citep{troyanskaya2001missing}}\hskip0pt%DIFAUXCMD
, multiple imputation by chained equations (MICE)~\mbox{%DIFAUXCMD
\citep{vanbuuren1999multiple}}\hskip0pt%DIFAUXCMD
}\DIFaddend , and Random Forests\DIFdelbegin \DIFdel{(e.g., missForest \mbox{%DIFAUXCMD
\citep{stekhoven2012}}\hskip0pt%DIFAUXCMD
) }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{breiman2001random}}\hskip0pt%DIFAUXCMD
, including missForest~\mbox{%DIFAUXCMD
\citep{stekhoven2012}}\hskip0pt%DIFAUXCMD
, }\DIFaddend offered flexible, non-parametric alternatives for tabular data imputation. The heterogeneous-data adversarial learning network proposed for FinTech applications by Khuwaja et al. \citep{NDDP} is included here as a related data-driven baseline. Operating in a continuous functional space, Kernel \DIFdelbegin \DIFdel{Ridge Regression \mbox{%DIFAUXCMD
\citep{smith2020}}\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{ridge regression~\mbox{%DIFAUXCMD
\citep{saunders1998dual,aronszajn1950theory}}\hskip0pt%DIFAUXCMD
, in a fixed-income application~\mbox{%DIFAUXCMD
\citep{smith2020}}\hskip0pt%DIFAUXCMD
, }\DIFaddend estimates missing discount curves by projecting noisy bond prices into a \DIFdelbegin \DIFdel{Reproducing Kernel Hilbert Space }\DIFdelend \DIFaddbegin \DIFadd{reproducing kernel Hilbert space }\DIFaddend (RKHS). This framework minimizes a weighted least-squares objective regularized with exponential weighting to deliberately suppress wild extrapolations during periods of high volatility.

As datasets expanded, traditional machine learning models such as \DIFdelbegin \DIFdel{K-Nearest Neighbors (KNN), Multiple Imputation by Chained Equations (MICE)}\DIFdelend \DIFaddbegin \DIFadd{$k$-nearest neighbours~\mbox{%DIFAUXCMD
\citep{troyanskaya2001missing}}\hskip0pt%DIFAUXCMD
, MICE~\mbox{%DIFAUXCMD
\citep{vanbuuren1999multiple}}\hskip0pt%DIFAUXCMD
}\DIFaddend , and Random Forest\DIFdelbegin \DIFdel{imputation \mbox{%DIFAUXCMD
\citep{stekhoven2012} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{/missForest imputation~\mbox{%DIFAUXCMD
\citep{breiman2001random,stekhoven2012} }\hskip0pt%DIFAUXCMD
}\DIFaddend were widely adopted. Random Forest imputation, in particular, leverages ensemble decision trees to dynamically predict missing values by capturing complex, non-linear feature interactions. \DIFdelbegin \DIFdel{Empirical studies demonstrate that in specific financial contexts---such as corporate distress prediction---Random Forest frameworks can surprisingly outperformadvanced deep learning alternatives, particularly when data sparsity is extreme (e.g., missing rates of 40--50\%)}\DIFdelend \DIFaddbegin \DIFadd{Evidence from corporate-distress prediction indicates that tree-based ensembles can remain competitive with, and in some sparse-data settings outperform, deep learners~\mbox{%DIFAUXCMD
\citep{sideras2024bankruptcy}}\hskip0pt%DIFAUXCMD
; the precise magnitude of the advantage is dataset-dependent}\DIFaddend .

\subsection{\DIFaddbegin \DIFadd{Imputation: }\DIFaddend Tensor Factorization and Deep Sequential Approaches}

To accommodate higher-dimensional non-linearities and multivariate temporal dependencies, research progressed toward deep interpolation and tensor decomposition networks. Deep sequential fusion architectures, such as the BiGRU-BiLSTM framework proposed by \DIFaddbegin \DIFadd{Wang et al.~}\DIFaddend \citep{wang2021bigru}, capture bidirectional temporal flows \DIFaddbegin \DIFadd{using gated recurrent units~\mbox{%DIFAUXCMD
\citep{cho2014learning} }\hskip0pt%DIFAUXCMD
within a bidirectional recurrent architecture~\mbox{%DIFAUXCMD
\citep{schuster1997bidirectional}}\hskip0pt%DIFAUXCMD
, }\DIFaddend and employ a \DIFdelbegin \DIFdel{Group Method of Data Handling }\DIFdelend \DIFaddbegin \DIFadd{group method of data handling }\DIFaddend (GMDH) polynomial \DIFdelbegin \DIFdel{regression decoder}\DIFdelend \DIFaddbegin \DIFadd{decoder~\mbox{%DIFAUXCMD
\citep{ivakhnenko1971polynomial} }\hskip0pt%DIFAUXCMD
}\DIFaddend to reconstruct complex missing patterns in multivariate series.

Concurrently, tensor factorization models emerged as highly efficient solutions for structural missingness. NMTucker\DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{varolgunes2023nmtucker} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{varolgunes2023nmtucker}}\hskip0pt%DIFAUXCMD
, building on Tucker decomposition~\mbox{%DIFAUXCMD
\citep{tucker1966some}}\hskip0pt%DIFAUXCMD
, }\DIFaddend introduced a non-linear Matryoshka Tucker decomposition, systematically replacing linear core operations with neural network-based reconstructions and enforcing recursive core constraints to mitigate overfitting on sparse financial tensors. Expanding upon this, RegTensor \citep{zhou2023regtensor} facilitates fast, non-linear coupled tensor completion across multiple sparse datasets. By sharing factor matrices across tensors, it captures latent cross-market dynamics while strictly enforcing uncorrelated temporal embeddings via an orthogonal regularization penalty:
\begin{equation}
\mathcal{L}_{RegTensor} = \mathcal{L}_{recon} + \lambda \|\mathbf{U}^\top \mathbf{U} - \mathbf{I}\|_F^2
\end{equation}
where $\mathbf{U}$ represents the temporal factor matrix and $\lambda$ controls the orthogonal constraint. While computationally efficient, these models occasionally fail to capture the high-frequency volatility jumps inherent to tick-level financial data.

\subsection{\DIFaddbegin \DIFadd{Imputation: }\DIFaddend Adversarial and Generative Imputation Dynamics}

To overcome the smoothing effects typical of standard interpolators, Generative \DIFdelbegin \DIFdel{Adversarial Networks }\DIFdelend \DIFaddbegin \DIFadd{adversarial networks }\DIFaddend (GANs)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan,yoon2018gain} }\hskip0pt%DIFAUXCMD
}\DIFaddend formulate missing data recovery as a competitive game. However, standard GANs frequently suffer from severe distributional incoherence—commonly referred to as "drift"—during contiguous data outages. To directly address this, AttnWGAIN \citep{wang2025attn} substitutes traditional convolutional layers with diagonal masked attention blocks, explicitly forcing the generator to jointly learn temporal and feature-wise correlations without accessing masked future tokens. Similarly, ImputeGAN\DIFaddbegin \DIFadd{~}\DIFaddend \citep{qin2023imputegan} leverages an Informer-based generator\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{zhou2021informer} }\hskip0pt%DIFAUXCMD
}\DIFaddend utilizing probabilistic sparse attention, significantly reducing the $O(L^2)$ computational complexity of long-sequence dependencies while maintaining fidelity.

To rigorously enforce distributional stability across extensive missing windows, the CWGAIN-GP framework \citep{wang2024cwgain} employs a \DIFdelbegin \DIFdel{Conditional }\DIFdelend \DIFaddbegin \DIFadd{conditional }\DIFaddend Wasserstein GAN fortified with a \DIFdelbegin \DIFdel{Gradient Penalty}\DIFdelend \DIFaddbegin \DIFadd{gradient penalty~\mbox{%DIFAUXCMD
\citep{arjovsky2017wgan,gulrajani2017wgangp}}\hskip0pt%DIFAUXCMD
}\DIFaddend . By conditioning the adversarial game on the observation mask $\mathbf{M}$ and enforcing Lipschitz continuity, it successfully bounds the generative drift, ensuring that the synthetic interpolations mathematically align with the heavy-tailed distributions of surrounding observable financial regimes.

\subsection{\DIFaddbegin \DIFadd{Imputation: }\DIFaddend Diffusion and Foundation Models for Missing Data}

Score-based diffusion models\DIFdelbegin \DIFdel{have recently established a new state-of-the-art in financial }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{ho2020ddpm,song2019generative,song2021score} }\hskip0pt%DIFAUXCMD
have become a leading approach for probabilistic }\DIFaddend imputation by treating missing value reconstruction as a conditional generative process. These architectures define a forward process that adds Gaussian noise strictly to the missing regions (or the entire sequence, masked subsequently), and optimize a reverse denoising step conditioned on $\mathbf{X}_{obs}$:
\begin{equation}
\mathcal{L}_{diff} = \mathbb{E}_{\mathbf{x}_0, \boldsymbol{\epsilon}, t}[\|\boldsymbol{\epsilon} - \boldsymbol{\epsilon}_\theta(\mathbf{x}_t, t, \mathbf{X}_{obs})\|^2]
\end{equation}
A \DIFdelbegin \DIFdel{seminal }\DIFdelend \DIFaddbegin \DIFadd{central }\DIFaddend architecture in this domain is CSDI \citep{tashiro2021csdi}, which utilizes a dual 2D-attention mechanism to seamlessly integrate temporal dynamics and cross-feature correlations. Subsequent works such as LSSDM, Score-CDM, and SaSDim \citep{LSSDM, Score-CDM, SaSDim} further advanced this paradigm. To eliminate the "boundary disharmony" problem—unrealistic volatility jumps at the borders of observed and imputed regions—advanced diffusion models incorporate explicit consistency constraints across temporal and spatial horizons \citep{zhang2024spatial}, as well as cross-variable boundaries \citep{zhou2024cross}.

Furthermore, models like LSCD \citep{fons2025lscd} and SPDM \citep{SPDM} employ frequency-domain transformations to map 1D time series into 2D periodic representations. This isolates stable macroeconomic cyclical components from high-frequency market microstructure noise during the denoising phase. Expanding this to transformer scales, TimeDiT \citep{cao2024timedit} introduces a \DIFdelbegin \DIFdel{Diffusion }\DIFdelend \DIFaddbegin \DIFadd{diffusion }\DIFaddend Transformer (DiT)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{peebles2023scalable} }\hskip0pt%DIFAUXCMD
}\DIFaddend utilizing a unified masking strategy, uniquely enabling fine-tuning-free model editing. This allows quantitative analysts to directly inject external physical constraints or market heuristics into the sampling trajectory.

Simultaneously, \DIFdelbegin \DIFdel{Large Language Models }\DIFdelend \DIFaddbegin \DIFadd{large language models }\DIFaddend (LLMs)\DIFdelbegin \DIFdel{have been ingeniously }\DIFdelend \DIFaddbegin \DIFadd{, building on few-shot language-model capabilities~\mbox{%DIFAUXCMD
\citep{brown2020language}}\hskip0pt%DIFAUXCMD
, have been }\DIFaddend adapted for heterogeneous tabular and sequential data imputation. LLM-Forest \citep{chen2023llmforest} pioneers an ensemble approach utilizing graph-augmented prompts. It constructs bipartite graphs to map data entries to feature values, utilizing hierarchical merging to retrieve nearest neighbors. These neighbors serve as few-shot prompts for a forest of LLMs, resolving the final imputed value via confidence-weighted voting. Additionally, frameworks like MTabGen \citep{liu2023mtabgen} bridge diffusion dynamics and mixed-type tabular formats by running parallel forward processes for categorical and continuous numerical features, unifying them via columnar embeddings.

\subsection{\DIFaddbegin \DIFadd{Imputation: }\DIFaddend Emerging Trends}

Recent literature \DIFdelbegin \DIFdel{underscores a definitive paradigm }\DIFdelend \DIFaddbegin \DIFadd{indicates a substantial }\DIFaddend shift toward handling complex, non-random missing patterns through specialized extensions of these generative frameworks. As \DIFdelbegin \DIFdel{comprehensively }\DIFdelend documented in recent methodological surveys by \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{shan2021} }\hskip0pt%DIFAUXCMD
and }\DIFdelend \DIFaddbegin \DIFadd{Shan et al.~\mbox{%DIFAUXCMD
\citep{shan2021} }\hskip0pt%DIFAUXCMD
and Du et al.~}\DIFaddend \citep{du2024}, the field is rapidly moving away from isolated point-interpolators toward unified, context-aware sequence modelers. We highlight three critical emerging trends driving this evolution.

The first trend concerns unified multi-task architectures: instead of treating imputation as an isolated preprocessing step, modern models increasingly solve related financial tasks simultaneously to improve representational learning. For example, \DIFaddbegin \DIFadd{Miao et al.~}\DIFaddend \citep{miao2025} propose coupled diffusion models that jointly perform forecasting and imputation, leveraging shared latent representations to enforce temporal consistency. To enhance structural robustness, \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{zhang2023imputation} }\hskip0pt%DIFAUXCMD
utilize }\DIFdelend \DIFaddbegin \DIFadd{Zhang et al.~\mbox{%DIFAUXCMD
\citep{zhang2023imputation} }\hskip0pt%DIFAUXCMD
use }\DIFaddend dual-attention networks to simultaneously capture local and global dependencies, while \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{cai2025} }\hskip0pt%DIFAUXCMD
employ Temporal Graph Networks }\DIFdelend \DIFaddbegin \DIFadd{Cai et al.~\mbox{%DIFAUXCMD
\citep{cai2025} }\hskip0pt%DIFAUXCMD
employ temporal graph networks }\DIFaddend (TGNs)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{rossi2020temporal} }\hskip0pt%DIFAUXCMD
}\DIFaddend to map dynamic cross-asset spillovers during fragmented market outages \citep{chen2023b}. Furthermore, \DIFaddbegin \DIFadd{Zhang et al.~}\DIFaddend \citep{zhang2025a} extend this paradigm beyond numerical data, advancing multi-modal frameworks that integrate textual sentiment to inform the imputation of missing price data.

The second trend focuses on high-frequency and microstructure imputation: it centers on the granular complexities of high-frequency trading (HFT) and Limit Order Book (LOB) data. Standard daily-level imputers often fail at the microstructural level; thus, \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{bamford2023} }\hskip0pt%DIFAUXCMD
and }\DIFdelend \DIFaddbegin \DIFadd{Bamford et al.~\mbox{%DIFAUXCMD
\citep{bamford2023} }\hskip0pt%DIFAUXCMD
and Shankar et al.~}\DIFaddend \citep{shankar2025} have proposed specialized deep learning architectures and benchmark suites that strictly respect LOB queue dynamics and volume imbalances. To bridge the gap between generative fidelity and the stringent latency requirements of HFT systems, \DIFaddbegin \DIFadd{Nater and Khayati~}\DIFaddend \citep{nater2025} introduce scalable probabilistic imputation techniques that bypass the computational overhead of iterative diffusion without sacrificing distributional shape.

The third trend concerns regime-awareness and stress-testing protocols: the evaluation of imputation models has matured to account for market non-stationarity. Recognizing that structural breaks often coincide with missing data (e.g., trading halts), \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{qian2024} }\hskip0pt%DIFAUXCMD
and }\DIFdelend \DIFaddbegin \DIFadd{Qian et al.~\mbox{%DIFAUXCMD
\citep{qian2024} }\hskip0pt%DIFAUXCMD
and Gao et al.~}\DIFaddend \citep{gao2024} introduce dynamic, regime-aware score-based models specifically designed to adapt to shifting market volatility. Crucially, a growing body of work advocates for \textit{stratified stress-testing evaluation protocols}. \DIFaddbegin \DIFadd{Wi et al.~}\DIFaddend \citep{wi2023} emphasize that imputation fidelity must be validated during simulated market crashes rather than strictly during stationary periods, ensuring that imputed data maintains structural integrity when risk managers need it most. Complementing this \DIFdelbegin \DIFdel{rigorous evaluation, \mbox{%DIFAUXCMD
\citep{wang2024c} }\hskip0pt%DIFAUXCMD
formally }\DIFdelend \DIFaddbegin \DIFadd{evaluation, Wang et al.~\mbox{%DIFAUXCMD
\citep{wang2024c} }\hskip0pt%DIFAUXCMD
}\DIFaddend investigate the pervasive issue of look-ahead bias in deep learning imputers, establishing strict cross-validation guidelines to ensure that reconstructed historical sequences do not inadvertently leak future market information into backtesting pipelines.

\subsection{\DIFdelbegin \DIFdel{Summarization}\DIFdelend \DIFaddbegin \DIFadd{Imputation: Research Synthesis}\DIFaddend }

The landscape of financial missing data recovery is defined by a rigorous trade-off between computational efficiency and generative fidelity, as synthesized in Table~\ref{tab:imputation_comparison}. The empirical case for diffusion-based imputation is, however, less settled than the tabular taxonomy above might suggest. The most-cited diffusion imputer, CSDI~\citep{tashiro2021csdi}, reports a 40--65\% improvement over probabilistic baselines and a 5--20\% improvement over deterministic baselines---but on \emph{healthcare and environmental} datasets, not financial. Direct head-to-head benchmarks that put diffusion imputers (CSDI, TimeDiT, LSCD) on the same financial dataset as GAN imputers (GAIN, AttnWGAIN, ImputeGAN, CWGAIN-GP) are not standard in the literature, and the few within-paper comparisons that exist (e.g.\ Score-CDM vs GAIN on air-quality or electricity data) do not transfer cleanly to the financial regime of contiguous missingness and regime-switching volatility. The honest reading is therefore: diffusion models are the most promising current direction for high-fidelity imputation under contiguous missingness, but the claim that they ``stand unrivaled'' for financial imputation specifically rests on architectural plausibility and on transferred evidence rather than on a published financial benchmark. Tensor factorization and tree-based methods remain preferable when the constraint is large-scale historical-database throughput rather than peak reconstruction quality.
 \DIFdelbegin %DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \subsection{\DIFdelbegin \DIFdel{Financial Time-Series }\DIFdelend Synthesis\DIFaddbegin \DIFadd{: Problem Formulation and Characteristics}\DIFaddend }
\label{sec:synthesis}

\DIFdelbegin \subsection{\DIFdel{Problem Formulation and Characteristics}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend Financial Time-Series Synthesis (FTSS) refers to the systematic, algorithmic generation of statistically credible and temporally coherent synthetic sequences. The overarching objective is to enlarge or enrich the empirical sample space upon which downstream quantitative models and trading agents are trained. Unlike extrapolation (which predicts the continuation of a partial series) or imputation (which mathematically restores broken or missing intervals), FTSS is designed to create entirely new, unobserved data manifolds from scratch. Formally, the goal is to learn a generative model $p_\theta(\mathbf{X})$ that closely approximates the true multidimensional data-generating distribution $p_{data}(\mathbf{X})$, enabling unconditional or condition-guided sampling of synthetic market trajectories $\hat{\mathbf{X}}$.

The primary functions of FTSS include restoring population heterogeneity, rebalancing extremely heavy-tailed historical episodes to correct dataset skewness, and providing controlled, counterfactual market regimes for rigorous stress testing. As shown in Figure \ref{fig:ftss_comparison}, methodologically, the literature can be categorized across two distinct scales: \textit{micro-level synthesis}, which focuses on generating univariate or low-dimensional asset prices and return sequences, and \textit{macro-level market simulation}, which attempts to reproduce complex, asynchronous market microstructures\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{ohara1995market,gould2013limit} }\hskip0pt%DIFAUXCMD
}\DIFaddend such as order flows and full \DIFdelbegin \DIFdel{Limit Order Books }\DIFdelend \DIFaddbegin \DIFadd{limit order books }\DIFaddend (LOB).

\subsection{\DIFdelbegin \DIFdel{Micro-Level }\DIFdelend Synthesis: \DIFaddbegin \DIFadd{Micro-Level }\DIFaddend Asset Returns and Pricing Dynamics}

Micro-level synthesis targets the generation of asset price trajectories and return sequences, serving primarily as a sophisticated data augmentation mechanism. Early applications of \DIFdelbegin \DIFdel{standard Generative Adversarial Networks }\DIFdelend \DIFaddbegin \DIFadd{generative adversarial networks }\DIFaddend (GANs)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan} }\hskip0pt%DIFAUXCMD
}\DIFaddend in this domain frequently suffered from severe training instability and mode collapse, largely due to the \DIFdelbegin \DIFdel{unique "stylized facts" of financial data}\DIFdelend \DIFaddbegin \DIFadd{financial stylized facts~\mbox{%DIFAUXCMD
\citep{cont2001empirical,mandelbrot1963variation}}\hskip0pt%DIFAUXCMD
}\DIFaddend , such as pronounced volatility clustering and non-Gaussian heavy tails.

To overcome these barriers, the field rapidly transitioned to Wasserstein GANs (\DIFdelbegin \DIFdel{WGAN)\mbox{%DIFAUXCMD
\citep{naritomi2020data}}\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{WGANs)~\mbox{%DIFAUXCMD
\citep{arjovsky2017wgan,naritomi2020data}}\hskip0pt%DIFAUXCMD
}\DIFaddend , which replace the structurally unstable Jensen-Shannon divergence with the Wasserstein-1 (Earth Mover's) distance. The optimization objective focuses on minimizing:
\begin{equation}
W_1(\mathbb{P}_r, \mathbb{P}_g) = \inf_{\gamma \in \Pi(\mathbb{P}_r, \mathbb{P}_g)} \mathbb{E}_{(\mathbf{x}, \tilde{\mathbf{x}}) \sim \gamma}[\|\mathbf{x} - \tilde{\mathbf{x}}\|]
\end{equation}
where $\Pi(\mathbb{P}_r, \mathbb{P}_g)$ denotes the set of all joint distributions mapping the real distribution $\mathbb{P}_r$ to the generated distribution $\mathbb{P}_g$. The WGAN family was extended for finance in several methodologically distinct directions, which we now unpack critically.

QuantGAN \citep{wiese2020} established the WGAN+\DIFdelbegin \DIFdel{TCN }\DIFdelend \DIFaddbegin \DIFadd{temporal convolutional network (TCN)~\mbox{%DIFAUXCMD
\citep{bai2018empirical} }\hskip0pt%DIFAUXCMD
}\DIFaddend template for multi-step unconditional price-path generation. Its verification protocol relies on reproducing eight classical stylized facts (leverage, volatility clustering, heavy tails, absence of autocorrelation in returns, long-memory in absolute returns, etc.) on synthetic S\&P~500 paths; \DIFdelbegin \DIFdel{subsequent work \mbox{%DIFAUXCMD
\citep{wiese2020} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{the same study }\DIFaddend reports that QuantGAN-generated trajectories pass all eight checks while the baseline convolutional GAN fails on at least three. QuantGAN is, however, restricted to \emph{univariate} return series---a limitation that every constraint-aware variant below explicitly tries to relax.

RSQGAN \citep{RSQGAN} injects \emph{regime awareness} into the generator by conditioning on a latent regime variable (typically inferred via a hidden Markov model\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{rabiner1989tutorial,hamilton1989new} }\hskip0pt%DIFAUXCMD
}\DIFaddend over historical returns). The reported value proposition is distributional alignment \emph{across} regimes rather than within a single regime: paths sampled under the bull regime should resemble bull-period historical paths, and likewise for bear and transition periods. The conditioning mechanism is comparatively cheap (a single discrete regime index appended to the noise input) and is the only one of the four that does not require a custom loss function.

DAT-CGAN \citep{DAT-CGAN} reformulates the GAN objective around \emph{decision-related} quantities rather than marginal distributions. Concretely, the discriminator is trained against a multi-Wasserstein loss evaluated on portfolio-level statistics (e.g.\ cumulative PnL, Sharpe \DIFaddbegin \DIFadd{ratio~\mbox{%DIFAUXCMD
\citep{sharpe1994sharpe}}\hskip0pt%DIFAUXCMD
}\DIFaddend , drawdown) computed on rolling windows of the generated path. The authors demonstrate on a multi-time-step portfolio-choice task that DAT-CGAN improves generative quality on both the underlying data and on decision-related quantities relative to strong GAN baselines. The cost is a loss function that is harder to optimize: training stability degrades when the decision-statistic set grows.

Jinkou \citep{Jinkou} imposes \emph{state-variable constraints} that keep generated trajectories consistent with economically meaningful drivers (e.g.\ macro factors, implied volatility surfaces). Unlike the other three, Jinkou is positioned for scenario generation rather than return-path generation, which makes it a natural bridge from micro-level synthesis to macro-level market simulation.

TAIL-GAN \citep{cont2025tailgan} stands apart from the other three because its training criterion is not a divergence over the data distribution at all but a \emph{risk-functional score} based on the joint elicitability of Value-at-Risk (VaR) and Expected Shortfall (ES)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{acerbi2002coherence,acerbi2014expected}}\hskip0pt%DIFAUXCMD
}\DIFaddend . The discriminator is optimized against the Acerbi--Szekely scoring function\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{acerbi2014expected}}\hskip0pt%DIFAUXCMD
}\DIFaddend , which is the standard backtesting score for VaR/ES; the generator therefore receives gradient signal dominated by tail outcomes rather than by typical samples. This matters because, as \DIFaddbegin \DIFadd{Cont et al.~}\DIFaddend \citep{cont2025tailgan} argue explicitly, Wasserstein- or cross-entropy-based losses are sample-dominated and structurally fail to capture tail events---which is exactly the regime regulators care about. Their empirical evaluation compares Tail-GAN against a Historical Simulation baseline and a plain WGAN on a five-name universe (AAPL, AMZN, GOOG, JPM, QQQ) for the period 2019-11 to 2019-12. On out-of-sample VaR/ES estimation for static buy-and-hold, mean-reversion, and trend-following strategies, the reported OOS relative errors are: Historical Simulation $\sim$10.4\%, WGAN $\sim$26.9\%, Tail-GAN $\sim$10.1\%---i.e.\ Tail-GAN closes most of the gap to the oracle, while a vanilla WGAN does roughly 2.5$\times$ worse. On the more demanding score-based test of distributional fidelity under dynamic strategies, Tail-GAN achieves a 21.3\% rejection rate versus WGAN's 11.4\% and historical simulation's 0\%---the higher rejection rate being preferable, since it indicates the synthetic distribution is distinguishable from the test set in the way real markets are. Two limitations temper the result: (i) the headline numbers are on a one-month training window with five names, so scalability to a full S\&P~500 universe required eigenportfolio-based dimensionality reduction, with Tail-GAN(Eig) at 25.6\% OOS error versus Historical Simulation at 25.9\% (a much smaller gap); (ii) Tail-GAN does \emph{not} target stylized-fact reproduction---its evaluation is risk-functional only, so it is complementary to rather than substitutable for QuantGAN/RSQGAN.

Despite the improvements brought by WGANs, score-based \DIFdelbegin \DIFdel{Diffusion models}\DIFdelend \DIFaddbegin \DIFadd{diffusion models~\mbox{%DIFAUXCMD
\citep{song2019generative,song2021score} }\hskip0pt%DIFAUXCMD
}\DIFaddend have been increasingly applied to multi-asset return synthesis with the explicit goal of preserving cross-sectional correlation matrices. The empirical case for this claim is, however, mostly \emph{within-paper} rather than cross-paper: CoFinDiff \citep{tanaka2025cofindiff}, Fin-DDPM \citep{takahashi2024generation}, and GBM-Diff \citep{kim2025gbm} each report better cross-correlation preservation against their own GAN baselines on their own chosen datasets, but a shared benchmark that pits all three against the same WGAN/QuantGAN family on the same multi-asset universe is not currently standard practice. The forward diffusion process systematically corrupts real financial data $\mathbf{x}_0$ over $T$ steps by adding Gaussian noise:
\begin{equation}
q(\mathbf{x}_t | \mathbf{x}_{t-1}) = \mathcal{N}(\mathbf{x}_t; \sqrt{1 - \beta_t}\mathbf{x}_{t-1}, \beta_t \mathbf{I})
\end{equation}
where $\beta_t$ dictates the variance schedule. A parameterized neural network $\theta$ then learns the reverse denoising process:
\begin{equation}
p_\theta(\mathbf{x}_{t-1} | \mathbf{x}_t) = \mathcal{N}(\mathbf{x}_{t-1}; \boldsymbol{\mu}_\theta(\mathbf{x}_t, t), \Sigma_\theta(\mathbf{x}_t, t))
\end{equation}
\DIFdelbegin \DIFdel{This rigorous score-matching technique allows models like }\DIFdelend \DIFaddbegin \DIFadd{Score-matching techniques allow models such as }\DIFaddend Fin-DDPM and CoFinDiff \citep{takahashi2024generation, tanaka2025cofindiff} to \DIFdelbegin \DIFdel{achieve stronger distributional stability than standard GAN-based generators while reducing the risk of mode collapse. To ensure that the generated noise mathematically respects financial reality}\DIFdelend \DIFaddbegin \DIFadd{seek improved distributional stability relative to selected GAN baselines, although comparisons remain largely within-paper. To impose domain-consistent dynamics}\DIFaddend , models such as GBM-Diff and Fin-Denoiser \citep{kim2025gbm, wang2024denoiser} structurally incorporate \DIFdelbegin \DIFdel{Geometric Brownian Motion }\DIFdelend \DIFaddbegin \DIFadd{geometric Brownian motion }\DIFaddend (GBM) priors\DIFdelbegin \DIFdel{directly into the }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{black1973pricing} }\hskip0pt%DIFAUXCMD
into }\DIFaddend diffusion stochastic differential equations (SDEs) \DIFdelbegin \DIFdel{, guaranteeing financially sound baseline sequences}\DIFdelend \DIFaddbegin \DIFadd{to constrain baseline dynamics, although these priors do not by themselves guarantee financial validity}\DIFaddend .

\subsection{\DIFaddbegin \DIFadd{Synthesis: }\DIFaddend Macro-Level Market Simulation \DIFdelbegin \DIFdel{: Microstructure }\DIFdelend and Order Flows}

Macro-level simulation elevates the generative objective from simple price paths to the reproduction of complex market microstructures and multi-agent interactive environments. By generating realistic Limit Order Books (LOB) and order flows, these models facilitate the high-fidelity validation of execution strategies without exposing institutions to real capital risk.

Macro-level market simulation has progressed through three architectural generations, and a careful reading of the empirical evidence suggests that the strongest claims about the diffusion/LMM generation need to be qualified.

The first generation, conditional GANs for correlated price paths, was aimed at reproducing the cross-asset correlation structure that vanilla WGANs miss. \DIFaddbegin \DIFadd{Masi et al.~}\DIFaddend \citep{CoMeTS-GAN} explicitly model correlated stock-market time series by injecting a learnable correlation block into the generator, evaluated on the LOBSTER limit-order-book dataset. \DIFaddbegin \DIFadd{Istiaque et al.~}\DIFaddend \citep{istiaque2024cts} extend this idea to \emph{conditional} simulation, conditioning on macro state variables so that the generated regime matches a user-supplied label. \DIFaddbegin \DIFadd{Xia et al.~}\DIFaddend \citep{xia2024market} (Market-GAN) go further with a two-stage pipeline in which the first stage generates a market state and the second stage generates price paths conditioned on it. These three papers share a common limitation: their empirical evaluation is mostly on stylized-fact reproduction, and the downstream utility (i.e.\ ``does the synthetic data actually improve trading agents trained on it?'') is rarely reported. \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{GAN-MoEx} }\hskip0pt%DIFAUXCMD
reports }\DIFdelend \DIFaddbegin \DIFadd{Kazadaev et al.~\mbox{%DIFAUXCMD
\citep{GAN-MoEx} }\hskip0pt%DIFAUXCMD
report }\DIFaddend a momentum-effect-specific benchmark on Moscow Stock Exchange data and \DIFdelbegin \DIFdel{shows }\DIFdelend \DIFaddbegin \DIFadd{show that }\DIFaddend GAN-synthetic trajectories reproduce the observed momentum premium; this is one of the few macro-simulation papers with an economic downstream task as the headline metric.

The second generation emerged from macroeconomic conditioning. \DIFaddbegin \DIFadd{Alex et al.~}\DIFaddend \citep{alex2024macroeconomic} (MacroSynth) directly condition on macroeconomic factor panels (CPI, rates, credit spreads) so that generated market scenarios are internally consistent with a specified macro state. The reported evaluation is downstream mean-variance and risk-parity allocations computed on synthetic samples, with the synthetic allocations showing sign-consistent risk premia relative to history. The methodological advance here is that the conditioning signal is itself an economic primitive, not a latent regime inferred from the data---a useful design choice for stress-testing use cases where the user specifies the macro scenario in advance.

The third generation, diffusion and large market models, is the most recent wave of macro-simulation work and has moved to diffusion and discrete-token foundation models. \DIFaddbegin \DIFadd{Berti et al.~}\DIFaddend \citep{berti2025trades} propose TRADES, a transformer-based denoising diffusion engine conditioned on market state for LOB order-flow simulation. The headline empirical claim is a 3.27- and 3.48-point improvement on the predictive score (MAE between real-data-trained and synthetic-data-trained price-prediction models) over the prior state of the art on two stocks. The authors themselves note that ``there is a notable absence of quantitative metrics for evaluating generative market simulation models in the literature''---the field has been inventing its own evaluation protocols, which makes between-paper comparison fragile. \DIFaddbegin \DIFadd{Huang et al.~}\DIFaddend \citep{huang2024diga} (DiGA / DigMA) split the problem into two stages: a conditional diffusion model over time-evolving distribution parameters of mid-price return rate and order arrival rate, and a meta-agent with financial economic priors that samples orders from the learned distributions. This decomposition sidesteps the difficulty of directly diffusing over discrete order sequences. \DIFaddbegin \DIFadd{Li et al.~}\DIFaddend \citep{li2025mars} (MarS) push the boundary furthest by training a Large Market Model (LMM)---a transformer foundation model over discrete order tokens, analogous to a language model over text---and report order-level interactive simulation that is responsive to user-injected orders.

Two \DIFdelbegin \DIFdel{critical caveats apply to the Generation-3 claims}\DIFdelend \DIFaddbegin \DIFadd{important caveats qualify the evidence for this third generation of diffusion and large-market-model approaches}\DIFaddend . First, all three diffusion/LMM papers report improvements over a small set of in-paper baselines, not over a shared benchmark, so the apparent dominance of diffusion over GANs in macro simulation is a within-paper claim rather than a cross-paper empirical fact. Second, the diffusion and LMM papers report evaluation on at most two stocks (TRADES) or single datasets (DiGA, MarS); whether the reported fidelity survives a regime change (e.g.\ 2008 crisis, 2020 COVID) is not established. The survey's earlier claim that ``macro-level LOB simulation and controllable text-to-market generation are still in their infancy'' is therefore better supported than the implicit ``diffusion vastly outperforms GANs'' reading: the field has demonstrably advanced methodologically (transformer denoising, LMM-style discrete tokenization, meta-agent decomposition), but the empirical case for production deployment remains open. We flag this as a measurement gap the field would benefit from closing.

\subsection{\DIFaddbegin \DIFadd{Synthesis: }\DIFaddend Privacy-Preserving and Regulation-Aware Generation}

The financial industry is heavily bottlenecked by strict data-sharing regulations (e.g., GDPR, CCPA). Synthetic data generation provides a mathematically viable pathway to share proprietary datasets across institutional boundaries without violating compliance standards. Models such as NVF-DPGAN \citep{NVF-DPGAN} directly incorporate \DIFdelbegin \DIFdel{Differential Privacy }\DIFdelend \DIFaddbegin \DIFadd{differential privacy }\DIFaddend (DP) mechanisms\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{dwork2006differential,dwork2014algorithmic} }\hskip0pt%DIFAUXCMD
}\DIFaddend into the generative training process. By clipping gradients and injecting calibrated noise during \DIFdelbegin \DIFdel{the discriminator's }\DIFdelend optimization, the framework \DIFdelbegin \DIFdel{mathematically guarantees that the generated synthetic sequences cannot be reverse-engineered to leak sensitive information regarding individual retail trading accounts or institutional proprietary alpha }\DIFdelend \DIFaddbegin \DIFadd{provides a formal $(\epsilon,\delta)$-DP bound on membership inference~\mbox{%DIFAUXCMD
\citep{shokri2017membership}}\hskip0pt%DIFAUXCMD
; this bound limits, but does not eliminate, inference about individual records or proprietary }\DIFaddend signals. The ongoing central research focus in this sub-domain is optimizing the delicate trade-off between strict privacy bounds ($\epsilon$) and the downstream predictive utility of the resulting synthetic series.

% \subsection{Advanced Conditional Generation and Emerging Trends}
\subsection{\DIFaddbegin \DIFadd{Synthesis: }\DIFaddend Emerging Trends}

Recent literature reflects a rapid maturation of financial synthesis, particularly transitioning away from unconditional generation toward highly controllable, context-aware frameworks. Rather than treating synthesis as a purely statistical exercise, recent models simultaneously condition on diverse metadata and partially observed signals to dictate the semantic direction of the synthetic output \citep{gholamrezaei2023conditioning}.

For instance, \DIFaddbegin \DIFadd{Lozano et al.~}\DIFaddend \citep{lozano2023dual} propose advanced dual-sourced diffusion networks that align text-based market sentiment directly with numerical trajectory generation, ensuring exceptionally high temporal coherence across modalities. Parallel to improvements in fidelity, there is a critical push to reduce the computational burden of generative sampling. \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{tao2024} }\hskip0pt%DIFAUXCMD
introduce novel distillation techniques}\DIFdelend \DIFaddbegin \DIFadd{Tao et al.~\mbox{%DIFAUXCMD
\citep{tao2024} }\hskip0pt%DIFAUXCMD
introduce distillation techniques~\mbox{%DIFAUXCMD
\citep{hinton2015distilling} }\hskip0pt%DIFAUXCMD
}\DIFaddend specifically designed to accelerate the generation process, a critical requirement for deploying synthetic simulators in live, high-frequency trading environments. Finally, \DIFaddbegin \DIFadd{Oppel et al.~}\DIFaddend \citep{oppel2025} demonstrate how these advanced conditional generators are practically utilized, embedding them into dynamic stress-testing pipelines to evaluate institutional portfolio resilience under highly specific, synthetically engineered counterfactual crises.

\subsection{\DIFdelbegin \DIFdel{Summarization}\DIFdelend \DIFaddbegin \DIFadd{Synthesis: Research Synthesis}\DIFaddend }

Data synthesis currently represents the most dynamic and rapidly evolving branch of Financial Time-Series Generation (FTSG). As summarized in Table \ref{tab:synthesis_comparison}, the field relies on a diverse array of generative architectures, each serving distinct institutional needs. While micro-level return generation has \DIFdelbegin \DIFdel{reached a high level of mathematical maturity}\DIFdelend \DIFaddbegin \DIFadd{become comparatively well developed}\DIFaddend —reliably capturing volatility clustering and cross-asset correlations via diffusion models—macro-level LOB simulation and controllable text-to-market generation are still in their infancy. Moving forward, the transition towards computationally efficient, privacy-preserving, and condition-specific market simulators \DIFdelbegin \DIFdel{will significantly democratize }\DIFdelend \DIFaddbegin \DIFadd{can lower barriers to }\DIFaddend algorithmic trading research and enhance the capability of financial institutions to conduct rigorous, counterfactual stress testing.
 \subsection{Applications\DIFaddbegin \DIFadd{: Extrapolation and the Application Landscape}\DIFaddend }
\label{sec:applications}
% ---------------------------------------------------------------------

This section zooms out to the downstream use-cases that actually justify an FTSG
model in a quantitative-finance workflow. We organize the discussion
along the task axis (FTSE, FTSI, FTSS) and end with a cross-cutting
paragraph on shared infrastructure (RL training gyms, risk-management
sandboxes, and synthetic-data sharing). Throughout, we restrict
ourselves to peer-reviewed or peer-reproducible works whose authors
explicitly evaluate against a real downstream application rather than
only on statistical fidelity.

% --- 5.3.1 Applications of FTSE -------------------------------------
\DIFdelbegin \subsection{\DIFdel{Applications of FTSE}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend 

FTSE is the most mature sub-task in deployment. The classical use-case
is to feed extrapolated point or quantile forecasts into a downstream
decision module, and the strongest evidence that this works comes from
the deep-reinforcement-learning (DRL) literature.
FinRL~\citep{liu2020finrl} and its successor
FinRL-Meta~\citep{liu2022finrlmeta} treat pretrained time-series
forecasters (LSTM, Transformer, or more recently LLM-based adapters) as
the observation pipeline for PPO/A2C/SAC trading agents\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{schulman2017proximal,mnih2016asynchronous,haarnoja2018soft} }\hskip0pt%DIFAUXCMD
}\DIFaddend trained on
Dow-30 and S\&P-500 constituents; their empirical study reports that
agents whose observation encoders are first fine-tuned on FTSE targets
achieve higher Sharpe \DIFaddbegin \DIFadd{ratios~\mbox{%DIFAUXCMD
\citep{sharpe1994sharpe} }\hskip0pt%DIFAUXCMD
}\DIFaddend and lower maximum drawdown than agents trained on
raw OHLCV. Hierarchical extensions such as
HRPM~\citep{wang2020hrpm} couple a portfolio-management FTSE head with
a separate order-execution head, explicitly accounting for price
slippage when large extrapolation errors are amplified by capital
allocation. \DIFdelbegin \DIFdel{More recently, LLM-driven forecasters
(}\DIFdelend Time-LLM~\citep{jin2024timellm} \DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{and }\DIFaddend CALF~\citep{liu2025calf} \DIFdelbegin \DIFdel{) have been
evaluated as the predictor component inside FinRL-style pipelines, with consistent gains on A-share and crypto benchmarks}\DIFdelend \DIFaddbegin \DIFadd{are representative financial forecasters with adapter architectures related to these workflows; direct, standardized FinRL comparisons remain limited}\DIFaddend .

A second, often-overlooked use of FTSE is volatility and risk
forecasting. TAIL-GAN~\citep{cont2025tailgan} is explicitly designed to
satisfy the joint elicitability of Value-at-Risk and Expected Shortfall,
and its authors plug the tail-respecting generator into a VaR back-test
to \DIFdelbegin \DIFdel{demonstrate regulatory-grade coverage }\DIFdelend \DIFaddbegin \DIFadd{examine coverage under VaR/ES back-testing criteria~\mbox{%DIFAUXCMD
\citep{acerbi2014expected}}\hskip0pt%DIFAUXCMD
}\DIFaddend . On the option-pricing side,
deep generative models of the underlying (e.g., quant GAN variants) have
been used to produce Monte-Carlo paths\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{boyle1977options} }\hskip0pt%DIFAUXCMD
}\DIFaddend that replace historical
resampling for pricing exotic payoffs, where the FTSE tail behavior
dominates pricing error. FTSE outputs also feed
stochastic-portfolio-optimization engines: Hybrid CGAN for Portfolio
Analysis~\citep{lu2022hybrid} trains a conditional GAN whose label is
the Markowitz risk-aversion level\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{markowitz1952portfolio}}\hskip0pt%DIFAUXCMD
}\DIFaddend , and the resulting synthetic future
paths are scored by an optimizer that selects weights without committing
to a single forecast.

% --- 5.3.2 Applications of FTSI -------------------------------------
\subsection{Applications\DIFdelbegin \DIFdel{of FTSI}\DIFdelend \DIFaddbegin \DIFadd{: Imputation}\DIFaddend }

FTSI is most often deployed in production at the data-engineering
layer, where its outputs feed classical pipelines rather than a learning
agent. \DIFdelbegin \DIFdel{GAIN}\DIFdelend \DIFaddbegin \DIFadd{Yoon et al.}\DIFaddend ~\citep{yoon2018gain} introduced \DIFaddbegin \DIFadd{GAIN, }\DIFaddend the generative-adversarial
imputation paradigm\DIFdelbegin \DIFdel{and }\DIFdelend \DIFaddbegin \DIFadd{, and it }\DIFaddend remains a baseline in many open finance
datasets for handling missing quotes during off-hours, ticker
delistings, and exchange halts. Subsequent work has tightened the fit
between GAN imputation and financial time structure:
ImputeGAN~\citep{qin2023imputegan} and
AttnWGAIN~\citep{wang2025attn} replace generic CNN backbones with
Informer and diagonal-masked attention respectively, both reporting
smaller distributional drift on the K-line dataset than linear \DIFaddbegin \DIFadd{interpolation }\DIFaddend or MICE
 baselines\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{vanbuuren1999multiple}}\hskip0pt%DIFAUXCMD
}\DIFaddend . Beyond price reconstruction, missing-data scenarios in
finance also arise at the macro layer.
FCI~\citep{herculano2025financial} uses Bayesian factor imputation to
keep Financial Condition Indices computable during incomplete-reporting
periods (e.g., during COVID-era reporting lags), demonstrating that
imputed macro factors can materially change the sign of the indicator
relative to linear interpolation.

A second family of applications concerns downstream classifier
performance on financial tabular data. For instance, \DIFdelbegin \DIFdel{evaluations of
}\DIFdelend \DIFaddbegin \DIFadd{Emaan~\mbox{%DIFAUXCMD
\citep{emaan2025improving} }\hskip0pt%DIFAUXCMD
reports
that }\DIFaddend Transformer-enhanced \DIFdelbegin \DIFdel{GAN oversampling~\mbox{%DIFAUXCMD
\citep{emaan2025improving} }\hskip0pt%DIFAUXCMD
report
that }\DIFdelend \DIFaddbegin \DIFadd{GAN oversampling for }\DIFaddend synthesizing minority fraud transactions yields substantial gains
in detection recall compared to heuristic \DIFdelbegin \DIFdel{interpolation baselines }\DIFdelend \DIFaddbegin \DIFadd{oversampling methods }\DIFaddend such as
SMOTE\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{chawla2002smote} }\hskip0pt%DIFAUXCMD
}\DIFaddend on standard credit-card transaction benchmarks;
similarly, in credit-scoring ensembles~\citep{liu2021classification},
GAN-augmented minority classes materially reduce false-negative rates.
These studies reinforce that, in the financial setting, generative
imputation and augmentation are rarely the final product; rather, they
serve as value-adding pre-processing steps whose utility is judged by
downstream predictive metrics.

% --- 5.3.3 Applications of FTSS -------------------------------------
\subsection{Applications\DIFdelbegin \DIFdel{of FTSS}\DIFdelend \DIFaddbegin \DIFadd{: Synthesis}\DIFaddend }

FTSS is the sub-task with the broadest and most consequential deployment
surface. We organize it along the natural lifecycle of a quantitative
strategy.

\DIFdelbegin \DIFdel{\revadd{In strategy backtesting and replay,} }\DIFdelend ABIDES~\citep{byrd2019abides} and its Gym-compatible successor
ABIDES-Gym~\citep{amrouni2021abides} are agent-based discrete-event
simulators that wrap a synthetic limit-order-book generator in an
OpenAI Gym interface\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{brockman2016openai}}\hskip0pt%DIFAUXCMD
}\DIFaddend , enabling head-to-head backtests of RL agents that
interact with thousands of background traders.
\DIFaddbegin \DIFadd{Balch et al.~}\DIFaddend \citep{balch2019how} compare single-agent market replay against
multi-agent interactive simulation and find that the interactive mode
systematically reorders the leaderboard of public trading algorithms
--- a striking demonstration that the choice of an FTSS simulation backbone can flip
strategy rankings. \DIFaddbegin \DIFadd{Rollins and Cliff~}\DIFaddend \citep{rollins2020which} extend this with a threaded
parallel simulator, scaling ABIDES-style order generation to high agent
counts, and reproduce qualitatively different ``pecking orders'' of
strategies compared with single-agent replay.

\DIFdelbegin \DIFdel{\revadd{For stress testing, scenario generation and risk management,} }\DIFdelend GAN- and diffusion-based synthesizers are increasingly used to
manufacture counterfactual \DIFdelbegin \DIFdel{or Black-Swan }\DIFdelend \DIFaddbegin \DIFadd{tail }\DIFaddend scenarios that historical
samples cannot supply. TAIL-GAN~\citep{cont2025tailgan} produces tail
scenarios that satisfy joint VaR/ES elicitability and are consumed by
bank-level stress-testing frameworks; the authors explicitly demonstrate
that the synthesized tails \DIFdelbegin \DIFdel{pass the Basel }\DIFdelend \DIFaddbegin \DIFadd{perform well in VaR/ES }\DIFaddend back-tests where naive
\DIFdelbegin \DIFdel{block-bootstrap fails}\DIFdelend \DIFaddbegin \DIFadd{block bootstrap~\mbox{%DIFAUXCMD
\citep{politis1994stationary} }\hskip0pt%DIFAUXCMD
performs less reliably}\DIFaddend . Hybrid CGAN for Portfolio
Analysis~\citep{lu2022hybrid} generates market scenarios conditioned on
risk aversion, allowing a portfolio engine \DIFaddbegin \DIFadd{grounded in mean--variance optimization~\mbox{%DIFAUXCMD
\citep{markowitz1952portfolio} }\hskip0pt%DIFAUXCMD
}\DIFaddend to be re-evaluated against
thousands of hypothetical regimes in seconds. On the macro-finance
side, MacroSynth~\citep{alex2024macroeconomic} conditions on
macroeconomic factors to produce co-occurring crash and bull-run
scenarios, and the authors show that downstream mean-\DIFaddbegin \DIFadd{-}\DIFaddend variance and
risk-parity allocations\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{maillard2010properties} }\hskip0pt%DIFAUXCMD
}\DIFaddend computed on the synthetic samples exhibit
sign-consistent risk premia relative to the historical record.
Diffusion-based generators are entering the same niche:
CoFinDiff~\citep{tanaka2025cofindiff} is evaluated as a scenario engine
for a portfolio whose weights are re-optimized on each generated
window, and Fin-Denoiser~\citep{wang2024denoiser} supplies high-SNR
scenarios for risk models that are \DIFdelbegin \DIFdel{notoriously }\DIFdelend sensitive to data
artifacts.

\DIFdelbegin \DIFdel{\revadd{As trading-agent training environments,} }\DIFdelend perhaps the most visible deployment of FTSS is as a training environment for RL
trading agents. FinRL-Meta~\citep{liu2022finrlmeta} ships near-real
market environments built from Yahoo Finance and CCXT feeds and
demonstrates that DRL agents pre-trained on these synthetic-augmented
environments generalize better to the held-out test year than agents
trained only on the historical window --- a result that is reproduced
across DOW-30, S\&P-500 and crypto-asset universes.
MarS~\citep{li2025mars} extends this idea to the order level by
generating asynchronous event streams, allowing agents to be evaluated
on metrics such as realized spread and adverse-selection cost\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{glosten1985bid,huang1996dealer} }\hskip0pt%DIFAUXCMD
}\DIFaddend that are invisible to bar-level environments. Robust Risk-Sensitive RL
Agents~\citep{gao2021robust} stress-test the resulting agents by
perturbing the synthetic environment, showing that risk-aware training
reduces performance variance by an order of magnitude across random
seeds. Collectively, these works establish FTSS as the substrate for
``sim-to-real'' transfer in financial RL.

\DIFdelbegin \DIFdel{\revadd{In privacy-preserving data sharing and regulatory scenarios,} }\DIFdelend \DIFaddbegin \DIFadd{In privacy-preserving data sharing and regulatory scenarios, }\DIFaddend synthetic financial data is a recognized route to overcome the friction
between data sharing and the privacy constraints imposed by GDPR, CCPA,
and the equivalent Chinese PIPL. \DIFaddbegin \DIFadd{Balch et al.~\mbox{%DIFAUXCMD
\citep{balch2024six} }\hskip0pt%DIFAUXCMD
propose the }\DIFaddend Six Levels of
Privacy \DIFdelbegin \DIFdel{~\mbox{%DIFAUXCMD
\citep{balch2024six} }\hskip0pt%DIFAUXCMD
proposes a six-tier framework and demonstrates }\DIFdelend \DIFaddbegin \DIFadd{framework and demonstrate }\DIFaddend empirically that synthetic equities, options and LOB
samples generated by TimeGAN, QuantGAN, and diffusion variants preserve
aggregate utility (Sharpe ratios, correlation matrices, tail-event
counts) while reducing \DIFdelbegin \DIFdel{membership-inference vulnerability to }\DIFdelend \DIFaddbegin \DIFadd{vulnerability to membership-inference attacks~\mbox{%DIFAUXCMD
\citep{shokri2017membership} }\hskip0pt%DIFAUXCMD
to
}\DIFaddend near-baseline. \DIFdelbegin \DIFdel{Privacy Measurement in Tabular Synthetic
Data~\mbox{%DIFAUXCMD
\citep{boudewijn2023privacy} }\hskip0pt%DIFAUXCMD
formalizes }\DIFdelend \DIFaddbegin \DIFadd{Boudewijn et al.~\mbox{%DIFAUXCMD
\citep{boudewijn2023privacy} }\hskip0pt%DIFAUXCMD
formalize }\DIFaddend the corresponding
evaluation protocol that quantifies the privacy/utility frontier. On
the regulatory side, \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{balch2024six} }\hskip0pt%DIFAUXCMD
}\DIFdelend \DIFaddbegin \DIFadd{Balch et al.~\mbox{%DIFAUXCMD
\citep{balch2024six} }\hskip0pt%DIFAUXCMD
further }\DIFaddend argue that FTSS pipelines can
produce synthetic benchmark datasets for third-party model validation
without exposing proprietary positions, a direction that has begun to
attract policy and supervisory attention in public-sector pilot studies.
This shift reframes FTSG not only as a research artifact but as
compliance infrastructure.

% --- 5.3.4 Cross-cutting ---------------------------------------------
\subsection{Cross-cutting \DIFdelbegin \DIFdel{Use}\DIFdelend \DIFaddbegin \DIFadd{Applications}\DIFaddend : Synthetic Datasets as Benchmarks}

Beyond the per-task applications above, FTSG has produced a small but
rapidly maturing ecosystem of benchmark datasets that are themselves
reusable scientific infrastructure. TSGBench~\citep{ang2023tsgbench}
provides paired real/synthetic time-series collections for GOOGL and FX
series and is consumed by downstream papers as a standardized fidelity
benchmark. CTBench~\citep{ang2025ctbench} extends this to Binance
cryptocurrency markets, where extreme volatility makes synthetic-data
quality both harder to achieve and more consequential.
FinTSB~\citep{hu2025fintsb} and FinTSBridge~\citep{wang2025fintsbridge}
package millions of Chinese A-share, NYSE and NASDAQ trajectories
together with pre-defined train/valid/test splits, lowering the cost of
entry for new FTSG research and making FTSE/FTSI results directly
comparable. These benchmarks invert the typical deployment pipeline:
rather than synthesizing data to fit a model, the model is now fitted
to a benchmark whose provenance, statistics and license are publicly
auditable. This is a subtle but important shift for reproducibility and
regulatory acceptance.

In summary, FTSE, FTSI, and FTSS are not merely three academic
formulations of the same underlying problem. Each maps onto a
distinct, value-generating workflow in industry. FTSE plugs into
decision engines (trading agents, portfolio optimizers, risk models);
FTSI into data-engineering layers (data cleaning, augmentation for
tabular classifiers); and FTSS into simulation infrastructure (RL
training, stress testing, privacy-preserving sharing). The applications
in this section collectively motivate the breadth of design choices
catalogued in the taxonomy and task-specific generation subsections.

 \subsection{Evaluation\DIFdelbegin \DIFdel{Protocols}\DIFdelend \DIFaddbegin \DIFadd{: Why Generic Generation Metrics Are Insufficient}\DIFaddend }
\label{sec:evaluation}

\DIFdelbegin \subsection{\DIFdel{Why Generic Generation Metrics are Insufficient}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend Evaluation remains one of the most critical, yet under-standardized, aspects of financial time-series generation. In conventional supervised learning or computer vision tasks, performance is often adequately measured by generic metrics like Mean Squared Error (MSE), Fréchet Inception Distance (FID)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{heusel2017gans}}\hskip0pt%DIFAUXCMD
}\DIFaddend , or visual realism. However, FTSG involves multiple competing objectives where visual similarity or pointwise accuracy does not equate to financial validity.

A synthetic sequence might achieve a low MSE or MAE against a ground-truth trajectory, yet entirely fail to preserve crucial dynamics such as volatility clustering or tail-risk behavior, \DIFdelbegin \DIFdel{rendering it useless }\DIFdelend \DIFaddbegin \DIFadd{limiting its usefulness }\DIFaddend for stress testing. Conversely, generic metrics cannot evaluate whether generated data improves a trading agent's performance in a downstream simulation. Financial data's noise, non-stationarity, and regime-dependent nature demand that evaluation protocols go beyond generic reconstruction. In this section, we systematically organize evaluation practices into five dimensions, culminating in a unified evaluation checklist.

\subsection{\DIFdelbegin \DIFdel{Fidelity-Oriented }\DIFdelend Evaluation\DIFaddbegin \DIFadd{: Fidelity-Oriented Assessment}\DIFaddend }
A primary objective of Financial Time-Series Generation (FTSG) is to approximate the underlying multivariate data-generating distribution closely. Fidelity-oriented evaluation rigorously quantifies the statistical divergence between the synthetic sequences $\hat{\mathbf{X}}$ and the empirical observations $\mathbf{X}$. This assessment spans several mathematical dimensions:

\DIFdelbegin %DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Distributional Similarity:}} %DIFAUXCMD
\DIFdel{To evaluate }\DIFdelend \DIFaddbegin \DIFadd{Distributional similarity evaluates }\DIFaddend whether the model captures the marginal distributions of returns or prices \DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{through }\DIFaddend optimal transport and probabilistic divergences\DIFdelbegin \DIFdel{are employed}\DIFdelend . The 1-Wasserstein distance\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{villani2009optimal,arjovsky2017wgan} }\hskip0pt%DIFAUXCMD
}\DIFaddend between the empirical distribution $\mathbb{P}_r$ and the generated distribution $\mathbb{P}_g$ measures the minimum cost required to transform one distribution into the other:
    \begin{equation}
        W_1(\mathbb{P}_r, \mathbb{P}_g) = \inf_{\gamma \in \Pi(\mathbb{P}_r, \mathbb{P}_g)} \mathbb{E}_{(x,y)\sim \gamma}[\|x-y\|]
    \end{equation}
    where $\Pi(\mathbb{P}_r, \mathbb{P}_g)$ is the set of all joint distributions with marginals $\mathbb{P}_r$ and $\mathbb{P}_g$. Additionally, the \DIFdelbegin \DIFdel{Maximum Mean Discrepancy }\DIFdelend \DIFaddbegin \DIFadd{maximum mean discrepancy }\DIFaddend (MMD)\DIFdelbegin \DIFdel{is utilized to evaluate }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{gretton2012kernel} }\hskip0pt%DIFAUXCMD
evaluates }\DIFaddend distances in a \DIFdelbegin \DIFdel{Reproducing Kernel Hilbert Space }\DIFdelend \DIFaddbegin \DIFadd{reproducing kernel Hilbert space }\DIFaddend (RKHS)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{aronszajn1950theory} }\hskip0pt%DIFAUXCMD
}\DIFaddend $\mathcal{H}$ with kernel $k$:
    \begin{equation}
        \text{MMD}^2(\mathbb{P}_r, \mathbb{P}_g) = \mathbb{E}[k(x, x')] + \mathbb{E}[k(y, y')] - 2\mathbb{E}[k(x, y)]
    \end{equation}
    where $x, x' \sim \mathbb{P}_r$ and $y, y' \sim \mathbb{P}_g$.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Temporal Dependence:}} %DIFAUXCMD
\DIFdel{Financial data is }\DIFdelend \DIFaddbegin \DIFadd{For temporal dependence, financial data are }\DIFaddend fundamentally sequential. The generator must capture auto-regressive dynamics and lead-lag relationships. This is mathematically verified by comparing the Autocorrelation Function (ACF) of the generated returns $\hat{r}_t$ with the empirical returns $r_t$. For a given lag $k$, the ACF is defined as:
    \begin{equation}
        \rho_k = \frac{\text{Cov}(r_t, r_{t-k})}{\text{Var}(r_t)}
    \end{equation}
The Mean Absolute Error (MAE) between the empirical ACF vector and the synthetic ACF vector serves as a standard metric for temporal fidelity.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Cross-Sectional Dependence:}} %DIFAUXCMD
\DIFdel{In multivariate generation}\DIFdelend \DIFaddbegin \DIFadd{For cross-sectional dependence}\DIFaddend , the covariance structure among assets \DIFaddbegin \DIFadd{in multivariate generation }\DIFaddend dictates portfolio-level risk. Cross-sectional fidelity is evaluated by computing the Frobenius norm of the difference between the empirical correlation matrix $\Sigma_r$ and the synthetic correlation matrix $\Sigma_g$:
    \begin{equation}
        \mathcal{E}_{corr} = \|\Sigma_r - \Sigma_g\|_F = \sqrt{\sum_{i=1}^N \sum_{j=1}^N (\sigma_{r, ij} - \sigma_{g, ij})^2}
    \end{equation}

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Spectral and Frequency-Domain Similarity:}} %DIFAUXCMD
\DIFdel{To ensure }\DIFdelend \DIFaddbegin \DIFadd{Spectral and frequency-domain similarity checks }\DIFaddend that cyclical macroeconomic patterns and the low signal-to-noise ratio are maintained \DIFdelbegin \DIFdel{, spectral evaluation applies the Fast Fourier Transform }\DIFdelend \DIFaddbegin \DIFadd{by applying the fast Fourier transform }\DIFaddend (FFT)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{cooley1965algorithm} }\hskip0pt%DIFAUXCMD
}\DIFaddend to convert sequences into the frequency domain. The Power Spectral Density (PSD) of the synthetic data is then compared against the real data to penalize generators that produce over-smoothed, frequency-deficient signals.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Evaluation: }\DIFaddend Financial Validity \DIFdelbegin \DIFdel{Evaluation}\DIFdelend \DIFaddbegin \DIFadd{Assessment}\DIFaddend }
A distinctive \DIFdelbegin \DIFdel{, non-negotiable }\DIFdelend requirement of FTSG is that generated sequences must adhere to established financial theories and principles. High distributional similarity does not guarantee the preservation of empirical ``stylized facts''—\DIFdelbegin \DIFdel{the universal, }\DIFdelend \DIFaddbegin \DIFadd{recurrent }\DIFaddend non-linear statistical regularities observed across \DIFdelbegin \DIFdel{global financial markets. Evaluation must mathematically verify }\DIFdelend \DIFaddbegin \DIFadd{many financial markets~\mbox{%DIFAUXCMD
\citep{cont2001empirical}}\hskip0pt%DIFAUXCMD
. Evaluation should therefore test }\DIFaddend the presence of these facts:

\DIFdelbegin %DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Heavy Tails and Kurtosis:}} %DIFAUXCMD
\DIFdel{Financial returns are notoriously }\DIFdelend \DIFaddbegin \DIFadd{Heavy tails and kurtosis are assessed because financial returns are commonly }\DIFaddend non-Gaussian, exhibiting excess kurtosis and fat tails. To quantify the generator's ability to produce extreme tail events (e.g., black swan crashes), researchers measure the sample kurtosis $\kappa = \frac{\mathbb{E}[(r_t - \mu)^4]}{\sigma^4}$ and estimate the tail index $\alpha$ using the Hill estimator\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{hill1975simple}}\hskip0pt%DIFAUXCMD
}\DIFaddend . For the upper tail, ordering observations as $X_{(1)} \ge X_{(2)} \ge \dots \ge X_{(n)}$, the Hill estimator for a threshold $k$ is:
    \begin{equation}
        \hat{\alpha} = \left( \frac{1}{k} \sum_{i=1}^k \ln X_{(i)} - \ln X_{(k+1)} \right)^{-1}
    \end{equation}
The synthetic sequence must yield an $\hat{\alpha}$ closely matching that of historical data.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Volatility Clustering:}} %DIFAUXCMD
\DIFdel{A fundamental market property where }\DIFdelend \DIFaddbegin \DIFadd{Volatility clustering, documented by Mandelbrot~\mbox{%DIFAUXCMD
\citep{mandelbrot1963variation} }\hskip0pt%DIFAUXCMD
and systematized among stylized facts by Cont~\mbox{%DIFAUXCMD
\citep{cont2001empirical}}\hskip0pt%DIFAUXCMD
, is assessed as a market property in which }\DIFaddend large price variations are followed by large variations. A valid generator must replicate this slow-decaying memory in variance. This is evaluated by computing the autocorrelation of squared returns $r_t^2$ or absolute returns $|r_t|$ over extended lags, verifying that $\rho_k(|r_t|) > 0$ for large $k$.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Leverage Effect:}} %DIFAUXCMD
\DIFdel{This asymmetric phenomenon dictates that }\DIFdelend \DIFaddbegin \DIFadd{The leverage effect~\mbox{%DIFAUXCMD
\citep{black1976studies,cont2001empirical} }\hskip0pt%DIFAUXCMD
is assessed as the asymmetric phenomenon in which }\DIFaddend negative returns increase future volatility more severely than positive returns of the same magnitude. It is rigorously quantified by the cross-correlation between past returns and future squared returns at lag $\tau > 0$:
    \begin{equation}
        L(\tau) = \text{Corr}(r_t, r_{t+\tau}^2)
    \end{equation}
A financially valid synthetic dataset must exhibit $L(\tau) < 0$ for small $\tau$.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Tail Risk Preservation (VaR and ES):}} %DIFAUXCMD
\DIFdel{Synthetic }\DIFdelend \DIFaddbegin \DIFadd{Tail risk preservation, assessed through VaR and ES, is checked because synthetic }\DIFaddend data used for institutional stress-testing must not systematically underestimate market risk. Generative fidelity is audited using Value-at-Risk (VaR) and Expected Shortfall (ES)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{acerbi2002coherence,acerbi2014expected}}\hskip0pt%DIFAUXCMD
}\DIFaddend . For a confidence level $1 - \alpha$ (e.g., $99\%$), VaR is the quantile of the return distribution:
    \begin{equation}
        \text{VaR}_\alpha = \inf \{ z \in \mathbb{R} \mid \mathbb{P}(r_t \le z) \ge 1 - \alpha \}
    \end{equation}
While ES measures the expected loss conditional on exceeding the VaR threshold:
    \begin{equation}
        \text{ES}_\alpha = \mathbb{E}[r_t \mid r_t \le \text{VaR}_\alpha]
    \end{equation}
Significant deviations between empirical and synthetic VaR/ES indicate a dangerous smoothing of downside risk.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Evaluation: }\DIFaddend Downstream Utility \DIFdelbegin \DIFdel{Evaluation}\DIFdelend \DIFaddbegin \DIFadd{Assessment}\DIFaddend }
To bridge the gap between theoretical machine learning metrics and practical finance, an increasingly adopted paradigm evaluates generated data \DIFdelbegin \DIFdel{strictly based on its }\DIFdelend \DIFaddbegin \DIFadd{primarily by }\DIFaddend utility in executing downstream financial tasks. \DIFdelbegin \DIFdel{If synthetic data enhances the performance of algorithmic systems, it possesses intrinsic }\DIFdelend \DIFaddbegin \DIFadd{Improved downstream performance provides task-specific evidence of synthetic-data }\DIFaddend value.

\DIFdelbegin %DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Forecasting Improvement (TSTR vs. TRTR):}} %DIFAUXCMD
\DIFdel{This protocol }\DIFdelend \DIFaddbegin \DIFadd{Forecasting improvement, assessed through TSTR versus TRTR, }\DIFaddend evaluates the generator as a data augmentation engine. A predictive model $f_\theta$ is trained entirely on generated data $\hat{\mathbf{X}}$ and evaluated on real, out-of-sample data $\mathbf{X}_{test}$. This \DIFdelbegin \DIFdel{Train-on-Synthetic, Test-on-Real }\DIFdelend \DIFaddbegin \DIFadd{train-on-synthetic/test-on-real }\DIFaddend (TSTR) performance\DIFaddbegin \DIFadd{, following the evaluation design of Esteban et al.~\mbox{%DIFAUXCMD
\citep{esteban2017real}}\hskip0pt%DIFAUXCMD
, }\DIFaddend is compared against the \DIFdelbegin \DIFdel{Train-on-Real, Test-on-Real }\DIFdelend \DIFaddbegin \DIFadd{train-on-real/test-on-real }\DIFaddend (TRTR) baseline. A TSTR predictive error (e.g., MSE or directional accuracy) that closely approaches or surpasses TRTR demonstrates exceptional generative utility.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Trading Agent Training and Simulation:}} %DIFAUXCMD
\DIFdel{By }\DIFdelend \DIFaddbegin \DIFadd{Trading-agent training and simulation is evaluated by }\DIFaddend deploying the generative model as an interactive market environment \DIFdelbegin \DIFdel{, Reinforcement Learning }\DIFdelend \DIFaddbegin \DIFadd{in which reinforcement learning }\DIFaddend (RL) trading agents can be trained on millions of synthetic episodes. The utility of the generator is subsequently quantified by the financial performance of the agent when deployed on real out-of-sample data, measured via the annualized Sharpe \DIFdelbegin \DIFdel{Ratio }\DIFdelend \DIFaddbegin \DIFadd{ratio }\DIFaddend ($SR$)\DIFdelbegin \DIFdel{and Maximum Drawdown }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{sharpe1994sharpe} }\hskip0pt%DIFAUXCMD
and maximum drawdown }\DIFaddend ($MDD$):
    \begin{equation}
        SR = \frac{\mathbb{E}[R_p - R_f]}{\sqrt{\text{Var}(R_p)}}, \quad MDD = \max_{\tau \in (0, T)} \left( \frac{\max_{t \in (0, \tau)} P_t - P_\tau}{\max_{t \in (0, \tau)} P_t} \right)
    \end{equation}
    where $R_p$ is portfolio return, $R_f$ is the risk-free rate, and $P_t$ is the portfolio value at time $t$.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Evaluation: }\DIFaddend Robustness, Controllability, and Privacy}
As generative architectures—particularly foundation models and diffusion models—become increasingly sophisticated, evaluation protocols must expand to audit advanced capabilities and regulatory compliance.

\DIFdelbegin %DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Regime Robustness:}} %DIFAUXCMD
\DIFdel{Generic }\DIFdelend \DIFaddbegin \DIFadd{Regime robustness is assessed because generic }\DIFaddend metrics calculated over long, blended time horizons obscure a model's failure in specific contexts. Robustness evaluation partitions test data into discrete regimes (e.g., low-volatility bull markets vs. high-volatility crashes) using \DIFdelbegin \DIFdel{Hidden Markov Models (HMM)}\DIFdelend \DIFaddbegin \DIFadd{hidden Markov models (HMMs)~\mbox{%DIFAUXCMD
\citep{rabiner1989tutorial,hamilton1989new} }\hskip0pt%DIFAUXCMD
}\DIFaddend or variance thresholds. Models are evaluated on their ability to maintain stable generative fidelity across these shifting boundaries.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Conditional Consistency:}} %DIFAUXCMD
\DIFdel{For }\DIFdelend \DIFaddbegin \DIFadd{Conditional consistency is assessed for }\DIFaddend multimodal or controllable generation (e.g., text-to-market or target-volatility synthesis)\DIFdelbegin \DIFdel{, evaluation }\DIFdelend \DIFaddbegin \DIFadd{. Evaluation }\DIFaddend must quantify semantic alignment. This involves measuring the correlation between the conditioning input (e.g., a target VaR level) and the actualized metric in the generated sequence, ensuring the generator is not merely ignoring the prompt to produce unconditional mode-collapsed outputs.

\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Privacy Leakage and Memorization Risk:}} %DIFAUXCMD
\DIFdel{A }\DIFdelend \DIFaddbegin \DIFadd{Privacy leakage and memorization risk are assessed because a }\DIFaddend critical motivation for FTSG is \DIFdelbegin \DIFdel{bypassing strict }\DIFdelend \DIFaddbegin \DIFadd{reducing privacy risk under }\DIFaddend financial data-sharing regulations (e.g., GDPR). To verify that a model has not simply memorized proprietary training data, the \DIFdelbegin \DIFdel{Distance-to-Closest Record }\DIFdelend \DIFaddbegin \DIFadd{distance-to-closest-record }\DIFaddend (DCR) \DIFaddbegin \DIFadd{statistic, used in privacy-preserving synthetic-data evaluation~\mbox{%DIFAUXCMD
\citep{bindschaedler2017plausible}}\hskip0pt%DIFAUXCMD
, }\DIFaddend is computed:
    \begin{equation}
        \text{DCR}(\hat{x}) = \min_{x \in \mathbf{X}_{train}} \|\hat{x} - x\|_2
    \end{equation}
If the DCR distribution peaks near zero, the model suffers from severe privacy leakage. Furthermore, models optimized with \DIFdelbegin \DIFdel{Differential Privacy }\DIFdelend \DIFaddbegin \DIFadd{differential privacy }\DIFaddend (DP)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{dwork2006differential,dwork2014algorithmic} }\hskip0pt%DIFAUXCMD
}\DIFaddend are evaluated against their $(\epsilon, \delta)$-DP bounds, analyzing the mathematical trade-off between the privacy budget $\epsilon$ and the resulting degradation in downstream utility.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Evaluation: }\DIFaddend A Unified \DIFdelbegin \DIFdel{Evaluation }\DIFdelend Checklist}
The lack of standardized evaluation frequently leads to contradictory claims regarding state-of-the-art performance in FTSG. To resolve this, we formalize a unified evaluation checklist, mapping the aforementioned rigorous metrics to their appropriate problem formulations.

As detailed in Table \ref{tab:evaluation_checklist}, researchers must construct a composite evaluation protocol. For instance, an \textit{imputation} model may prioritize pointwise accuracy (MSE) and temporal coherence, whereas a \textit{market simulation} architecture must be rigorously audited against stylized facts (\DIFdelbegin \DIFdel{Kurtosis, Ljung-Box testsfor volatility clustering}\DIFdelend \DIFaddbegin \DIFadd{kurtosis and Ljung--Box tests~\mbox{%DIFAUXCMD
\citep{ljung1978measure} }\hskip0pt%DIFAUXCMD
for temporal dependence}\DIFaddend ) and downstream utility (TSTR). No single metric is sufficient; financial validity emerges only at the intersection of distributional fidelity, economic logic, and practical applicability.

\subsection{\DIFaddbegin \DIFadd{Evaluation: }\DIFaddend Emerging Trends \DIFdelbegin \DIFdel{in Evaluation }\DIFdelend and Benchmarking}

The evaluation of synthetic financial data is rapidly transitioning from the isolated calculation of generic metrics toward comprehensive, standardized benchmark platforms. Recent literature \DIFdelbegin \DIFdel{reveals a definitive paradigm shift focused }\DIFdelend \DIFaddbegin \DIFadd{indicates a shift in focus }\DIFaddend on multimodal reasoning, structural market constraints, and full-pipeline financial workflows. We categorize these emerging trends into three primary directions:

The first direction is conditional specificity and scenario-based evaluation: standard distribution-matching metrics often fail to evaluate whether a conditional generative model actually respects its conditioning metadata (e.g., specific market regimes or macroeconomic textual prompts). To address this, \DIFdelbegin \DIFdel{\mbox{%DIFAUXCMD
\citep{narasimhan2024timeweaver} }\hskip0pt%DIFAUXCMD
introduced }\DIFdelend \DIFaddbegin \DIFadd{Narasimhan and Jiang~\mbox{%DIFAUXCMD
\citep{narasimhan2024timeweaver} }\hskip0pt%DIFAUXCMD
introduced }\DIFaddend \textit{\DIFdelbegin \DIFdel{Time Weaver}\DIFdelend \DIFaddbegin \DIFadd{TimeWeaver}\DIFaddend }, \DIFdelbegin \DIFdel{pioneering novel }\DIFdelend \DIFaddbegin \DIFadd{together with }\DIFaddend specificity metrics that explicitly penalize generative models for failing to reproduce metadata-specific features in the synthesized sequences.

The second direction is market microstructure and LOB standardization: evaluating high-frequency tick data and Limit Order Books (LOB) requires specialized protocols due to their asynchronous nature and stringent cross-feature constraints. \DIFaddbegin \DIFadd{Zhong et al.~}\DIFaddend \citep{lobench2025} recently introduced \textit{LOBench}, \DIFdelbegin \DIFdel{the first }\DIFdelend \DIFaddbegin \DIFadd{a }\DIFaddend standardized benchmark specifically engineered for LOB representation and generation. Utilizing massive, real-world China A-share market data, LOBench establishes unified preprocessing pipelines and task-specific baselines, allowing researchers to evaluate generative fidelity directly against microstructural properties such as queue dynamics and volume imbalances.

The third direction is full-pipeline workflows and multimodal reasoning: instead of treating time-series generation as an isolated algorithmic task, modern benchmarks evaluate synthetic data based on its utility within broader economic workflows. \DIFaddbegin \DIFadd{Jiang et al.~}\DIFaddend \citep{finmaster2025} proposed \textit{FinMaster}, a holistic benchmark featuring a dedicated \textit{FinSim} module that evaluates how well generated synthetic, privacy-compliant financial data replicates market dynamics for downstream corporate auditing and accounting tasks. Similarly, \DIFdelbegin \DIFdel{comprehensive }\DIFdelend \DIFaddbegin \DIFadd{Yu et al.~\mbox{%DIFAUXCMD
\citep{tsrbench2026} }\hskip0pt%DIFAUXCMD
introduced the }\DIFaddend multi-modal \DIFdelbegin \DIFdel{benchmarks like }\textit{\DIFdel{TSRBench}} %DIFAUXCMD
\DIFdel{\mbox{%DIFAUXCMD
\citep{tsrbench2026} }\hskip0pt%DIFAUXCMD
have been introduced }\DIFdelend \DIFaddbegin \DIFadd{benchmark }\textit{\DIFadd{TSRBench}} \DIFaddend to stress-test the reasoning capabilities of Large Language Models (LLMs) over time-series data. These platforms reveal a critical insight for evaluation: strong semantic reasoning does not guarantee accurate context-aware numerical generation, highlighting the need for hybrid evaluation metrics that simultaneously score textual alignment and continuous mathematical fidelity.

 \subsection{Data Resources\DIFaddbegin \DIFadd{: Raw Sources }\DIFaddend and \DIFdelbegin \DIFdel{Benchmarks}\DIFdelend \DIFaddbegin \DIFadd{Modalities}\DIFaddend }
\label{sec:data_resources}

The empirical foundation of Financial Time-Series Generation (FTSG) relies on the quality, granularity, and structure of the underlying data. Unlike static image datasets, financial data is continuous, highly non-stationary, and prone to hidden biases. We systematically cluster these resources moving from raw modalities to curated databases, standardized benchmarks, and ultimately, the practical pitfalls of dataset construction.

\DIFdelbegin \subsection{\DIFdel{Raw Data Sources and Modalities}}
%DIFAUXCMD
\addtocounter{subsection}{-1}%DIFAUXCMD
\DIFdelend Financial time series are not monolithic; they exhibit drastically different statistical signatures depending on the asset class and sampling frequency. The rationale for clustering these sources lies in the specific generative challenges they present. \DIFdelbegin \DIFdel{We categorize raw FTSG data }\DIFdelend \DIFaddbegin \DIFadd{Raw FTSG data fall }\DIFaddend into four distinct modalities\DIFdelbegin \DIFdel{:
}%DIFDELCMD < \begin{enumerate}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Equities and Market Indices:}} %DIFAUXCMD
\DIFdel{Encompassing }\DIFdelend \DIFaddbegin \DIFadd{.
}

\DIFadd{Equities and market indices encompass }\DIFaddend individual stocks (e.g., AAPL, TSLA) and aggregate indices (e.g., S\&P 500, CSI 300). Sampled typically at daily or minutely frequencies, these series are the primary testbeds for unconditional synthesis and extrapolation. They present fundamental generative challenges, including pronounced \textit{volatility clustering} \DIFdelbegin \DIFdel{, }\textit{\DIFdel{fat-tailed distributions}}%DIFAUXCMD
\DIFdel{, }\DIFdelend and \DIFaddbegin \textit{\DIFadd{fat-tailed distributions}}\DIFadd{~\mbox{%DIFAUXCMD
\citep{cont2001empirical}}\hskip0pt%DIFAUXCMD
, as well as }\DIFaddend \textit{regime-shifting non-stationarity}\DIFdelbegin \DIFdel{.
    }%DIFDELCMD < \item %%%
\textbf{\DIFdel{High-Frequency Microstructure (LOB):}} %DIFAUXCMD
\DIFdel{Limit Order Books }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{hamilton1989new}}\hskip0pt%DIFAUXCMD
.
}

\DIFadd{High-frequency microstructure data include limit order books }\DIFaddend (LOB), tick-by-tick trades, and Level-2 quotes recorded at the millisecond or microsecond level. \DIFdelbegin \DIFdel{Generating LOB datais exceptionally }\DIFdelend \DIFaddbegin \DIFadd{The market-microstructure properties of LOB data~\mbox{%DIFAUXCMD
\citep{ohara1995market,gould2013limit} }\hskip0pt%DIFAUXCMD
make generation }\DIFaddend complex due to its asynchronous nature and the strict spatial constraints required (e.g., bids must strictly remain below asks, and queue volumes dictate execution). This data is essential for simulating interactive, multi-agent algorithmic trading environments.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Macroeconomics and Fixed Income:}} %DIFAUXCMD
\DIFdel{Indicators }\DIFdelend \DIFaddbegin 

\DIFadd{Macroeconomic and fixed-income data include indicators }\DIFaddend such as Treasury yield curves, the Consumer Price Index (CPI), and Gross Domestic Product (GDP). Sampled at low frequencies (monthly or quarterly), these datasets reflect systemic economic health. The primary challenge here is severe data sparsity and the need to interpolate/impute missing values across shifting long-term economic cycles.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Alternative Data and Textual Metadata:}} %DIFAUXCMD
\DIFdel{With the rise of multi-modal foundation models, }\DIFdelend \DIFaddbegin 

\DIFadd{Alternative data and textual metadata augment }\DIFaddend purely numerical series \DIFdelbegin \DIFdel{are increasingly augmented with alternative data}\DIFdelend \DIFaddbegin \DIFadd{as multi-modal foundation models become more common}\DIFaddend . This includes macroeconomic news headlines, central bank policy texts, earnings call transcripts, and social media sentiment\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{tetlock2007giving,loughran2011liability}}\hskip0pt%DIFAUXCMD
}\DIFaddend . This modality is crucial for training controllable generators and scenario-guided market simulators.
\DIFdelbegin %DIFDELCMD < \end{enumerate}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Data Resources: }\DIFaddend Curated Databases and APIs}
Historically, raw financial data is fragmented and optimized for human visualization. To facilitate the massive data ingestion required by deep generative models, researchers \DIFdelbegin \DIFdel{rely on }\DIFdelend \DIFaddbegin \DIFadd{routinely aggregate data from }\DIFaddend institutional databases and developer APIs\DIFdelbegin \DIFdel{:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Institutional and Academic Repositories:}} %DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{.
}

\DIFaddend For rigorous backtesting and synthesis, \DIFaddbegin \DIFadd{institutional and academic repositories including }\DIFaddend Wharton Research Data Services (WRDS), the Center for Research in Security Prices (CRSP), and Compustat\DIFdelbegin \DIFdel{provide survivorship-bias-free equity data.}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{wrds,crsp,compustat} }\hskip0pt%DIFAUXCMD
are designed to mitigate the survivorship bias documented by Brown et al.~\mbox{%DIFAUXCMD
\citep{brown1992survivorship}}\hskip0pt%DIFAUXCMD
. }\DIFaddend For macroeconomic series, FRED (Federal Reserve Economic Data)\DIFdelbegin \DIFdel{remains the gold standard.}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{fred} }\hskip0pt%DIFAUXCMD
is a widely used public repository maintained by the Federal Reserve Bank of St.~Louis. }\DIFaddend In Chinese markets, CSMAR\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{csmar} }\hskip0pt%DIFAUXCMD
}\DIFaddend offers comprehensive, high-quality A-share trading records.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Open-Source and Developer APIs:}} %DIFAUXCMD
\DIFdel{Platforms like }\DIFdelend \DIFaddbegin 

\DIFadd{Open-source and developer APIs are also available through platforms such as }\DIFaddend Yahoo Finance and Investing.com\DIFdelbegin \DIFdel{are }\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{yahoofinance,investingcom}}\hskip0pt%DIFAUXCMD
, }\DIFaddend frequently accessed via \DIFaddbegin \DIFadd{the }\DIFaddend open-source Python \DIFdelbegin \DIFdel{wrappers (}\DIFdelend \DIFaddbegin \DIFadd{package }\DIFaddend \texttt{yfinance}\DIFdelbegin \DIFdel{)}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{yfinance}}\hskip0pt%DIFAUXCMD
}\DIFaddend . For programmatic, high-frequency access, APIs such as Alpha Vantage, Alpaca, and Polygon.io\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{alphavantage,alpaca,polygon} }\hskip0pt%DIFAUXCMD
}\DIFaddend deliver massive historical streams suitable for training large-scale predictive models.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Cryptocurrency Exchanges:}} %DIFAUXCMD
\DIFdel{Due to }\DIFdelend \DIFaddbegin 

\DIFadd{Cryptocurrency exchanges are widely used because }\DIFaddend 24/7 trading and open data access \DIFdelbegin \DIFdel{, crypto markets have become highly popular }\DIFdelend \DIFaddbegin \DIFadd{make crypto markets particularly suitable }\DIFaddend for testing generative algorithms. Platforms \DIFdelbegin \DIFdel{like }\DIFdelend \DIFaddbegin \DIFadd{such as }\DIFaddend Binance and CoinMarketCap\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{binance,coinmarketcap} }\hskip0pt%DIFAUXCMD
}\DIFaddend provide free API access to granular tick and volume data, capturing extreme volatility events ideal for stress-testing generation.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{\DIFaddbegin \DIFadd{Data Resources: }\DIFaddend Standardized Benchmarks and Evaluation Suites}
Historically, FTSG research relied on ad-hoc, proprietary data slices, severely hindering reproducibility and fair model comparison. The recent emergence of unified benchmark platforms marks a critical maturation in the field. \DIFdelbegin \DIFdel{These benchmarks }\DIFdelend \DIFaddbegin \DIFadd{Recent benchmark studies, including FinTSB~\mbox{%DIFAUXCMD
\citep{hu2025fintsb}}\hskip0pt%DIFAUXCMD
, GIFT-Eval~\mbox{%DIFAUXCMD
\citep{aksu2024gifteval}}\hskip0pt%DIFAUXCMD
, TFB~\mbox{%DIFAUXCMD
\citep{qiu2024tfb}}\hskip0pt%DIFAUXCMD
, FinTSBridge~\mbox{%DIFAUXCMD
\citep{wang2025fintsbridge}}\hskip0pt%DIFAUXCMD
, TSGBench~\mbox{%DIFAUXCMD
\citep{ang2023tsgbench}}\hskip0pt%DIFAUXCMD
, and CTBench~\mbox{%DIFAUXCMD
\citep{ang2025ctbench}}\hskip0pt%DIFAUXCMD
, }\DIFaddend encapsulate vast datasets, pre-define chronological train/test/validation splits, and standardize evaluation metrics (e.g., masking ratios for imputation, prompt templates for forecasting).

By providing a ``level playing field,'' these suites allow researchers to disentangle algorithmic improvements from data-mining advantages. A summary of recent and authoritative FTSG benchmarks is provided in Table \ref{tab:datasets}.

\subsection{\DIFaddbegin \DIFadd{Data Resources: }\DIFaddend Practical Issues in Dataset Construction}
Constructing financial datasets for generative models is fraught with domain-specific pitfalls. A generative architecture, no matter how mathematically advanced, will learn and amplify the structural biases present in its training data. \DIFdelbegin \DIFdel{Key issues include:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Survivorship Bias:}} %DIFAUXCMD
\DIFdel{Datasets }\DIFdelend \DIFaddbegin \DIFadd{Five issues are particularly important.
}

\DIFadd{Survivorship bias~\mbox{%DIFAUXCMD
\citep{brown1992survivorship} }\hskip0pt%DIFAUXCMD
occurs when datasets }\DIFaddend that only include currently active assets inadvertently filter out companies that went bankrupt or were delisted. Generative models trained on such data will synthesize artificially optimistic market trajectories and fail to accurately model the probability of asset default, ruining their utility for downside risk management.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Look-Ahead Bias in Alignment:}} %DIFAUXCMD
\DIFdel{This }\DIFdelend \DIFaddbegin 

\DIFadd{Look-ahead bias in alignment }\DIFaddend occurs when models are trained on information that was not publicly available at the timestamp of generation. A common pitfall in FTSG is using historically \textit{revised} macroeconomic data (e.g., revised GDP figures published months later) rather than the \DIFdelbegin \DIFdel{"}\DIFdelend point-in-time\DIFdelbegin \DIFdel{" unrevised data }\DIFdelend \DIFaddbegin \DIFadd{, unrevised data emphasized in leakage-aware financial machine learning~\mbox{%DIFAUXCMD
\citep{lopezdeprado2018advances}}\hskip0pt%DIFAUXCMD
}\DIFaddend , causing the generator to leak future realities.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Chronological Splitting vs. Random Masking:}} %DIFAUXCMD
\DIFdel{Unlike }\DIFdelend \DIFaddbegin 

\DIFadd{Chronological splitting, rather than random masking, is required because unlike }\DIFaddend static images where independent and identically distributed (i.i.d.) random splitting is acceptable, financial time series must \DIFdelbegin \DIFdel{be strictly partitioned chronologically}\DIFdelend \DIFaddbegin \DIFadd{generally be partitioned chronologically, using temporal validation or purged and embargoed splits where appropriate~\mbox{%DIFAUXCMD
\citep{lopezdeprado2018advances}}\hskip0pt%DIFAUXCMD
}\DIFaddend . Randomly splitting overlapping sequence windows leads to severe temporal data leakage. Similarly, in imputation tasks, random masking often fails to reflect the \DIFdelbegin \DIFdel{true missingness mechanisms }\DIFdelend \DIFaddbegin \DIFadd{missingness mechanisms distinguished by Rubin~\mbox{%DIFAUXCMD
\citep{rubin1976inference} }\hskip0pt%DIFAUXCMD
}\DIFaddend (e.g., contiguous trading halts), leading to models that perform well on paper but fail in live environments.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Asynchronous Frequencies and Missingness:}} %DIFAUXCMD
\DIFdel{In }\DIFdelend \DIFaddbegin 

\DIFadd{Asynchronous frequencies and missingness arise in }\DIFaddend multivariate generation (e.g., \DIFdelbegin \DIFdel{simulating }\DIFdelend \DIFaddbegin \DIFadd{simulation of }\DIFaddend a global portfolio) \DIFdelbegin \DIFdel{, }\DIFdelend \DIFaddbegin \DIFadd{because }\DIFaddend assets trade on different global exchanges with distinct operating hours and holidays\DIFaddbegin \DIFadd{, creating non-synchronous observations~\mbox{%DIFAUXCMD
\citep{hayashi2005covariance}}\hskip0pt%DIFAUXCMD
}\DIFaddend . Naively padding these datasets forward creates artificial zero-volatility spans, causing cross-sectional generators (\DIFdelbegin \DIFdel{like Diffusion }\DIFdelend \DIFaddbegin \DIFadd{such as diffusion }\DIFaddend models) to learn spurious flat-line correlations.
\DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Market Regime Imbalance:}} %DIFAUXCMD
\DIFdel{Historical }\DIFdelend \DIFaddbegin 

\DIFadd{Market regime imbalance, consistent with regime-switching models~\mbox{%DIFAUXCMD
\citep{hamilton1989new}}\hskip0pt%DIFAUXCMD
, occurs because historical }\DIFaddend datasets are overwhelmingly dominated by standard, low-to-medium volatility market conditions. Without specialized sampling techniques, models suffer from severe class imbalance and will systematically fail to generate realistic \DIFdelbegin \DIFdel{, high-fidelity }\DIFdelend crash scenarios (e.g., the \DIFdelbegin \DIFdel{2020 }\DIFdelend COVID-19 \DIFdelbegin \DIFdel{flash crash}\DIFdelend \DIFaddbegin \DIFadd{market reaction in 2020~\mbox{%DIFAUXCMD
\citep{ramelli2020feverish}}\hskip0pt%DIFAUXCMD
}\DIFaddend ) simply because these events represent a fraction of the training distribution.
 \DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin 

\DIFaddend % Discussion (no subheadings, integrates challenges, limitations, conclusions)
\section{Discussion}
\label{sec:discussion}

Despite the rapid advancements in financial time-series generation, several fundamental challenges remain unresolved at the intersection of generative artificial intelligence and empirical financial econometrics. Bridging the gap between statistical expressiveness and market validity requires addressing core bottlenecks in causality, theoretical boundedness, cross-modal integration, and temporal non-stationarity.

\DIFdelbegin \DIFdel{\revadd{A first fundamental challenge concerns causal and invariant representation learning, as current} }\DIFdelend \DIFaddbegin \DIFadd{A first fundamental challenge concerns causal and invariant representation learning: current }\DIFaddend FTSG models often learn spurious correlations specific to a particular market environment or training window. This stems from a lack of explicit disentanglement between true causal factors (e.g., shifts in fundamental central bank policies) and mere coincidental patterns (e.g., a specific high-frequency trading algorithm's temporary market dominance). Consequently, models that assume data are Independent and Identically Distributed (i.i.d.) \DIFdelbegin \DIFdel{fail drastically }\DIFdelend \DIFaddbegin \DIFadd{can degrade substantially }\DIFaddend when encountering novel market regimes due to severe out-of-distribution (OOD) shifts \citep{arjovsky2019invariant, peters2016causal}. 
To address this, future FTSG frameworks must move toward environment-invariant learning. By treating specific market regimes (e.g., high vs. low volatility) as distinct training ``environments,'' techniques like Invariant Risk Minimization (IRM) \citep{arjovsky2019invariant} can be \DIFdelbegin \DIFdel{leveraged to eliminate }\DIFdelend \DIFaddbegin \DIFadd{used to reduce sensitivity to }\DIFaddend spurious environmental correlations. Early attempts in forecasting demonstrate that enforcing representation invariance across regimes improves robustness \citep{cao2023irm}. Porting this paradigm to generative models \DIFdelbegin \DIFdel{will ensure that synthesized trajectories remain realistic even under unprecedented }\DIFdelend \DIFaddbegin \DIFadd{may improve the robustness of synthesized trajectories under novel }\DIFaddend economic shifts.

\DIFdelbegin \DIFdel{\revadd{A second challenge is econometrically bounded generation.} A }\DIFdelend \DIFaddbegin \DIFadd{A second challenge is econometrically bounded generation. A }\DIFaddend well-documented constraint of purely data-driven generative models (such as standard GANs\DIFdelbegin \DIFdel{and Diffusion Models}\DIFdelend \DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{goodfellow2014gan} }\hskip0pt%DIFAUXCMD
and diffusion models~\mbox{%DIFAUXCMD
\citep{ho2020ddpm,song2021score}}\hskip0pt%DIFAUXCMD
}\DIFaddend ) is that they can replicate past empirical distributions without guaranteeing adherence to established market microstructure or stochastic process theory \citep{cont2001empirical}. For instance, unconstrained models might generate prices that breach arbitrage boundaries or fail to exhibit mean-reverting volatility.
Drawing inspiration from Physics-Informed Neural Networks (PINNs) in the physical sciences \citep{raissi2019physics}, future research should focus on \textit{econometrically-bounded} generation. Embedding economic priors---such as \DIFdelbegin \DIFdel{Geometric Brownian Motion }\DIFdelend \DIFaddbegin \DIFadd{geometric Brownian motion }\DIFaddend (GBM)\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{black1973pricing}}\hskip0pt%DIFAUXCMD
}\DIFaddend , stochastic volatility models, or jump-diffusion processes for extreme events\DIFaddbegin \DIFadd{~}\DIFaddend \citep{merton1976option}---directly into the generator's loss functions provides a powerful white-box constraint. Recent advancements have successfully integrated GBM priors into score-matching diffusion models \citep{tanaka2025cofindiff}. Expanding this to capture more complex stylized facts will ensure that synthesized sequences are not merely statistically plausible, but also economically rational.

\DIFdelbegin \DIFdel{\revadd{A third challenge is multi-modal time series generation.} }\DIFdelend \DIFaddbegin \DIFadd{A third challenge is multi-modal time series generation. }\DIFaddend Existing FTSG models broadly generate time-series data in isolation, neglecting the exogenous, multi-modal signals that drive actual market dynamics. However, financial markets are multi-causal systems: a macroeconomic announcement (textual metadata) triggers shifts in order flow (discrete events), which subsequently alter implied volatility surfaces (continuous manifolds). To capture this complexity, future work should pursue two promising avenues. First, cross-modal conditioned generation can encode broader heterogeneous data, such as central bank operations, limit order book states, and earnings call transcripts \cite{gholamrezaei2023conditioning, T2S, lozano2023dual}, using a unified representation space to precisely steer generative trajectories beyond narrow textual prompts \cite{Time-LLM, liu2025calf}. Second, joint multi-modal generation involves the simultaneous generation of linked data streams. By jointly modeling limit order books, financial news headlines, and derivative prices, generators can inherently learn cross-modal causal dependencies and lead-lag relationships \cite{lozano2023dual}, which is vital for simulating high-impact scenarios such as flash crashes.

\DIFdelbegin \DIFdel{\revadd{A fourth challenge is online adaptation under regime shifts.} }\DIFdelend \DIFaddbegin \DIFadd{A fourth challenge is online adaptation under regime shifts. }\DIFaddend Despite the zero-shot capabilities of modern time-series foundation models, they remain susceptible to concept drift---the frequent distributional shifts triggered by evolving economic cycles or regulatory changes. Therefore, static generative models will inevitably degrade over time. To avoid the notorious ``catastrophic forgetting'' associated with fine-tuning on new data \cite{mccloskey1989catastrophic, zenke2017continual}, FTSG must transition toward online adaptation.
One promising avenue is Bayesian Continuous Learning, utilizing Streaming Variational Bayes \cite{nguyen2017variational} to incrementally update the generator's latent space as new market observations arrive in real time. Alternatively, framing generation within an Online Reinforcement Learning framework \cite{moody2001learning}, where the model receives rewards based on the real-time fidelity of its synthetic samples relative to current market conditions, allows for near-instant calibration to abrupt regime shifts without retraining from scratch.

\DIFdelbegin \DIFdel{\revadd{Finally, we reflect on limitations, practical implications, and a concluding outlook.} }\DIFdelend \DIFaddbegin \DIFadd{Finally, we reflect on limitations, practical implications, and a concluding outlook. }\DIFaddend While this survey synthesizes 131 studies spanning 2021 to early 2026, several intrinsic limitations characterize the current empirical literature. Primarily, existing benchmarks remain disproportionately concentrated in liquid equity and cryptocurrency markets, with a comparative scarcity of standardized suites for fixed income, credit derivatives, and illiquid alternative assets. Furthermore, the absence of universally accepted, open-source matching engine simulators hampers standardized, apples-to-apples comparisons of downstream execution utility. 
Nonetheless, our proposed two-level taxonomy successfully unifies the field across task-oriented and technique-driven dimensions, providing a structured conceptual framework for future studies. By compiling curated databases, establishing a multi-dimensional evaluation protocol, and identifying theoretical boundaries, this review provides a \DIFdelbegin \DIFdel{definitive }\DIFdelend \DIFaddbegin \DIFadd{structured }\DIFaddend foundation for researchers and practitioners developing next-generation financial generative models. 
% Methods (compulsory subheadings, placed after Discussion)
\section{Methods}
\label{sec:methods}
\DIFaddbegin 

\DIFadd{This review follows the reporting logic of PRISMA 2020 for transparent search, screening, eligibility assessment, and synthesis~\mbox{%DIFAUXCMD
\citep{page2021prisma}}\hskip0pt%DIFAUXCMD
.
}\DIFaddend To ensure the reproducibility, transparency, and comprehensiveness of this review, we adhered to a systematic literature review methodology tailored to the intersection of artificial intelligence and quantitative finance, designed in accordance with the Preferred Reporting Items for Systematic Reviews and Meta-Analyses (PRISMA 2020) guidelines\DIFaddbegin \DIFadd{~\mbox{%DIFAUXCMD
\citep{page2021prisma}}\hskip0pt%DIFAUXCMD
}\DIFaddend . The completed PRISMA 2020 checklist is provided as Supplementary Information. This section outlines our protocols for literature retrieval, inclusion/exclusion criteria, data extraction, and the dimensions utilized for synthesizing the gathered research.

\subsection{Literature Retrieval Strategy}
Our primary objective was to capture the state-of-the-art advancements in financial time-series generation, particularly focusing on the paradigm shift driven by generative AI over recent years.

We conducted exhaustive searches across premier academic databases and preprint repositories to ensure broad coverage of both peer-reviewed and cutting-edge unpublished works. The primary databases included Google Scholar, DBLP, Web of Science, Scopus, and arXiv.

To construct a robust search query, we combined terms related to the financial domain, time-series data, and generative tasks. The primary Boolean search strings included:
(\textit{``financial time-series generation''} OR \textit{``synthetic financial data''} OR \textit{``market simulation generative model''}) AND (\textit{``financial diffusion model''} OR \textit{``financial GAN''} OR \textit{``time-series imputation finance''} OR \textit{``financial foundation models''}).

Given the rapid evolution of deep generative models, we restricted our primary search window to papers published or uploaded between \DIFdelbegin \textbf{\DIFdel{2021 and early 2026 (including early-access and preprints)}}%DIFAUXCMD
\DIFdelend \DIFaddbegin \DIFadd{2021 and early 2026 (including early-access and preprints)}\DIFaddend . We included articles from high-impact journals (e.g., \textit{Information Sciences}, \textit{Expert Systems with Applications}, \textit{Quantitative Finance}), top-tier computer science conferences (e.g., NeurIPS, ICML, ICLR, KDD, IJCAI), and highly cited arXiv preprints that represent significant methodological breakthroughs.

After full-text screening, the final corpus comprises 131 studies, each assigned to a unique primary task category: extrapolation (54), imputation (29), and synthesis (47). Multi-task or cross-cutting contributions are discussed in the relevant sections but are not double-counted in task-level comparison tables.

\subsection{Inclusion and Exclusion Criteria}
As illustrated in Figure\DIFdelbegin \DIFdel{\ref{fig:lit_dist}, to maintain a high signal-to-noise ratio and ensure strict relevance to the core theme of FTSG, }\DIFdelend \DIFaddbegin \DIFadd{~\ref{fig:lit_dist}, }\DIFaddend all retrieved papers underwent a two-stage screening process\DIFdelbegin \DIFdel{(}\DIFdelend \DIFaddbegin \DIFadd{, comprising }\DIFaddend title/abstract screening followed by full-text review \DIFdelbegin \DIFdel{) based on the following criteria:
}\DIFdelend \DIFaddbegin \DIFadd{against explicit eligibility criteria.
}\DIFaddend 

\DIFdelbegin \DIFdel{The following inclusion criteria were applied:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\DIFdel{Papers that explicitly formulate and address }\DIFdelend \DIFaddbegin \DIFadd{Studies were eligible when they explicitly formulated or addressed }\DIFaddend the generation, imputation, or fully synthetic simulation of financial time-series data\DIFdelbegin \DIFdel{.
    }%DIFDELCMD < \item %%%
\DIFdel{Studies that propose }\DIFdelend \DIFaddbegin \DIFadd{, proposed }\DIFaddend novel generative architectures \DIFdelbegin \DIFdel{(e.g., GANs, diffusion models, LLMs) }\DIFdelend specifically adapted for financial contexts\DIFdelbegin \DIFdel{.
    }%DIFDELCMD < \item %%%
\DIFdel{Research that establishes }\DIFdelend \DIFaddbegin \DIFadd{, or established }\DIFaddend unified evaluation protocols, benchmarks, or datasets for synthetic financial data.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\DIFdelbegin \DIFdel{Papers meeting any of the following exclusion criteria were removed:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\DIFdel{Purely }\DIFdelend \DIFaddbegin \DIFadd{We excluded purely }\DIFaddend predictive or forecasting studies that treat\DIFaddbegin \DIFadd{ed}\DIFaddend  outputs strictly as deterministic point estimates without a generative, distributional, or probabilistic perspective. \DIFdelbegin %DIFDELCMD < \item %%%
\DIFdel{General }\DIFdelend \DIFaddbegin \DIFadd{We also excluded general }\DIFaddend time-series generation papers \DIFdelbegin \DIFdel{that lack }\DIFdelend \DIFaddbegin \DIFadd{without }\DIFaddend an identifiable and empirically tested financial application\DIFdelbegin \DIFdel{(e.g., solely tested }\DIFdelend \DIFaddbegin \DIFadd{, such as studies tested solely }\DIFaddend on weather or traffic data\DIFdelbegin \DIFdel{).
    }%DIFDELCMD < \item %%%
\DIFdel{Outdated }\DIFdelend \DIFaddbegin \DIFadd{, together with earlier }\DIFaddend surveys or methodologies that \DIFdelbegin \DIFdel{do }\DIFdelend \DIFaddbegin \DIFadd{did }\DIFaddend not incorporate modern deep learning or generative AI techniques \DIFdelbegin \DIFdel{, unless }\DIFdelend \DIFaddbegin \DIFadd{unless they were }\DIFaddend necessary for historical context.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{Data Extraction and Coding Protocol}
\DIFdelbegin \DIFdel{For each paper that met the inclusion criteria, we executed a structured data extraction protocol. Two independent reviewers (authors) coded the literature to extract key metadata, minimizing subjective bias.
The extracted fields included:
}%DIFDELCMD < \begin{itemize}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Task Type:}} %DIFAUXCMD
\DIFdel{Extrapolation}\DIFdelend \DIFaddbegin \DIFadd{Two reviewers independently screened records and completed a structured extraction form; disagreements were resolved by discussion, consistent with PRISMA 2020 guidance~\mbox{%DIFAUXCMD
\citep{page2021prisma}}\hskip0pt%DIFAUXCMD
.
}

\DIFadd{Task type recorded whether a study addressed extrapolation}\DIFaddend , imputation, micro-level synthesis, or macro-level market simulation. \DIFdelbegin %DIFDELCMD < \item %%%
\textbf{\DIFdel{Model Family:}} %DIFAUXCMD
\DIFdel{Statistical/}\DIFdelend \DIFaddbegin \DIFadd{Model family classified each method as statistical or }\DIFaddend probabilistic, discriminative deep learning, GAN-based, diffusion \DIFdelbegin \DIFdel{/}\DIFdelend \DIFaddbegin \DIFadd{or }\DIFaddend score-based, or \DIFdelbegin \DIFdel{foundation/}\DIFdelend \DIFaddbegin \DIFadd{foundation-model or }\DIFaddend LLM-based\DIFdelbegin \DIFdel{models. }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Data Modality:}} %DIFAUXCMD
\DIFdel{Univariate vs. multivariate }\DIFdelend \DIFaddbegin \DIFadd{. Data modality captured whether the study used univariate or multivariate series, its sampling frequency}\DIFaddend , \DIFdelbegin \DIFdel{frequency (high-frequency order book vs. daily/monthly returns), and conditioning variables (e.g., text , macroeconomic indicators). }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Evaluation Metrics:}} %DIFAUXCMD
\DIFdel{The specific }\DIFdelend \DIFaddbegin \DIFadd{and any conditioning variables such as text or macroeconomic indicators. Evaluation metrics documented the }\DIFaddend quantitative and qualitative \DIFdelbegin \DIFdel{metrics }\DIFdelend \DIFaddbegin \DIFadd{measures }\DIFaddend used to validate \DIFdelbegin \DIFdel{the generated outputs(e.g., }\DIFdelend \DIFaddbegin \DIFadd{generated outputs, including }\DIFaddend MSE, Wasserstein distance, stylized facts, \DIFdelbegin \DIFdel{downstream utility). }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Code/Data Availability:}} %DIFAUXCMD
\DIFdel{Whether the authors open-sourced their codebases or utilized }\DIFdelend \DIFaddbegin \DIFadd{and downstream utility. Code and data availability recorded whether authors released code or used }\DIFaddend public benchmark datasets.
\DIFdelbegin %DIFDELCMD < \end{itemize}
%DIFDELCMD < %%%
\DIFdelend 

\subsection{Review Dimensions and Taxonomy Synthesis}
The extracted data were synthesized \DIFdelbegin \DIFdel{and are presented }\DIFdelend in the Results and Discussion sections along six \DIFdelbegin \DIFdel{unified analytical dimensions:
}%DIFDELCMD < \begin{enumerate}
%DIFDELCMD <     \item %%%
\textbf{\DIFdel{Task Formulation:}} %DIFAUXCMD
\DIFdel{Defining }\DIFdelend \DIFaddbegin \DIFadd{analytical dimensions.
}

\DIFadd{Task formulation defined }\DIFaddend the mathematical boundaries and generative objectives of extrapolation, imputation, and synthesis\DIFdelbegin \DIFdel{(presented in the }\DIFdelend \DIFaddbegin \DIFadd{, as presented in }\DIFaddend Problem Formulation and \DIFdelbegin \DIFdel{A Two-Level Taxonomy subsection). }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Model Design:}} %DIFAUXCMD
\DIFdel{Analyzing }\DIFdelend \DIFaddbegin \DIFadd{Setup. Model design examined }\DIFaddend how specific generative architectures \DIFdelbegin \DIFdel{are engineered to overcome }\DIFdelend \DIFaddbegin \DIFadd{address }\DIFaddend financial data challenges \DIFdelbegin \DIFdel{(detailed across the Extrapolation, Imputation, and Synthesis subsections).
    }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Applications:}} %DIFAUXCMD
\DIFdel{Examining how }\DIFdelend \DIFaddbegin \DIFadd{across the }\DIFaddend extrapolation, imputation, and synthesis \DIFaddbegin \DIFadd{subsections. Applications examined how these tasks }\DIFaddend support downstream quantitative workflows\DIFdelbegin \DIFdel{(presented in the Applications subsection). }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Evaluation Protocol:}} %DIFAUXCMD
\DIFdel{Critiquing }\DIFdelend \DIFaddbegin \DIFadd{. Evaluation protocol assessed }\DIFaddend how the financial realism and empirical validity of generated series are quantified\DIFdelbegin \DIFdel{(presented in the Evaluation Protocols subsection). }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Datasets and Benchmarks:}} %DIFAUXCMD
\DIFdel{Summarizing }\DIFdelend \DIFaddbegin \DIFadd{. Datasets and benchmarks summarized }\DIFaddend the empirical foundations \DIFdelbegin \DIFdel{upon which generative modelsare trained and benchmarked (presented in the Data Resources and Benchmarks subsection).
    }%DIFDELCMD < \item %%%
\textbf{\DIFdel{Open Challenges and Roadmap:}} %DIFAUXCMD
\DIFdel{Identifying }\DIFdelend \DIFaddbegin \DIFadd{used to train and evaluate generative models. Open challenges and the roadmap identified }\DIFaddend theoretical and practical bottlenecks requiring future research \DIFdelbegin \DIFdel{interventions (presented }\DIFdelend \DIFaddbegin \DIFadd{and are discussed }\DIFaddend in the Discussion section\DIFdelbegin \DIFdel{).
}%DIFDELCMD < \end{enumerate}
%DIFDELCMD < 

%DIFDELCMD < %%%
\DIFdelend \DIFaddbegin \DIFadd{.
}\DIFaddend By rigorously adhering to this methodology, this survey transitions from a mere enumeration of literature to a systematic, evaluation-oriented resource paper, providing a \DIFdelbegin \DIFdel{highly reliable foundation for researchers in the field.
 }\DIFdelend \DIFaddbegin \DIFadd{reproducible basis for subsequent synthesis and critical appraisal.
 }

\DIFaddend % Declarations (6 mandatory statements)
\section*{Data Availability}
\DIFdelbegin \DIFdel{\revadd{The datasets generated and/or analyzed during the current study are not publicly available due to the fact that the literature retrieval query logs and the systematic extraction coding tables contain internal reviewer annotations and are subject to licensing restrictions on the underlying subscription databases, but they are available from the corresponding author on reasonable request.} }\DIFdelend \DIFaddbegin \DIFadd{The datasets generated and/or analyzed during the current study are not publicly available because the literature retrieval query logs and systematic extraction coding tables contain reviewer annotations and are subject to licensing restrictions on the underlying subscription databases. They are available from the corresponding author on reasonable request. }\DIFaddend All benchmark datasets, curated databases, and public APIs reviewed and analyzed in this survey (including FinTSB, TFB, TSGBench, CTBench, FinTSBridge, WRDS, CRSP, and FRED) are derived from published literature and publicly accessible repositories as referenced in the Data Resources and Benchmarks subsection.

\section*{Code Availability}
This work is a systematic literature survey and did not generate custom algorithmic codebases or software artifacts. All generative models, benchmark evaluation suites, and foundational model repositories discussed are publicly accessible via their respective cited publications and open-source repositories as cataloged throughout the manuscript.

\section*{Acknowledgements}
The authors would like to thank the colleagues and members of the research group for constructive discussions that contributed to refining the scope and taxonomy of this survey.

\section*{Funding}
The authors declare that no funds, grants, or other financial support were received during the preparation of this manuscript.

\section*{Author Contributions}
Y. Zhang: Conceptualization, Methodology, Writing -- Original Draft. J. Duan: Validation, Formal analysis, Writing -- Review \& Editing. Y. Zou: Validation, Formal analysis, Writing -- Review \& Editing. K. Feng: Supervision, Project administration. J. Xiao: Supervision, Project administration. All authors reviewed, discussed, and approved the final submitted version of the manuscript.

\section*{Competing Interests}
The authors declare no competing financial or non-financial interests.

% References (Nature sequential numbered format)
% naturemag.bst supports natbib's \citet author-name extraction (format.lab.names)
\newpage
\providecommand{\bibcommenthead}{}
\bibliographystyle{sn-nature}
\bibliography{ultimate_complete}

% Figures and Tables (placed after References per IRDM instructions)
\newpage
\section*{Figure Legends and Figures}

\begin{figure}[!ht]
\centering
\includegraphics[width=0.98\linewidth]{separate_figures/Figure1.pdf}
\caption{\textbf{Evolution of Financial Time-Series Generation techniques over the past decade.} Timeline of representative milestones in financial time-series generation (FTSG) from 2020 to early 2026. Colored cards along the central axis denote technical families: coral pink for adversarial generative architectures (GANs), steel blue for diffusion and score-based models, amber orange for time-series foundation models and adapted large language models (LLMs), and sage green for classical statistical, tensor factorization, and deep predictive baselines. Key models include NMTucker\DIFdelbeginFL \DIFdelFL{(Non-negative Matrix Tucker factorization)}\DIFdelendFL , RegTensor, NDDP\DIFdelbeginFL \DIFdelFL{(Nonlinear Diffusion Drift Process)}\DIFdelendFL , PEC-W, SGP-LSTM, TimeGrad, CSDI, Time-LLM, StockTime, CALF\DIFdelbeginFL \DIFdelFL{(Cross-modal Aligned Foundation model)}\DIFdelendFL , FinCast, and MarS\DIFdelbeginFL \DIFdelFL{(Multi-Agent Market Simulator)}\DIFdelendFL . White circular markers denote calendar years 2020, 2021, 2023, 2024, and 2025; directed arrows align each contribution to its release year.}
\label{fig:timeline}
\end{figure}

\newpage

\begin{figure}[!ht]
\centering
\includegraphics[width=0.98\linewidth]{separate_figures/Figure2.pdf}
\caption{\textbf{Hierarchical two-level taxonomy of financial time-series generation.} Systematic classification structure decoupling financial generation tasks from generative modeling paradigms. The primary tree root partitions FTSG into three functional task branches. Light blue shaded blocks designate the forward-looking Financial Time-Series Extrapolation (FTSE) branch, spanning statistical autoregressive models, deep sequential networks, adversarial frameworks, diffusion dynamics, and foundation model adaptations. Light green shaded blocks designate the bidirectional Financial Time-Series Imputation (FTSI) branch, covering temporal disaggregation, tensor factorization baselines, recurrent encoders, generative adversarial imputers, score-based diffusion methods, and graph-augmented transformer architectures. Light orange shaded blocks designate the generative Financial Time-Series Synthesis (FTSS) branch, categorized into micro-level asset return synthesis and macro-level market microstructure simulation across conditional GANs, diffusion models, and large market agent simulators. Grey orthogonal branch lines indicate conceptual inheritance. The asterisk ($^*$) indicates that only purely generative model families are enumerated in the synthesis branch.}
\label{fig:taxonomy}
\end{figure}

\newpage

\begin{figure}[!ht]
\centering
\includegraphics[width=0.9\linewidth]{final_figures/comparison_00.png}
\caption{\textbf{Structural comparison of core financial time-series generation tasks.} Schematic representation contrasting the conditioning context, temporal boundary conditions, and output targets across three generation formulations. \textbf{a,} Financial time-series extrapolation (FTSE): conditional forward-looking trajectory continuation, where a past historical prefix (solid line markers) guides probabilistic generation of unobserved future steps (dashed continuation markers). \textbf{b,} Financial time-series imputation (FTSI): closed-format bidirectional reconstruction, where known observed points bound an interval of missing or corrupted values, reconstructed via temporal and cross-sectional conditioning. \textbf{c,} Synthetic sequence generation (FTSS): unconditional or attribute-guided manifold creation from latent Gaussian noise and market condition vectors, simulating realistic synthetic price paths and order books without pointwise historical alignment.}
\label{fig:tasks}
\end{figure}

\newpage

\begin{figure}[!ht]
\centering
\includegraphics[width=0.8\textwidth,keepaspectratio]{final_figures/DiffWithDiscreteContinous.pdf}
\caption{\textbf{Architectural paradigms for adapting foundation models to financial time series.} Schematic diagram contrasting tokenization- and patch-based fine-tuning against model editing and prompt reprogramming. \textbf{a,} Fine-tuning paradigm (left): numerical continuous time series are transformed into discrete token vocabularies via quantile binning or linear patch projection layers, followed by backpropagation through transformer backbones. \textbf{b,} Prompt reprogramming and cross-modal model editing paradigm (right): a frozen foundation model backbone receives auxiliary steering signals from textual market prompts, cross-attention alignment bridges, and lightweight adapter modules, avoiding full-parameter gradient updates while preserving numerical precision. Blue rectangular boxes represent time-series input vectors, purple layered blocks denote multi-head self-attention transformer layers, and green highlighted modules represent learnable cross-modal projection heads.}
\label{fig:llm_comparison}
\end{figure}

\newpage

\begin{figure}[!ht]
\centering
\includegraphics[width=0.8\textwidth,keepaspectratio]{final_figures/FTSA.pdf}
\caption{\textbf{Multi-scale architectural paradigms for financial time-series synthesis.} Conceptual comparison of synthetic sequence generation partitioned into micro-level return modeling and macro-level market simulation. \textbf{a,} Micro-level synthesis (left): univariate or low-dimensional asset return path generation via Wasserstein GANs with temporal convolutional backbones and score-matching diffusion models, engineered to preserve stylized econometric facts such as heavy tails, absence of linear autocorrelation, and volatility clustering. \textbf{b,} Macro-level market simulation (right): multi-agent and limit order book (LOB) queue modeling where generative architectures simulate asynchronous order message flows, bid-ask depth ladders, cross-asset spillover correlations, and macroeconomic counterfactual shock scenarios under regulatory and privacy constraints. Color shading highlights the transition from low-dimensional statistical moments to high-dimensional interactive market topologies.}
\label{fig:ftss_comparison}
\end{figure}

\newpage

\begin{figure}[!ht]
\centering
\includegraphics[width=0.95\textwidth,keepaspectratio]{final_figures/filter_pipeline.png}
\caption{\textbf{Literature identification and PRISMA screening pipeline.} Flow diagram illustrating the systematic retrieval, multi-stage screening, and eligibility determination process compliant with the PRISMA 2020 framework. Blue rectangular boxes represent initial literature identification across primary databases (Google Scholar, DBLP, Web of Science, Scopus, arXiv) and duplicate removal. Orange rectangular boxes denote title and abstract screening stages with corresponding exclusion criteria. Red dashed borders and rightward branching callouts indicate excluded records at each phase along with explicit rationales (non-financial domains, deterministic point forecasting without generative distributions, non-reproducible records). Green solid rectangular boxes indicate the final corpus of 131 retained studies meeting all eligibility criteria, partitioned into three task categories: financial time-series extrapolation, imputation, and data synthesis. Detailed quantitative counts for each filtering phase are recorded in the accompanying retrieval statistics table.}
\label{fig:lit_dist}
\end{figure}

\newpage
\section*{Tables}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Comparison of this survey with the six prior surveys cited in the literature comparison. \textbf{FTSE/FTSI/FTSS} columns use \checkmark/\texttimes\ for whether the sub-task is a first-class topic ($\triangle$ = mentioned but no dedicated section). \textbf{Eval.} distinguishes a unified \emph{protocol} from per-paper \emph{metrics}. \textbf{Datasets} indicates whether financial benchmarks are inventoried. \textbf{\# refs} is the count of references cited in each survey. \textbf{Window} is the publication-year range of each survey's references.}
\label{tab:related_surveys}
\renewcommand{\arraystretch}{1.18}
\begin{tabular}{l c c c l l c l}
\toprule
\textbf{Survey} & \textbf{FTSE} & \textbf{FTSI} & \textbf{FTSS} & \textbf{Eval.} & \textbf{Datasets} & \textbf{\# refs} & \textbf{Window} \\
\midrule
Zhang et al.~\citep{Survey1}            & \checkmark & \texttimes & \texttimes & None    & Multimodal TS & 93  & 2005--2024 \\
Liang et al.~\citep{Survey2}            & \checkmark & $\triangle$ & \texttimes & None    & General TS        & 123 & 2017--2024 \\
Kim et al.~\citep{Survey3}            & \checkmark & \texttimes & \texttimes & Metrics & General TS    & 287 & 2000--2024 \\
Kong et al.~\citep{Survey4}            & \checkmark & \texttimes & \texttimes & Metrics & General TS    & 285 & 2000--2024 \\
Nie et al.~\citep{FinSurvey1}         & \checkmark & \texttimes & \texttimes & Metrics & Financial NLP     & 318 & 2001--2024 \\
Arsenault et al.~\citep{FinSurvey3}         & \checkmark & \texttimes & \texttimes & Metrics & Financial TS      & 115 & 2018--2023 \\
\midrule
\textbf{This survey}      & \checkmark & \checkmark & \checkmark & \textbf{Protocol} & \textbf{Financial TS} & \textbf{131} & \textbf{2021--early 2026} \\
\bottomrule
\end{tabular}
\end{table*}

\vspace{2em}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Unified Task-Technique-Evaluation Mapping for Financial Time-Series Generation.}
\label{tab:mapping}
\resizebox{\textwidth}{!}{
\begin{tabular}{p{2cm} p{2.5cm} p{5.5cm} p{2.5cm} p{3.5cm}}
\toprule
\textbf{Task} & \textbf{Model Family} & \textbf{Representative Approaches} & \textbf{Data Type} & \textbf{Primary Evaluation} \\
\midrule
\multirow{3}{*}{\textbf{Extrapolation}} & Deep Sequence & SGP-LSTM \citep{SGP-LSTM1} & Prices, Returns & MSE, Directional Accuracy \\
 & Diffusion Models & TimeGrad \citep{rasul2021timegrad} & Multivariate Series & CRPS, Predictive Likelihood \\
 & Foundation Models & Chronos \citep{ansari2024chronos}, FinCast \citep{zhu2025fincast} & Multi-asset & Zero-shot MSE, MAE \\
\midrule
\multirow{2}{*}{\textbf{Imputation}} & Tensor Methods & RegTensor \citep{zhou2023regtensor} & Fundamentals & Reconstruction Error \\
 & Diffusion Models & CSDI \citep{tashiro2021csdi}, TimeDiT \citep{TimeDiT} & Missing Prices & Temporal Coherence \\
\midrule
\multirow{3}{*}{\textbf{Synthesis}} & GANs & QuantGAN \citep{wiese2020} & Returns & Stylized Facts \\
 & Diffusion Models & CoFinDiff \citep{tanaka2025cofindiff} & Scenarios & Distributional Fidelity \\
 & Controllable & Market-GAN \citep{xia2024market} & Order Flow (LOB) & Downstream Utility \\
\bottomrule
\end{tabular}
}
\end{table*}

\vspace{2em}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Comprehensive Comparison of Financial Time-Series Extrapolation Paradigms}
\label{tab:extrapolation_comparison}
\renewcommand{\arraystretch}{1.3}
\resizebox{\textwidth}{!}{
\begin{tabular}{lp{3.5cm}p{4cm}p{4.5cm}p{4cm}}
\toprule
\textbf{Paradigm} & \textbf{Output Format} & \textbf{Key Strengths} & \textbf{Primary Limitations} & \textbf{Typical Financial Use Cases} \\
\midrule
\textbf{Predictive Models} & Deterministic point forecasts & Extremely fast inference; highly interpretable methodologies. & Lacks robust uncertainty quantification; fails to model tail risks. & High-frequency trading; algorithmic order execution. \\
\textbf{GANs} & Sampled stochastic trajectories & Produces sharp, multi-modal sequences without iterative sampling. & Prone to severe training instability and mode collapse. & Scenario generation; counterfactual stress testing. \\
\textbf{Diffusion Models} & Sampled stochastic trajectories & Achieves strong distributional fidelity and stable convergence. & Iterative sampling process induces prohibitive computational latency. & Risk management; complex portfolio optimization. \\
\textbf{Foundation Models} & Zero-shot probabilistic forecasts & Exhibits promising cross-domain generalization and rapid adaptability. & Suffers from potential hallucination and difficult text-series data alignment. & Dynamic asset allocation; broad market backtesting. \\
\bottomrule
\end{tabular}
}
\end{table*}

\newpage

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Comparative Analysis of Financial Time-Series Imputation Frameworks}
\label{tab:imputation_comparison}
\renewcommand{\arraystretch}{1.3}
\resizebox{\textwidth}{!}{
\begin{tabular}{lp{3.5cm}p{4cm}p{4.5cm}}
\toprule
\textbf{Methodology} & \textbf{Core Mechanism} & \textbf{Key Advantages} & \textbf{Primary Limitations} \\
\midrule
\textbf{Matrix/Tensor Completion} & Low-rank factorization & Highly computationally efficient; effective on sparse data. & Fails to capture non-linear volatility clustering. \\
\textbf{Tree-Based (Random Forest)} & Non-linear ensemble regression & Extremely robust to high missing rates (e.g., 50\%+). & Struggles with high-frequency temporal dependencies. \\
\textbf{GAN-Based Models} & Adversarial distribution matching & Sharp interpolations; mitigates drift via gradient penalties. & Mode collapse; unstable training on extreme outages. \\
\textbf{Diffusion Models} & Conditional reverse denoising & State-of-the-art fidelity; preserves cross-asset correlations. & High inference latency prohibitive for real-time systems. \\
\textbf{LLM/Graph Frameworks} & Bipartite graphs \& textual prompting & Excels on mixed-type (categorical/numerical) tabular data. & Heavy computational footprint; context-window limits. \\
\bottomrule
\end{tabular}
}
\end{table*}

\vspace{2em}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Comparative Overview of Financial Time-Series Synthesis Paradigms}
\label{tab:synthesis_comparison}
\renewcommand{\arraystretch}{1.3}
\resizebox{\textwidth}{!}{
\begin{tabular}{lp{3.5cm}p{4cm}p{4.5cm}}
\toprule
\textbf{Model Paradigm} & \textbf{Core Methodology} & \textbf{Primary Strengths} & \textbf{Key Limitations} \\
\midrule
\textbf{Wasserstein GANs} & Adversarial minimax game with Earth Mover's distance & Rapid inference; highly effective for targeted metric alignment. & Susceptible to mode collapse; struggles with microstructural nuance. \\
\textbf{Diffusion \& SDEs} & Iterative forward noising and reverse score-matching & Exceptional distributional fidelity; easily integrates financial priors (e.g., GBM). & Computationally intensive sampling process limits real-time utility. \\
\textbf{Large Market Models} & Transformer-based discrete sequence modeling & Scales to asynchronous tick data; enables interactive agent simulation. & Requires massive computational infrastructure and vast order-flow datasets. \\
\textbf{DP-Generators} & Differentially private gradient clipping & Ensures strict regulatory compliance and data anonymization. & Incurs a harsh trade-off between privacy bounds ($\epsilon$) and data utility. \\
\bottomrule
\end{tabular}
}
\end{table*}

\vspace{2em}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{A Unified Evaluation Checklist for Financial Time-Series Generation.}
\label{tab:evaluation_checklist}
\resizebox{\textwidth}{!}{
\begin{tabular}{p{3.5cm} p{5.5cm} p{3.5cm} p{4cm}}
\toprule
\textbf{Evaluation Dimension} & \textbf{Key Metrics / Methods} & \textbf{Primary Task Suitability} & \textbf{Reflects Financial Validity?} \\
\midrule
\textbf{Point-wise Accuracy} & MSE, MAE, RMSE, MAPE & Extrapolation, Imputation & Low (Ignores market dynamics) \\
\textbf{Distributional Fidelity} & Wasserstein Distance, MMD, KL Divergence, Cross-Correlation & Synthesis, Extrapolation & Medium (Captures static shapes) \\
\textbf{Stylized Facts} & ACF of squared returns, Tail Index, Kurtosis, Leverage effect tests & Synthesis, Simulation & High (Domain-specific realism) \\
\textbf{Downstream Utility} & Train-on-Synthetic, Test-on-Real (TSTR); Train-on-Real, Test-on-Real (TRTR); Sharpe Ratio of trained agents; VaR/ES accuracy & All Tasks & High (Practical applicability) \\
\textbf{Privacy \& Security} & Distance-to-Closest Record (DCR), Differential Privacy $\epsilon$ & Synthesis (Data Sharing) & N/A (Focuses on compliance) \\
\bottomrule
\end{tabular}
}
\end{table*}

\newpage

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Summary of Recent Financial Time-Series Generation Benchmarks.}
\label{tab:datasets}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{3.5cm} c p{4.5cm} c c}
\toprule
\textbf{Dataset / Benchmark} & \textbf{Tasks} & \textbf{Primary Data Sources} & \textbf{Split Ratio} & \textbf{Scale (Approx.)} \\
\midrule
\textbf{FinTSB} \citep{hu2025fintsb} & Extrapolation / Imputation & Chinese A-share, NYSE, NASDAQ & 7:1:2 & 150M points \\
\rowcolor{gray!10}
\textbf{GIFT-EVAL} \citep{GIFT-EVAL} & Forecasting & 27 datasets (Stock, Crypto, FX, etc.) & Various & 144,380 series \\
\textbf{TFB} \citep{TFB} & Forecasting & Broad finance domain & Various & 2.1M series \\
\rowcolor{gray!10}
\textbf{FinTSBridge} \citep{wang2025fintsbridge} & Extrapolation / Imputation & S\&P 500, NASDAQ, FTSE, CSI 300 & 7:1:2 & 86,978 series \\
\textbf{TSGBench} \citep{ang2023tsgbench} & Synthesis / Simulation & GOOGL, Global Exchanges & 9:0:1 & 13,213 series \\
\textbf{CTBench} \citep{ang2025ctbench} & Synthesis & Cryptocurrencies on Binance & 9:0:1 & 530 series \\
\textbf{FinMultiTime} \citep{FinMultiTime} & Extrapolation & S\&P 500, HS 300 & N/A & 6M points \\
\bottomrule
\end{tabular}%
}
\end{table*}

\vspace{2em}

\begin{table*}[!ht]
\centering
\scriptsize
\caption{Literature Retrieval and Screening Statistics.}
\label{tab:retrieval_stats}
\begin{tabular}{p{5cm} p{2cm} p{2cm} p{3cm}}
\toprule
\textbf{Screening Stage} & \textbf{Count} & \textbf{Retained} & \textbf{Primary Reason for Exclusion} \\
\midrule
Initial Keyword Search & 2,568 & 2,568 & N/A \\
Title/Abstract Screening & 2,568 & 604 & Not financial data / Pure deterministic forecasting \\
Full-text Review & 604 & \textbf{131} & Weak financial validation / Duplicated preprints \\
\midrule
\multicolumn{4}{l}{\textbf{Primary Task Assignment}} \\
\quad Extrapolation & & 54 & \\
\quad Imputation & & 29 & \\
\quad Synthesis / Simulation & & 47 & \\
\bottomrule
\end{tabular}
\begin{flushleft}
\textit{Note: Each of the 131 retained studies is assigned to exactly one primary task category; cross-cutting contributions (e.g., forecasting with imputation or synthesis with scenario conditioning) are discussed in the relevant sections but are not double-counted.}
\end{flushleft}
\end{table*}

\end{document}
