# npj Artificial Intelligence 第三轮修改内容清单（Change List）

> **稿件编号**：5a3d24b0-5398-4e89-835d-34efbe8bd9e3
> **目标期刊**：npj Artificial Intelligence（Nature Portfolio）
> **基线版本**：`2675d80`（2026-09-10）
> **当前版本**：`d90bd4d`（2026-09-13），已推送至 `origin/main`
> **编写日期**：2026-09-13
> **交付物快照**：`is26.pdf` 55 页 / `is26_marked.pdf` 59 页 / 参考文献 222 条 / 语料 131 篇

---

## 一、总体概览

本轮修改共 **6 个提交**，覆盖 **29 个源文件**，代码层面 `+1750 / −432` 行（不含 `.bbl`、`.pdf`、编译中间产物）。

| # | 提交 SHA | 日期 | 提交说明 |
|---|---|---|---|
| 1 | `a729e33` | 2026-09-11 | `refactor(技术审查)`：第三轮修改——Results/Methods 仅保留主标题并全量转散文 |
| 2 | `f9c365b` | 2026-09-11 | 修正第三轮回复信中的数字，使其与稿件实际内容一致 |
| 3 | `a364c9f` | 2026-09-13 | `feat(引用审计)`：移植 clean 版全部增量内容（96 条经典文献、具名归属、PRISMA 严谨性、过度声明收敛） |
| 4 | `906bcac` | 2026-09-13 | `fix(标记稿)`：移除 33 个空白标记，标记稿内容向修订后的 `is26` 看齐 |
| 5 | `b1c088d` | 2026-09-13 | `fix(标记稿)`：块级删除补上红色删除线，观感对齐 latexdiff redline |
| 6 | `d90bd4d` | 2026-09-13 | `fix(回信数字)`：语料 130→131，参考文献数改为实际 222，清理 `.DS_Store` |

**三条主线**：

1. **结构合规化**（`a729e33`）——按编辑部意见拆除冗余层级、全量转散文，使 Results/Methods 只保留期刊允许的主标题。
2. **内容实质增补**（`a364c9f`）——从 clean 版移植 96 条经典文献与全部内容增量，参考文献 129 → 222 条。
3. **标记稿观感修正**（`906bcac` → `b1c088d` → `d90bd4d`）——修复"删除内容没有删除线、读起来像正文"这一核心缺陷，并对齐所有伴生文件的数字。

---

## 二、逐项修改清单

### A. 稿件结构与行文（`a729e33`）

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| A1 | 标题层级 | 删除 Results 下 **38 个二级小标题**、5 个空壳标题；最终仅保留 4 个 Methods 主标题（全文 `\subsection` 共 11 处：Results 7 + Methods 4），无 `\subsubsection` | `sections/03`–`sections/10` |
| A2 | 列表转散文 | 拆解 **14 组 `itemize` / `enumerate` 列表**，覆盖 49 个加粗标签，全部改写为连续自然段 | `sections/04`–`sections/10` |
| A3 | 标记机制 | 新增 `\revnote` 宏（清洁版为空操作，标记版渲染为红色方括号提示），在 47 处删除位置植入 | `is26.tex`、`is26_marked.tex` |
| A4 | 术语统一 | 清除全部 8 处 "subsection" 表述，统一改称 "section" | `sections/*.tex` |
| A5 | 摘要规范 | 摘要归一为 **181 words**，单段无子标题 | `sections/00_abstract.tex` |
| A6 | 排版收敛 | 添加 `\tolerance` / `\emergencystretch`；数据集表格加 `\resizebox`（消除 52pt 溢出）；Overfull hbox 由 5 处降至 2 处 | `is26.tex`、`sections/14_tables.tex` |
| A7 | 版本号同步 | 首页说明框升级至 **v2.3** 并改用 A/B 双栏；`Cover_Letter.tex` 日期与版本同步；`Supplementary_Information_PRISMA.tex` 更新 7 个定位单元格 | `is26.tex`、`is26_marked.tex`、`Cover_Letter.tex`、`Supplementary_Information_PRISMA.tex` |
| A8 | 新增交付物 | 新建第三轮技术审查回复信与自查脚本 | `Response_to_Technical_Check_Round3.tex`、`check_red.py` |

### B. 回复信数字校准（`f9c365b`）

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| B1 | 事实订正 | 「删除 5 个空标题」→ 实际仅 **1 个**真正空标题（尾部 `Summarization`） | `Response_to_Technical_Check_Round3.tex` |
| B2 | 事实订正 | 「47 个二级标题位置」→ 拆分为 **38 处**（37 个有内容 + 1 个空）+ **9 处**列表转换 = **47 处红色标注** | `Response_to_Technical_Check_Round3.tex` |
| B3 | 一致性同步 | 将同一口径同步至标记稿说明框与投稿信 | `is26_marked.tex`、`Cover_Letter.tex` |
| B4 | 文件名订正 | 投稿信中过期的回复信文件名更新为 `Response_to_Technical_Check_Round3.pdf` | `Cover_Letter.tex` |

### C. 文献与内容增补（`a364c9f`）

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| C1 | 文献扩容 | `ultimate_complete.bib` **207 → 303** 条（新增 96 条经典文献与数据源条目）；正文引用 **129 → 222** 条 | `ultimate_complete.bib`、`is26.bbl` |
| C2 | 篇幅变化 | `is26.pdf` **47 → 55 页** | `is26.pdf` |
| C3 | 语料口径统一 | 语料数统一为 **131 篇 = 54（外推）+ 29（插补）+ 48（合成）**，修正原 47 导致的 130 算术矛盾 | `sections/02_methodology.tex`、`sections/14_tables.tex`、`sections/01_introduction.tex`、`sections/10_challenges.tex` |
| C4 | 缺陷修复 | 修正 `\citep{TimeDiT}` → `\citep{cao2024timedit}`（原渲染为 `[? ]`） | `sections/14_tables.tex` |
| C5 | 缺陷修复 | 修正 `brown1992survivorship` 的 DOI：`10.59629/rfs/5.4.553` → `10.1093/rfs/5.4.553` | `ultimate_complete.bib` |
| C6 | 样式修复 | `sn-nature.bst` 的 `FUNCTION {misc}` 增加 `new.block + format.url output`，使 13 条新增 `@misc` 数据源条目正确输出 URL | `sn-nature.bst` |

### D. 标记稿清理（`906bcac`）

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| D1 | 死标记清理 | 移除 **33 个空白标记**（如 `\revdelblock{  }`）——Latexdiff 无法对齐不透明宏 token 时产生的残留 | `sections/*.tex` |
| D2 | 脚本修复 | `revconvert.drop_empty_markers()` 改用 shell-stripping（而非直接删除）以保留空格语义；修复 3 处重复定义的 `_pristine_map` | `/tmp/redline_analyze/revconvert.py` |
| D3 | 内容对齐 | 语料 130 → 131、Synthesis 47 → 48 同步到标记稿；清除残留的 `\cite{TimeDiT}` | `sections/*.tex` |
| D4 | 标记量变化 | `\revaddblock` 454 → 449，`\revdelblock` 381 → 353 | `sections/*.tex` |

### E. 标记稿观感修正（`b1c088d`）—— 本轮核心缺陷

> **问题**：用户反馈「marked 版本没有用红色的横线把改动划去，也没有用蓝字把更正后的内容写上；还是红色的字，蓝色的标注，这不符合要求」。
>
> **根因**：`\revdelblock` / `\revaddblock` 的载荷内含 2 个以上连续换行，而 **ulem 无法跨段落实线删除线**，转换器于是退化为纯 `{\color{red}#1}` 平面着色。4 个被删除的贡献点小标题（`A Task-Technique Two-Level Taxonomy:`、`A Systematic Evaluation Framework:`、`A Comprehensive Resource Synthesis:`、`Identification of Open Challenges:`）打印出来与正文无异。

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| E1 | 段落切分 | 保留外层块包装，按 `PARA = re.compile(r'(?:\n[ \t]*){2,}')` 切分载荷，对每个存续文本段包裹 `\markdel{}` / `\markadd{}` | `sections/*.tex` |
| E2 | 覆盖度 | **43 个块标记 → 69 个已装饰片段**，遗留 8 处平面着色（均为 `\subsection{...}` 载荷，ulem 无法对其画删除线；latexdiff redline 存在完全相同的限制） | `sections/*.tex` |
| E3 | 中性性验证 | `decorate_blocks.py` 逐文件断言 `expand_clean(before) == expand_clean(after)`，10 个受影响文件全部 `clean-identical=True` | `/tmp/redline_analyze/decorate_blocks.py` |
| E4 | 校验脚本 | `landed2.py` 补入 `MARKDEL` / `MARKADD` 正则与对应的 `unwrap()` 调用，消除 4 处误报 | `/tmp/redline_analyze/landed2.py` |

### F. 数字校准与仓库清理（`d90bd4d`）

| 序号 | 类别 | 修改内容 | 涉及文件 |
|---|---|---|---|
| F1 | 语料订正 | 130 → 131（3 处） | `response_to_reviewers_final.md` |
| F2 | 合成数保持 | Synthesis 保留 **48**（用户明确指示；redline 的「131 studies … synthesis (47)」自相矛盾，54+29+47=130 ≠ 131，我方 54+29+48=131 自洽） | `sections/*.tex`、`response_to_reviewers_final.md` |
| F3 | 文献数订正 | 回信中的「130 entries」为历史值，改为实际 **222** 条（共 4 处：`response_to_reviewers_final.md` ×2、`response_to_technical_check_final.md` ×1、`response_to_reviewers_final_v1.0.md` ×1） | 4 个 `.md` 文件 |
| F4 | 页码订正 | 回复信中的页码 47/50 → **55/59** | `Response_to_Technical_Check_Round3.tex` |
| F5 | 仓库清理 | `git rm --cached final_figures/.DS_Store`（自 `a364c9f` 起被纳入版本控制）；磁盘上删除 4 个 `.DS_Store`；新增 `.gitignore` | `.gitignore`、`final_figures/.DS_Store` |

---

## 三、标记稿技术规范

### 3.1 标记宏定义（`is26_marked.tex`）

| 宏 | 渲染效果 | 用途 |
|---|---|---|
| `\revadd` / `\revadddec` | 蓝色 + 波浪下划线 | 行内新增 |
| `\revdel` / `\revdeldec` | 红色 + 删除线 | 行内删除 |
| `\revaddblock` | 蓝色（外层容器） | 块级新增 |
| `\revdelblock` | 红色（外层容器） | 块级删除 |
| `\markadd` | 蓝色 + 波浪下划线 | 块内新增片段 |
| `\markdel` | 红色 + 删除线 | 块内删除片段 |
| `\citebox` | `\mbox{#1}` | 阻断 natbib 空格吸收 |
| `\revnote` | 红色小字方括号 | 块级说明（当前 0 处使用） |

### 3.2 关键设计约束

- **`\citebox` 间接层**：`is26.tex` 中定义为恒等 `#1`，`is26_marked.tex` 中定义为 `\mbox{#1}`，覆盖 **42 处**引用点。此设计解决了 `\mbox{}` 阻断 natbib 空格吸收导致的 **40 字符 PDF 漂移**问题。
- **中文禁入 `.tex`**：含中文的 `.tex` 会导致 pdflatex 失败。
- **重建 `is26.pdf` 前置条件**：必须确保 `is26.tex` 中 `\citebox` 定义为恒等 `#1`，否则 40 字符漂移复现。
- **新标记宏必须双稿同步定义**，并同步登记到等价性校验脚本，否则验证会产生误报。

---

## 四、验证证据

### 4.1 内容等价性

| 校验项 | 结果 |
|---|---|
| `landed2.py` 文件级等价性 | **15 / 15 OK** |
| 合并字符数 | **139,776 字符** |
| 不一致文件数 | **0** |
| `expand_clean` 逐文件中性性断言 | 10 / 10 `clean-identical=True` |

### 4.2 清洁版未受影响

| 校验项 | 结果 |
|---|---|
| `is26.pdf` 页数 | **55 页** |
| `is26.pdf` 文本字符数 | **156,848 字符** |
| 与修改前基准 `/tmp/is26_before_land.pdf` 比对 | **完全一致：True** |
| sha256 | `044bb16ea1c56f80a24228641063ea19ca6db394951b0e44faa171cb39717712` |
| 渲染的语料表述 | `final corpus comprises 131 studies ... synthesis (48)` |
| 最大渲染文献序号 | **222** |

### 4.3 编译健康度

| 校验项 | 结果 |
|---|---|
| `is26_marked.pdf` 页数 | **59 页** |
| 编译错误 | **0** |
| 未定义引用 | **0** |
| `is26.bbl` / `is26_marked.bbl` 条目数 | 均为 **222 条**，集合与顺序完全一致 |
| `ultimate_complete.bib` 条目总数 | **303 条**（其中 222 条被引用） |
| `is26.pdf` Overfull hbox | 2 处（原 5 处） |

### 4.4 标记合规性普查（严格限定 `#ff0000` / `#0000ff`）

> 注意：超链接色为 `#0000b3`（= `blue!70!black`），不计入标记统计。

| 标记类型 | 本稿（`is26_marked.pdf`） | latexdiff redline | 差异 |
|---|---|---|---|
| 蓝色波浪线（`#0000ff`） | 2680 | 2691 | −11 |
| 红色删除线（`#ff0000`） | 1440 | 1466 | −26 |
| 蓝色平面着色 | 2453 | 2388 | +65 |
| 红色平面着色 | 2471 | 2472 | −1 |

**结论**：与编辑部期望的 latexdiff redline 观感已基本对齐。

### 4.5 标记宏用量统计（`sections/*.tex`）

| 宏 | 数量 |
|---|---|
| `\revadddec` | 437 |
| `\revdeldec` | 322 |
| `\revdelblock` | 31 |
| `\markdel` | 49 |
| `\markadd` | 20 |
| `\revaddblock` | 12 |
| `\citebox` | 42 |
| `\revnote` | 0 |
| `\mbox` | 0 |

---

## 五、伴生交付物状态

| # | 文件名 | 页数 | 状态 |
|---|---|---|---|
| 1 | `is26.pdf`（清洁主稿） | 55 | ✅ 未受影响，文本与基准完全一致 |
| 2 | `is26_marked.pdf`（标记稿） | 59 | ✅ 0 错误、0 未定义引用 |
| 3 | `Cover_Letter.pdf`（投稿信） | 3 | ✅ 版本号与日期已同步至 v2.3 |
| 4 | `Supplementary_Information_PRISMA.pdf` | 3 | ✅ 7 个定位单元格已更新 |
| 5 | `Response_to_Technical_Check_Round3.pdf` | 4 | ⚠️ 页码已修正；**标记描述部分已过时**（见第六节） |
| 6 | `separate_figures/`（独立高清图） | — | ✅ 6 张图，无嵌入图题 |

---

## 六、待决事项（待确认，尚未修改）

| # | 事项 | 详情 | 影响面 | 处置 |
|---|---|---|---|---|
| **1** | **Round3 回复信 Point 2 描述已过时** | 信中承诺「38 个红色方括号标记 `[Secondary subheading "..." removed.]`」+ 说明框 + 「28 页内 111 处红色字」。当前 PDF 实际情况为：**0 个标记、0 个说明框、2471 处红色族 span 分布于 39 页**。该描述在 `f9c365b` 时点为真，其后标记体系被 redline 风格约定取代，但信件未同步更新 | `Response_to_Technical_Check_Round3.tex` Point 2 | **用户已指示「这封信目前不用提交」，暂缓处理** |
| **2** | **Table 1 `# refs` 列语义存疑** | `sections/14_tables.tex:20` 渲染为 **131**，但实际参考文献为 **222** 条。表注称该列为 "count of references cited in each survey"，若确指「该综述引用的参考文献数」则应为本综述实际引用数；若意指「被审阅的论文数」则 131 正确。redline 版同样写作 131 | `sections/14_tables.tex:20` | **需用户决策** |
| **3** | **`\revnote` 宏已定义但 0 处使用** | 该宏在两份主稿中均有定义（清洁版空操作 / 标记版红色方括号），但 `sections/*.tex` 中已无任何调用点（`a729e33` 植入的 47 处已在后续重构中移除） | `is26.tex`、`is26_marked.tex` | 可保留（无害）或删除定义 |
| **4** | **`技术审查核对清单.md` 自身数字已过时** | 该文件仍写 `is26.pdf` 49 页 / `is26_marked.pdf` 51 页 / 133 篇文献 / 回复信 8 页 / 投稿信 2 页，均为 `a729e33` 之前的旧值 | `技术审查核对清单.md` | 可选刷新，本轮未在范围内 |

---

## 七、上传清单（SNAPP 系统对应关系）

| 上传位 | 文件 | 说明 |
|---|---|---|
| Manuscript File（清洁版） | `is26.pdf` | 55 页，IRDM 结构，顺序编号参考文献，图表置于参考文献之后 |
| Related Manuscript File（标记版） | `is26_marked.pdf` | 59 页，蓝色波浪线 = 新增，红色删除线 = 删除 |
| Response to Technical Check | `Response_to_Technical_Check_Round3.pdf` | 4 页（**暂不提交**，见第六节待决事项 1） |
| Cover Letter | `Cover_Letter.pdf` | 3 页，含 Figures 1–6 原创性声明 |
| Supplementary Information | `Supplementary_Information_PRISMA.pdf` | 3 页，PRISMA 2020 检查单 |
| Individual Figure Files | `separate_figures/Figure1–6.*` | 6 张独立高清图，已去除嵌入图题与图注 |

---

*本清单由本轮提交历史（`2675d80..d90bd4d`，6 个提交 / 29 个源文件 / +1750 −432 行）与实测编译产物交叉核对生成。*
